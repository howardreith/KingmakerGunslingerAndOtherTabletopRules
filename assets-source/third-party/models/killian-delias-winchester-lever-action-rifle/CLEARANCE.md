# Exact payload clearance

Disposition: **APPROVED — SOURCE CONTROL AND DERIVATIVES**, for Advanced Rifle
only.

The user attests that the preserved FBX and five textures came from *Winchester
lever action rifle* by Killian Delias (`Killian_Delias`), Sketchfab UID
`678f6e091d7149da8fce413b6fd31288`, CC-BY-4.0. Independent hashing matches all
six files to `asset-provenance.json` and `ORIGINAL-FILE-HASHES.sha256`.
Read-only FBX evidence (`fusil winchester v3.mb`, `FusilALevier`, and
`fusilALevier`) is consistent with that attested Winchester/lever-action source.

The adjacent `original-download/gunslinger-fourth-playtest-handoff.zip` in the
incoming folder is a playtest handoff archive, not an untouched Sketchfab model
archive, and is not used as the binary identity bridge. User attestation, exact
hashes, embedded identity, corrected source UID, and preserved provenance
history form the clearance record. The former Martini-Henry/ASHISH attribution
is retained only in `provenance-history`.
