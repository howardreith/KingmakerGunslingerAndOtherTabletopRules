diff --git a/BUILD-INFO.txt b/BUILD-INFO.txt
index d2ce277d32958c89277d3ce896404f155937ff9e..1e2c7ec34f4cb8e0ac87a52379e07214888627e0 100644
--- a/BUILD-INFO.txt
+++ b/BUILD-INFO.txt
@@ -1,6 +1,6 @@
 Project: Kingmaker Gunslinger
-Milestone: Sprint 22 loaded-round attack enforcement and token durability
-Version: 0.0.22-s22-loaded-round-enforcement
+Milestone: Sprint 22 attack-hook binding and weapon-only reconciliation repair
+Version: 0.0.22.1-s22-attack-hook-repair
 Target game: Pathfinder: Kingmaker Enhanced Plus Edition 2.1.7b
 Unity Mod Manager reference: 0.32.4
 Harmony reference: 1.2.0.1
@@ -8,10 +8,16 @@ Target framework: .NET Framework 4.7
 C# language version: 7.3
 Platform: AnyCPU
 Configuration: Release
+Compiler: Roslyn 3.11.0-4.21403.6
 Warnings as errors: true
-Deterministic compiler mode: true
+Deterministic compiler mode: true; DLL and PDB byte-identical across two same-output-path builds
 Active custom blueprints: 11
 Reserved custom blueprints: 1
-Exact dependency-free tests: 446 x 3 runs
-Kingmaker runtime status: UMM smoke-test candidate; quicksave durability and attack discharge qualification pending
-Private Kingmaker/Unity/UMM/Harmony assemblies are not redistributed.
+Exact dependency-free tests: 455 x 3 runs; 0 failures; byte-identical output
+Main DLL SHA-256: 36f46686856d2f0b04bf91ddd9665b47d9c6676de96af428bd0d697ec32fa463
+Standalone UMM package: KingmakerGunslinger-0.0.22.1-attack-hook-repair-smoke-test.zip
+Standalone UMM ZIP SHA-256: 83423ad78a5e62dd4e329c07c1f5ddb20b9bd49567690e5bab6f9495e7263541
+Artifact status: READY FOR KINGMAKER Sprint 22 repair smoke test
+Kingmaker runtime acceptance: pending for this exact repair build
+Sprint 23 status: blocked until the complete repair smoke-test gate passes
+Private Kingmaker/Unity/UMM/Harmony/Newtonsoft assemblies are compiler input only and are not redistributed.
diff --git a/BUILD-QUALIFICATION.json b/BUILD-QUALIFICATION.json
index df901bea9e8db997ecc526c66f2b1d63da327238..8ee48a88c9d53eb026093597b343ff4885e46545 100644
--- a/BUILD-QUALIFICATION.json
+++ b/BUILD-QUALIFICATION.json
@@ -1,27 +1,51 @@
 {
-  "schemaVersion": 1,
-  "modVersion": "0.0.22",
-  "informationalVersion": "0.0.22-s22-loaded-round-enforcement",
-  "targetGame": "Pathfinder: Kingmaker 2.1.7b",
-  "managerVersion": "0.32.4",
-  "targetFramework": ".NET Framework 4.7",
-  "languageVersion": "7.3",
+  "activeBlueprints": 11,
+  "blueprintManifestSha256": "af56f2a35bd05e055e8feee1e996d37abcb20b76efb90762784f31ccfe933337",
+  "compilerSha256": "f1b8f79f1761971b82a6d8a8f3fe82c45c6000b33a0a707fca532daebfca0740",
+  "compilerStderrEmpty": true,
   "compilerVersion": "3.11.0-4.21403.6",
   "configuration": "Release",
-  "warningsAsErrors": true,
+  "declaredTests": 455,
+  "deterministicCompilerMode": true,
   "exactReferenceCompileExitCode": 0,
-  "compilerStderrEmpty": true,
-  "modDllSha256": "25f8e1a25cf7871591f5dc3778b0732ebb5f03edec7b79c62775d05bf07b1660",
-  "blueprintManifestSha256": "af56f2a35bd05e055e8feee1e996d37abcb20b76efb90762784f31ccfe933337",
-  "installPackageSha256": "5a6fd105e78c0a8850b6988c4d1ee0edb7b4f6460d61a30a14db17d43d0447cd",
-  "declaredTests": 446,
-  "testRuns": 3,
-  "testFailures": 0,
-  "testExecutableSha256": "736f2b37cbeb1629442228c7c3564d7f2c964eac36ee6d806d00b316f00b31eb",
-  "testOutputSha256": "209f26caf959cc7a88015de5a98452a9d594a40962b982800b9a9e89cfdeb401",
-  "repeatedOutputIdentical": true,
-  "activeBlueprints": 11,
-  "reservedBlueprints": 1,
+  "harmonyVersion": "1.2.0.1",
+  "informationalVersion": "0.0.22.1-s22-attack-hook-repair",
+  "installPackageBinaryCount": 1,
+  "installPackageEntryCount": 8,
+  "installPackageFileName": "KingmakerGunslinger-0.0.22.1-attack-hook-repair-smoke-test.zip",
+  "installPackageSha256": "83423ad78a5e62dd4e329c07c1f5ddb20b9bd49567690e5bab6f9495e7263541",
+  "installPackageSizeBytes": 136303,
+  "installPackageValidationPassed": true,
+  "languageVersion": "7.3",
+  "managerVersion": "0.32.4",
+  "milestone": "Sprint 22 attack-hook binding and weapon-only reconciliation repair",
+  "modDllSha256": "36f46686856d2f0b04bf91ddd9665b47d9c6676de96af428bd0d697ec32fa463",
+  "modDllSizeBytes": 339456,
+  "modMvid": "b994b27e-9fb3-4229-acdf-cda1cd633610",
+  "modPdbSha256": "b76d3cb21da6d21154d756a95266cadb6e9c06b2790576be0b53b10a62ff2ee7",
+  "modPdbSizeBytes": 96984,
+  "modVersion": "0.0.22.1",
+  "platform": "AnyCPU",
+  "privateReferenceManifestSha256": "c012266487888df1025577402234841e365ace5d1dc18e61cc1fa89cfa9c5c04",
+  "privateReferenceManifestVerified": true,
+  "privateReferencesRedistributed": false,
   "readyForKingmakerSmokeTest": true,
-  "runtimeQualification": "quicksave token durability, loaded-round consumption, empty/wrecked forced misses, broken discharge, and native Heavy Crossbow isolation pending"
+  "reservedBlueprints": 1,
+  "runtimeAcceptancePassed": false,
+  "runtimeQualification": "0.0.22.1 must still prove attack-hook execution, one-round consumption, empty/Broken/Wrecked behavior, fault-free token reconciliation, full save/restart durability, and native Heavy Crossbow isolation in Kingmaker",
+  "sameOutputPathCompileRuns": 2,
+  "sameOutputPathDllByteIdentical": true,
+  "sameOutputPathPdbByteIdentical": true,
+  "schemaVersion": 1,
+  "sourceInvariantValidationPassed": true,
+  "sprint23EntryApproved": false,
+  "targetFramework": ".NET Framework 4.7",
+  "targetGame": "Pathfinder: Kingmaker 2.1.7b",
+  "testExecutableSha256": "c613af5456939c225952014acb5b3927ead3f199628f731eb8d360baf48ec042",
+  "testFailures": 0,
+  "testOutputRepeatedByteIdentical": true,
+  "testOutputSha256": "5e004b6f22411a12e4d4706b4b7c21822e43ffbfb3a47b1d57310429339d9148",
+  "testRuns": 3,
+  "testSourceSetSha256": "5030a858d2a39199062733ee75655cafebf2e1436fb4481fdeb3024385d67207",
+  "warningsAsErrors": true
 }
diff --git a/CHANGELOG.md b/CHANGELOG.md
index a6d43f5bc4538c2947d3687e94980720defb94b3..cfda19c2bcd828c370fcca0d3eaed51259521ea2 100644
--- a/CHANGELOG.md
+++ b/CHANGELOG.md
@@ -1,5 +1,16 @@
 # Changelog
 
+## 0.0.22.1-s22-attack-hook-repair
+
+- Evaluated the supplied 0.0.22 Kingmaker result and kept Sprint 23 blocked.
+- Corrected the Harmony target contract from an assumed zero-argument `OnTrigger()` to the exact installed `void OnTrigger(RulebookEventContext)` signature used by `RuleAttackRoll`, `RuleAttackWithWeapon`, and `RuleCalculateAC`.
+- Added a fail-closed executable reflection predicate and nine regression cases covering the exact accepted callback shape and rejected alternatives.
+- Limited native `ItemEntity.ApplyEnchantments()` firearm-state inspection to `ItemEntityWeapon`, preventing the observed `ItemEntityShield` reflection faults while retaining the exact-token restoration path.
+- Corrected the Windows runtime-contract inspection script so future qualification checks enforce the installed one-argument callback contract.
+- Preserved the item-owned inert `BlueprintWeaponEnchantment` state carrier, atomic reload transaction, loaded/empty/Broken/Wrecked discharge decisions, exact firearm marker, and native Heavy Crossbow isolation.
+- Raised the dependency-free exact .NET Framework 4.7 suite from 446 to 455 cases.
+- Added no natural-roll, forced-roll, misfire, condition-transition, explosion, automatic-reload, weapon-content, or Gunslinger-class behavior.
+
 ## 0.0.22-s22-loaded-round-enforcement
 
 - Fixed equipped loaded-state loss during quicksave and native item-enchantment refresh.
diff --git a/Directory.Build.props b/Directory.Build.props
index a4cd512245f8fc0b9c328c40dd34fe61a005653a..38dc649ef675646385883212890c125a5ff7a011 100644
--- a/Directory.Build.props
+++ b/Directory.Build.props
@@ -1,8 +1,8 @@
 <?xml version="1.0" encoding="utf-8"?>
 <Project ToolsVersion="15.0" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
   <PropertyGroup>
-    <KmgVersion>0.0.22</KmgVersion>
-    <KmgInformationalVersion>0.0.22-s22-loaded-round-enforcement</KmgInformationalVersion>
+    <KmgVersion>0.0.22.1</KmgVersion>
+    <KmgInformationalVersion>0.0.22.1-s22-attack-hook-repair</KmgInformationalVersion>
     <LangVersion>7.3</LangVersion>
     <PlatformTarget>AnyCPU</PlatformTarget>
     <Prefer32Bit>false</Prefer32Bit>
diff --git a/Info.json b/Info.json
index 01ace050202c9ad483b2c196179fc431524036f2..db5f7999de5eb9fa9a2a394cf55f479db292d210 100644
--- a/Info.json
+++ b/Info.json
@@ -2,7 +2,7 @@
   "Id": "KingmakerGunslinger",
   "DisplayName": "Kingmaker Gunslinger",
   "Author": "Howie",
-  "Version": "0.0.22",
+  "Version": "0.0.22.1",
   "ManagerVersion": "0.32.4",
   "Requirements": [],
   "AssemblyName": "KingmakerGunslinger.dll",
diff --git a/KNOWN-ISSUES.md b/KNOWN-ISSUES.md
index a219b1e5b1bd109029aa03f2d3c51d41ea85a2c6..f2d2e5f72dbe12ff6c3e1e4a2f38b2aea73d7183 100644
--- a/KNOWN-ISSUES.md
+++ b/KNOWN-ISSUES.md
@@ -1,15 +1,16 @@
 # Known issues
 
-- Version 0.0.22 is a disposable-save smoke-test build, not a release build.
+- Version 0.0.22.1 is a disposable-save Sprint 22 repair candidate, not a general release.
+- The exact rule-event hook and weapon-only reconciliation corrections require live Kingmaker confirmation.
+- Full save, desktop exit, restart, and reload durability must be reconfirmed on this exact repair build.
+- Loaded/empty/Broken/Wrecked attack enforcement and native Heavy Crossbow isolation remain blocking runtime tests.
 - The Test Musket still uses Heavy Crossbow name-adjacent presentation, icon, model, projectile, and animations.
 - Black Powder Charge and Lead Ball still use temporary Diamond Dust artwork.
-- Quicksave durability is fixed in source through parent context plus exact-token reconciliation, but the fix still requires the user's live Kingmaker confirmation.
 - The Test Musket has capacity one only.
-- Ordinary attacks now consume loaded state, but no firearm-specific combat-log explanation is shown for an empty forced miss.
-- A loaded Broken firearm may shoot, but natural-roll misfires, explosion behavior, and gameplay repair are not yet connected to attacks.
-- Iterative attacks after the first loaded shot are forced to miss; automatic free-action reloads are not implemented.
+- No firearm-specific combat-log explanation is shown for an empty forced miss.
+- Natural-roll misfires, deterministic forced-roll diagnostics, explosions, and gameplay repair are not implemented.
+- Iterative attacks after the first loaded shot are intended to be forced to miss; automatic free-action reloads are not implemented.
 - Reload free-hand enforcement and attacks of opportunity are not yet release-qualified.
 - State-token behavior across merchants, duplication, item deletion, respec, and third-party inventory reconstruction remains unqualified.
 - The parent mechanics context on an inert token may retain the owner that existed when the token was created. This has no intended mechanical effect but requires broader lifecycle and save-growth testing.
-- Native Heavy Crossbows are intended to remain unaffected; this remains a required runtime negative control.
 - Custom models, sounds, muzzle flash, vendors, crafting, the Gunslinger class, deeds, grit, and enemy AI are not implemented.
diff --git a/README.md b/README.md
index dcd83f315b9e298f8d69fbc33d9edd8d3e6e32f8..0919a145d597d6435c2ea91281b188f92b057071 100644
--- a/README.md
+++ b/README.md
@@ -1,6 +1,6 @@
 # Kingmaker Gunslinger
 
-Sprint 22 is an installable technical smoke-test build for Pathfinder: Kingmaker 2.1.7b.
+Version 0.0.22.1 is an installable Sprint 22 repair smoke-test build for Pathfinder: Kingmaker 2.1.7b.
 
 It currently provides:
 
@@ -9,14 +9,14 @@ It currently provides:
 - a Heavy-Crossbow-derived Test Musket;
 - Firearm Proficiency and disposable-save development controls;
 - close-range firearm touch-AC handling;
-- an item-owned enchantment-token firearm-state carrier proven across save and complete process restart;
+- an item-owned enchantment-token firearm-state carrier proven across save and complete process restart in version 0.0.19;
 - stackable Black Powder Charge and Lead Ball inventory items;
 - verified atomic inventory transactions;
 - a personal extraordinary **Reload Test Musket** ability that takes a full-round action and loads the exact equipped Test Musket; and
-- attack-time enforcement that consumes one loaded round or forces an empty or wrecked Test Musket to miss.
+- attack-time enforcement intended to consume one loaded round or force an empty or wrecked Test Musket to miss.
 
-Sprint 22 also guards Kingmaker's native `ItemEntity.ApplyEnchantments()` reconciliation so the loaded-state token is not lost during quicksave or equipment refresh. New state tokens on equipped firearms receive a parent `MechanicsContext`; an exact-token postfix restoration remains as a fail-safe for older null-context tokens.
+Version 0.0.22 proved that quicksave no longer removed the loaded token, but its rule-event target resolver expected the wrong `OnTrigger` signature, so attack enforcement never attached. It also inspected non-weapon items during native enchantment reconciliation and faulted on shields. Version 0.0.22.1 binds the exact installed `OnTrigger(RulebookEventContext)` methods and limits token inspection to `ItemEntityWeapon`.
 
-The Test Musket still appears as a Heavy Crossbow placeholder, and the ammunition uses Diamond Dust artwork. Misfires, custom models, crafting, vendors, iterative automatic reloads, and the Gunslinger class are not yet implemented.
+The Test Musket still appears as a Heavy Crossbow placeholder, and ammunition still uses Diamond Dust artwork. Natural-roll misfires, custom models, crafting, vendors, iterative automatic reloads, and the Gunslinger class are not implemented. Sprint 23 remains blocked until the complete 0.0.22.1 runtime gate passes.
 
 Install only the standalone UMM smoke-test ZIP. Use disposable saves and follow `SMOKE-TEST-GUIDE.md`.
diff --git a/SMOKE-TEST-GUIDE-0.0.22.1.md b/SMOKE-TEST-GUIDE-0.0.22.1.md
new file mode 100644
index 0000000000000000000000000000000000000000..0f9f82b2eb8208bdd5df9025e5fcea3c31a73837
--- /dev/null
+++ b/SMOKE-TEST-GUIDE-0.0.22.1.md
@@ -0,0 +1,187 @@
+# Kingmaker Gunslinger 0.0.22.1 Sprint 22 repair smoke test
+
+Use only a disposable campaign and disposable saves.
+
+## What this build repairs
+
+Version 0.0.22.1 does not add misfires. It repairs two failures observed in 0.0.22:
+
+1. the Kingmaker rule-event hooks were matched as zero-argument methods, but the installed methods take one `RulebookEventContext`, so loaded-round enforcement never ran; and
+2. the native item-reconciliation prefix inspected non-weapon items and faulted on `ItemEntityShield`.
+
+The decisive pass condition is no longer merely “the gun attacks.” The exact firing Test Musket must become empty, the attack diagnostics must advance, and reconciliation must remain fault-free.
+
+The Test Musket still displays and animates as a Heavy Crossbow. Black Powder Charge and Lead Ball still use placeholder Diamond Dust artwork.
+
+## Install
+
+1. Exit Kingmaker completely.
+2. Install `KingmakerGunslinger-0.0.22.1-attack-hook-repair-smoke-test.zip` through Unity Mod Manager over 0.0.22.
+3. Start Kingmaker and confirm Unity Mod Manager shows **Kingmaker Gunslinger 0.0.22.1** with a green status indicator.
+4. Load only a disposable campaign.
+5. Select exactly one character and click **Grant Firearm Proficiency to selected unit**. Repeating this development command is safe and also restores the reload ability if necessary.
+
+Do not continue if UMM reports a red status, a Harmony patch error, or a KMG bootstrap fault.
+
+## Prepare one clean Test Musket
+
+1. Equip exactly one Test Musket—the custom item still presented as a Heavy Crossbow.
+2. Click **Reset first equipped firearm to empty / normal**.
+3. Add 20 Black Powder Charges and 20 Lead Balls, or record the exact current counts.
+4. Click **Print Reload Test Musket readiness**.
+
+Expected readiness includes:
+
+```text
+hasReloadAbility=True
+available=True
+rounds=0
+condition=Normal
+```
+
+Record the initial `Firearm attack enforcement` and `State-token native reconciliation` lines.
+
+## Reload through the real ability
+
+1. Use **Reload Test Musket** in a context where a full-round action can finish.
+2. Let delivery complete.
+3. Print equipped-firearm state, reload readiness, and basic-ammunition counts.
+
+Expected state:
+
+```text
+rounds=1
+ammunition=kmg.debug.lead-ball
+condition=Normal
+```
+
+Expected inventory delta: exactly one Black Powder Charge and one Lead Ball consumed. The reload ability must now be unavailable.
+
+## Quicksave regression and shield-fault check
+
+With the same gun still loaded:
+
+1. Quicksave.
+2. Do not reload or load another save.
+3. Immediately print equipped-firearm state and reload readiness.
+4. Inspect `State-token native reconciliation`.
+
+Required result:
+
+- the exact gun still reports `rounds=1`, `condition=Normal`;
+- reload remains unavailable;
+- inventory counts do not change;
+- `conflicts=0` and `faults=0`; and
+- no last-fault text mentions `ItemEntityShield` or another non-weapon item.
+
+The reconciliation call count may be much lower than 0.0.22 because non-weapons are intentionally ignored.
+
+## Complete save/restart durability
+
+1. Save under a new disposable name while the gun remains loaded.
+2. Exit Kingmaker completely to the desktop.
+3. Restart Kingmaker and load that save.
+4. Print equipped-firearm state, reload readiness, and ammunition counts.
+
+Required result: the same exact gun is still Loaded/Normal, reload remains unavailable, counts are unchanged from immediately before the save, and reconciliation still reports no conflict or fault.
+
+## Fire one loaded shot — primary repair test
+
+1. Record the current powder and Lead Ball counts.
+2. Record all fields on `Firearm attack enforcement`.
+3. Make one ordinary ranged attack with the loaded Test Musket. A hit or miss is acceptable.
+4. Print equipped-firearm state, reload readiness, and ammunition counts.
+5. Re-read `Firearm attack enforcement`.
+
+Required item state:
+
+```text
+rounds=0
+ammunition=<none>
+condition=Normal
+```
+
+Required diagnostics and inventory behavior:
+
+- `observed` increases by at least one;
+- `fired` increases by exactly one for the shot;
+- `faults` remains zero;
+- reload becomes available again;
+- Black Powder Charge and Lead Ball counts are unchanged by firing; and
+- the native Kingmaker attack still completes through the ordinary weapon pipeline.
+
+**Immediate failure:** the item remains at `rounds=1`, `observed` does not increase, `fired` does not increase, or reload remains unavailable.
+
+## Attempt one empty shot
+
+Without reloading, order one more ordinary attack with the same Test Musket.
+
+Required result:
+
+- the attack is forced to miss even if another effect would auto-hit;
+- state remains Empty/Normal;
+- inventory ammunition remains unchanged;
+- `emptyRejected` increases by exactly one; and
+- `fired` does not increase.
+
+The placeholder Heavy Crossbow animation or projectile may still play; the rule result and counters are authoritative.
+
+## Loaded Broken and Wrecked checks
+
+These are blocking Sprint 23 controls, not misfire implementation.
+
+### Loaded Broken
+
+1. Reset the equipped Test Musket to Empty/Normal.
+2. Load one debug round or reload through the real ability.
+3. Click **Apply misfire damage to first equipped firearm** once.
+4. Verify `rounds=1`, `condition=Broken`.
+5. Make one ordinary attack.
+
+Required result: `fired` increases by one, the item becomes `rounds=0`, and `condition=Broken` is retained.
+
+### Wrecked
+
+1. Prepare a Broken Test Musket, then click **Apply misfire damage to first equipped firearm** again so it becomes Wrecked.
+2. Verify it is empty and Wrecked.
+3. Make one ordinary attack.
+
+Required result: the attack is forced to miss, `wreckedRejected` increases by one, and the state remains Wrecked.
+
+## Native Heavy Crossbow negative control
+
+1. Record `fired`, `emptyRejected`, and `wreckedRejected`.
+2. Equip an actual native Heavy Crossbow—not the custom Test Musket with borrowed presentation.
+3. Make one ordinary attack.
+
+Required result:
+
+- the native Heavy Crossbow attacks normally; and
+- `fired`, `emptyRejected`, and `wreckedRejected` do not increase.
+
+`observed` and `ignored` may increase because the exact rule event is seen and rejected from the firearm path. That is expected isolation behavior.
+
+## Final blocking conditions
+
+Do not approve Sprint 23 while any of these is true:
+
+- a loaded shot leaves the Test Musket loaded;
+- an empty or Wrecked Test Musket can produce a successful attack result;
+- powder or Lead Ball counts change when firing;
+- native Heavy Crossbows are treated as firearms;
+- `duplicateEvents` corresponds to more than one consumed round for one attack;
+- any bootstrap, Harmony, firearm-state, reconciliation, or attack-enforcement fault appears; or
+- the `ItemEntityShield` exception recurs.
+
+## Failure evidence
+
+Preserve screenshots showing:
+
+- **Last result**;
+- the exact equipped-firearm state;
+- reload readiness;
+- ammunition counts;
+- `Firearm attack enforcement`; and
+- `State-token native reconciliation`.
+
+For a crash, red UMM status, patch skip, conflict, or fault, also preserve the relevant `[KMG]` lines from Unity Mod Manager's **Logs** tab and `output_log.txt` when present.
diff --git a/SMOKE-TEST-GUIDE.md b/SMOKE-TEST-GUIDE.md
index cd933019d7408b0e689a82faa470f42032da106a..0f9f82b2eb8208bdd5df9025e5fcea3c31a73837 100644
--- a/SMOKE-TEST-GUIDE.md
+++ b/SMOKE-TEST-GUIDE.md
@@ -1,29 +1,33 @@
-# Kingmaker Gunslinger 0.0.22 smoke test
+# Kingmaker Gunslinger 0.0.22.1 Sprint 22 repair smoke test
 
 Use only a disposable campaign and disposable saves.
 
-## What this build is testing
+## What this build repairs
 
-Version 0.0.22 tests two related changes:
+Version 0.0.22.1 does not add misfires. It repairs two failures observed in 0.0.22:
 
-1. the loaded-state token must survive quicksave and Kingmaker's native item-enchantment refresh; and
-2. an ordinary Test Musket attack must consume one loaded round, while an empty Test Musket must be forced to miss.
+1. the Kingmaker rule-event hooks were matched as zero-argument methods, but the installed methods take one `RulebookEventContext`, so loaded-round enforcement never ran; and
+2. the native item-reconciliation prefix inspected non-weapon items and faulted on `ItemEntityShield`.
 
-The Test Musket still looks and animates like a Heavy Crossbow. Black Powder Charge and Lead Ball still use temporary Diamond Dust artwork.
+The decisive pass condition is no longer merely “the gun attacks.” The exact firing Test Musket must become empty, the attack diagnostics must advance, and reconciliation must remain fault-free.
+
+The Test Musket still displays and animates as a Heavy Crossbow. Black Powder Charge and Lead Ball still use placeholder Diamond Dust artwork.
 
 ## Install
 
 1. Exit Kingmaker completely.
-2. Install `KingmakerGunslinger-0.0.22-loaded-round-smoke-test.zip` through Unity Mod Manager over version 0.0.21.
-3. Start Kingmaker and confirm Unity Mod Manager shows **Kingmaker Gunslinger 0.0.22** with a green status indicator.
-4. Load the disposable campaign used for the reload test.
-5. Select exactly one character with Firearm Proficiency. Clicking **Grant Firearm Proficiency to selected unit** again is safe and will also restore the reload ability if necessary.
+2. Install `KingmakerGunslinger-0.0.22.1-attack-hook-repair-smoke-test.zip` through Unity Mod Manager over 0.0.22.
+3. Start Kingmaker and confirm Unity Mod Manager shows **Kingmaker Gunslinger 0.0.22.1** with a green status indicator.
+4. Load only a disposable campaign.
+5. Select exactly one character and click **Grant Firearm Proficiency to selected unit**. Repeating this development command is safe and also restores the reload ability if necessary.
+
+Do not continue if UMM reports a red status, a Harmony patch error, or a KMG bootstrap fault.
 
 ## Prepare one clean Test Musket
 
-1. Equip exactly one Test Musket—the custom item still displayed as a Heavy Crossbow.
+1. Equip exactly one Test Musket—the custom item still presented as a Heavy Crossbow.
 2. Click **Reset first equipped firearm to empty / normal**.
-3. Add 20 Black Powder Charges and 20 Lead Balls.
+3. Add 20 Black Powder Charges and 20 Lead Balls, or record the exact current counts.
 4. Click **Print Reload Test Musket readiness**.
 
 Expected readiness includes:
@@ -33,50 +37,44 @@ hasReloadAbility=True
 available=True
 rounds=0
 condition=Normal
-blackPowder=20
-leadBalls=20
 ```
 
+Record the initial `Firearm attack enforcement` and `State-token native reconciliation` lines.
+
 ## Reload through the real ability
 
-1. Use **Reload Test Musket** in a context where the action can execute.
-2. Allow the full-round delivery to finish.
-3. Print the equipped-firearm state and basic ammunition counts.
+1. Use **Reload Test Musket** in a context where a full-round action can finish.
+2. Let delivery complete.
+3. Print equipped-firearm state, reload readiness, and basic-ammunition counts.
 
-Expected result:
+Expected state:
 
 ```text
 rounds=1
 ammunition=kmg.debug.lead-ball
 condition=Normal
-blackPowder=19
-leadBalls=19
 ```
 
-The reload ability should now be unavailable.
+Expected inventory delta: exactly one Black Powder Charge and one Lead Ball consumed. The reload ability must now be unavailable.
 
-## Critical quicksave regression test
+## Quicksave regression and shield-fault check
 
-With that same gun still loaded:
+With the same gun still loaded:
 
 1. Quicksave.
-2. Do **not** reload or load a save.
-3. Immediately print the equipped-firearm state and reload readiness again.
-
-Expected:
-
-- the firearm still has `rounds=1`;
-- its condition remains `Normal`;
-- the reload ability remains unavailable;
-- Black Powder Charge and Lead Ball remain 19/19; and
-- `State-token native reconciliation` reports no conflict or fault.
+2. Do not reload or load another save.
+3. Immediately print equipped-firearm state and reload readiness.
+4. Inspect `State-token native reconciliation`.
 
-Either of these diagnostic outcomes is acceptable:
+Required result:
 
-- the token was preserved natively; or
-- one exact token was restored after native removal.
+- the exact gun still reports `rounds=1`, `condition=Normal`;
+- reload remains unavailable;
+- inventory counts do not change;
+- `conflicts=0` and `faults=0`; and
+- no last-fault text mentions `ItemEntityShield` or another non-weapon item.
 
-The important result is that the firearm remains loaded.
+The reconciliation call count may be much lower than 0.0.22 because non-weapons are intentionally ignored.
 
 ## Complete save/restart durability
 
@@ -85,15 +83,17 @@ The important result is that the firearm remains loaded.
 3. Restart Kingmaker and load that save.
 4. Print equipped-firearm state, reload readiness, and ammunition counts.
 
-Expected: the same gun is still Loaded / Normal, reload remains unavailable, and counts remain 19/19.
+Required result: the same exact gun is still Loaded/Normal, reload remains unavailable, counts are unchanged from immediately before the save, and reconciliation still reports no conflict or fault.
 
-## Fire one loaded shot
+## Fire one loaded shot — primary repair test
 
-1. Record the current Black Powder Charge and Lead Ball counts.
-2. Make one ordinary ranged attack with the loaded Test Musket.
-3. Print the firearm state, reload readiness, and ammunition counts.
+1. Record the current powder and Lead Ball counts.
+2. Record all fields on `Firearm attack enforcement`.
+3. Make one ordinary ranged attack with the loaded Test Musket. A hit or miss is acceptable.
+4. Print equipped-firearm state, reload readiness, and ammunition counts.
+5. Re-read `Firearm attack enforcement`.
 
-Expected:
+Required item state:
 
 ```text
 rounds=0
@@ -101,53 +101,87 @@ ammunition=<none>
 condition=Normal
 ```
 
-Also expected:
+Required diagnostics and inventory behavior:
 
-- the ordinary Kingmaker attack proceeds through its normal attack and damage pipeline;
-- `Firearm attack enforcement` reports one additional fired round;
-- the reload ability becomes available again; and
-- inventory powder and Lead Balls remain unchanged at 19/19.
+- `observed` increases by at least one;
+- `fired` increases by exactly one for the shot;
+- `faults` remains zero;
+- reload becomes available again;
+- Black Powder Charge and Lead Ball counts are unchanged by firing; and
+- the native Kingmaker attack still completes through the ordinary weapon pipeline.
 
-The loaded round was already represented by the item-owned state. Firing must not consume another powder charge or Lead Ball.
+**Immediate failure:** the item remains at `rounds=1`, `observed` does not increase, `fired` does not increase, or reload remains unavailable.
 
 ## Attempt one empty shot
 
-Without reloading, order another ordinary attack with the same Test Musket.
+Without reloading, order one more ordinary attack with the same Test Musket.
+
+Required result:
+
+- the attack is forced to miss even if another effect would auto-hit;
+- state remains Empty/Normal;
+- inventory ammunition remains unchanged;
+- `emptyRejected` increases by exactly one; and
+- `fired` does not increase.
+
+The placeholder Heavy Crossbow animation or projectile may still play; the rule result and counters are authoritative.
 
-Expected:
+## Loaded Broken and Wrecked checks
 
-- the attack may still animate and launch the placeholder crossbow presentation;
-- the attack roll is forced to miss;
-- firearm state remains empty/Normal;
-- inventory ammunition remains unchanged; and
-- `emptyRejected` increases by one in `Firearm attack enforcement`.
+These are blocking Sprint 23 controls, not misfire implementation.
+
+### Loaded Broken
+
+1. Reset the equipped Test Musket to Empty/Normal.
+2. Load one debug round or reload through the real ability.
+3. Click **Apply misfire damage to first equipped firearm** once.
+4. Verify `rounds=1`, `condition=Broken`.
+5. Make one ordinary attack.
+
+Required result: `fired` increases by one, the item becomes `rounds=0`, and `condition=Broken` is retained.
+
+### Wrecked
+
+1. Prepare a Broken Test Musket, then click **Apply misfire damage to first equipped firearm** again so it becomes Wrecked.
+2. Verify it is empty and Wrecked.
+3. Make one ordinary attack.
+
+Required result: the attack is forced to miss, `wreckedRejected` increases by one, and the state remains Wrecked.
 
 ## Native Heavy Crossbow negative control
 
-1. Record the `fired`, `emptyRejected`, and `wreckedRejected` counters.
-2. Equip an actual native Heavy Crossbow, not a Test Musket.
+1. Record `fired`, `emptyRejected`, and `wreckedRejected`.
+2. Equip an actual native Heavy Crossbow—not the custom Test Musket with borrowed presentation.
 3. Make one ordinary attack.
 
-Expected:
+Required result:
+
+- the native Heavy Crossbow attacks normally; and
+- `fired`, `emptyRejected`, and `wreckedRejected` do not increase.
 
-- the native Heavy Crossbow attacks normally;
-- no loaded-state restriction is applied; and
-- the firearm `fired`, `emptyRejected`, and `wreckedRejected` counters do not increase.
+`observed` and `ignored` may increase because the exact rule event is seen and rejected from the firearm path. That is expected isolation behavior.
 
-The general `ignored` counter may increase because a non-firearm attack-roll object was observed and deliberately rejected from the firearm path.
+## Final blocking conditions
 
-## Optional Broken-firearm check
+Do not approve Sprint 23 while any of these is true:
 
-A loaded Broken Test Musket is currently allowed to discharge and should remain Broken after its one round is consumed. Misfire and explosion behavior are not part of this build.
+- a loaded shot leaves the Test Musket loaded;
+- an empty or Wrecked Test Musket can produce a successful attack result;
+- powder or Lead Ball counts change when firing;
+- native Heavy Crossbows are treated as firearms;
+- `duplicateEvents` corresponds to more than one consumed round for one attack;
+- any bootstrap, Harmony, firearm-state, reconciliation, or attack-enforcement fault appears; or
+- the `ItemEntityShield` exception recurs.
 
 ## Failure evidence
 
-For an ordinary mismatch, preserve screenshots of:
+Preserve screenshots showing:
 
 - **Last result**;
-- `Firearm attack enforcement`;
-- `State-token native reconciliation`;
-- the equipped-firearm state; and
-- ammunition counts.
+- the exact equipped-firearm state;
+- reload readiness;
+- ammunition counts;
+- `Firearm attack enforcement`; and
+- `State-token native reconciliation`.
 
-For a crash, red UMM status, initialization fault, token conflict, token-reconciliation fault, or attack-enforcement fault, include the relevant `[KMG]` lines from Unity Mod Manager's **Logs** tab and `output_log.txt` when present.
+For a crash, red UMM status, patch skip, conflict, or fault, also preserve the relevant `[KMG]` lines from Unity Mod Manager's **Logs** tab and `output_log.txt` when present.
diff --git a/SPRINT-22-REPAIR-REPORT-0.0.22.1.md b/SPRINT-22-REPAIR-REPORT-0.0.22.1.md
new file mode 100644
index 0000000000000000000000000000000000000000..8aaf23604679e074c6ebf6e39e14418e78ec7c16
--- /dev/null
+++ b/SPRINT-22-REPAIR-REPORT-0.0.22.1.md
@@ -0,0 +1,122 @@
+# Sprint 22 repair report — 0.0.22.1 attack-hook binding and weapon-only reconciliation
+
+## Outcome
+
+Version 0.0.22 did **not** satisfy the Sprint 23 entry criteria. Quicksave no longer removed the loaded-state token, but an ordinary Test Musket attack left the exact item loaded, the reload ability stayed unavailable, and the attack-enforcement diagnostics remained at zero observations. The same evidence showed repeated state-token reconciliation faults on `ItemEntityShield`.
+
+The project therefore remains on Sprint 22. Version 0.0.22.1 is a bounded repair smoke-test candidate; no misfire implementation has been added.
+
+## Runtime result entering the repair
+
+The supplied 0.0.22 evidence established:
+
+- Reload still completed and wrote one loaded round to the exact Test Musket.
+- Save/quicksave no longer removed the loaded token.
+- Kingmaker completed an ordinary attack through its native weapon pipeline.
+- The firearm stayed at `rounds=1` after that attack.
+- `Firearm attack enforcement` remained `observed=0; fired=0; emptyRejected=0; wreckedRejected=0`.
+- `State-token native reconciliation` reached `faults=10`, with the last fault reporting that `ItemEntityShield` exposed no readable enumerable enchantment collection.
+
+The complete criterion-by-criterion assessment and original screenshots are under `evidence/sprint22-repair/`.
+
+## Root cause 1 — wrong rule-event method contract
+
+The exact private Kingmaker 2.1.7b assembly declares these methods:
+
+```text
+System.Void Kingmaker.RuleSystem.Rules.RuleAttackRoll.OnTrigger(
+    Kingmaker.RuleSystem.RulebookEventContext context)
+
+System.Void Kingmaker.RuleSystem.Rules.RuleAttackWithWeapon.OnTrigger(
+    Kingmaker.RuleSystem.RulebookEventContext context)
+
+System.Void Kingmaker.RuleSystem.Rules.RuleCalculateAC.OnTrigger(
+    Kingmaker.RuleSystem.RulebookEventContext context)
+```
+
+The 0.0.22 resolver required a zero-argument `OnTrigger`. It therefore found zero candidates and instructed Harmony to skip all three optional patches. This directly explains why the loaded-round callback was never observed and also means the intended touch-AC and combat-trace hooks were not attached at those targets.
+
+The repair accepts exactly one method only when all of these conditions hold:
+
+- declared on the intended type;
+- instance method;
+- non-generic;
+- name exactly `OnTrigger`;
+- return type exactly `System.Void`; and
+- exactly one parameter whose type is exactly `Kingmaker.RuleSystem.RulebookEventContext`.
+
+It does not fall back to a plausible overload.
+
+## Root cause 2 — base patch inspected non-weapons
+
+`Kingmaker.Items.ItemEntity.ApplyEnchantments()` is the intended exact zero-argument native reconciliation target. Because it is a base method, however, Harmony invokes the prefix for non-weapon item subclasses as well. The 0.0.22 prefix unconditionally attempted to enumerate firearm-state tokens, including on `ItemEntityShield`, which has no compatible weapon-enchantment collection for that adapter.
+
+The repair casts the runtime instance to `ItemEntityWeapon` before token inspection. Non-weapons receive an empty Harmony invocation state, native `ApplyEnchantments()` runs unchanged, and the postfix exits without firearm reflection. Weapon reconciliation behavior is otherwise unchanged.
+
+## Files changed for runtime behavior
+
+- `src/KingmakerGunslinger/Diagnostics/RuleEventPatchContract.cs`
+- `src/KingmakerGunslinger/Diagnostics/RuleEventPatchTarget.cs`
+- `src/KingmakerGunslinger/Firearms/FirearmStateTokenReconciliationPatch.cs`
+
+The corresponding project files, dependency-free regression harness, version metadata, and runtime-contract inspection script were updated to keep the repaired contract executable and prevent the zero-argument assumption from being reintroduced.
+
+## Scope deliberately preserved
+
+The repair does not change:
+
+- the exact `FirearmDefinitionComponent` marker requirement;
+- item-owned inert `BlueprintWeaponEnchantment` state tokens;
+- the accepted absence-means-Empty/Normal encoding;
+- atomic Black Powder Charge plus Lead Ball consumption during reload;
+- the full-round reload ability;
+- attack-time inventory isolation;
+- loaded/empty/Broken/Wrecked discharge decisions;
+- duplicate event-instance protection; or
+- native Heavy Crossbow exclusion.
+
+The rejected `ItemEntityWeapon.UniqueId` vault approach remains rejected.
+
+## Verification
+
+Final qualification for the packaged candidate:
+
+```text
+Source invariant validation:             PASS
+Exact private-reference Release compile: PASS
+Warnings as errors:                      enabled
+Deterministic compile comparison:         PASS — two same-output-path DLLs byte-identical
+Declared/executed tests:                  455 x 3
+Test failures:                            0
+Repeated test output:                     byte-identical
+Standalone UMM package validation:        PASS
+Private compiler references redistributed: no
+```
+
+Authoritative hashes:
+
+```text
+KingmakerGunslinger.dll
+  36f46686856d2f0b04bf91ddd9665b47d9c6676de96af428bd0d697ec32fa463
+
+KingmakerGunslinger-0.0.22.1-attack-hook-repair-smoke-test.zip
+  83423ad78a5e62dd4e329c07c1f5ddb20b9bd49567690e5bab6f9495e7263541
+
+455-test repeated stdout
+  5e004b6f22411a12e4d4706b4b7c21822e43ffbfb3a47b1d57310429339d9148
+```
+
+The standalone UMM ZIP exists and passed the eight-entry, one-binary allowlist, so the artifact is **READY FOR KINGMAKER — SPRINT 22 REPAIR SMOKE TEST**. This label means compile/package readiness only; it does not claim live runtime acceptance. Full machine-readable qualification is in `BUILD-QUALIFICATION.json`, `VALIDATION-RESULTS-S22-REPAIR.txt`, and `evidence/sprint22-repair/`.
+
+## Runtime acceptance still required
+
+Version 0.0.22.1 is not proof that the repair works in Kingmaker. The next live run must establish at minimum:
+
+1. loaded state survives quicksave with reconciliation `conflicts=0; faults=0`;
+2. one loaded Test Musket attack increments attack observation and fired counters and changes the exact item to `rounds=0`;
+3. reload becomes available again without consuming additional inventory ammunition;
+4. a second empty attack increments `emptyRejected` and is forced to miss;
+5. the `ItemEntityShield` reconciliation fault does not recur; and
+6. an actual native Heavy Crossbow remains outside firearm enforcement.
+
+The full bounded procedure is `SMOKE-TEST-GUIDE-0.0.22.1.md`. Sprint 23 remains blocked until all criteria in `planning/SPRINT-23-ENTRY-CRITERIA.md` are satisfied without KMG or Harmony faults.
diff --git a/TESTING.md b/TESTING.md
index 89401e8fc0646904be41a0eb9741c8a2246c92e7..27417c59d77ab0590a6bd103a79225966369c562 100644
--- a/TESTING.md
+++ b/TESTING.md
@@ -1,15 +1,7 @@
 # Testing
 
-For the current installable build, follow `SMOKE-TEST-GUIDE-0.0.22.md`.
+For the current installable repair build, follow `SMOKE-TEST-GUIDE-0.0.22.1.md`.
 
-The minimum runtime gate is:
+The dependency-free suite is compiled against the .NET Framework 4.7 reference surface with C# 7.3 and warnings as errors. The full suite must run three times with zero failures and byte-identical stdout before packaging.
 
-1. reload one empty Normal Test Musket;
-2. quicksave while it remains equipped and verify it stays loaded;
-3. save, exit to desktop, restart, and verify loaded state persists;
-4. fire once and verify exactly one loaded round is consumed;
-5. fire again while empty and verify the attack is forced to miss;
-6. verify firing does not alter Black Powder Charge or Lead Ball inventory counts; and
-7. verify a native Heavy Crossbow remains unaffected.
-
-Use only disposable saves. For a failure, preserve the relevant `[KMG]` entries from Unity Mod Manager's Logs tab and `output_log.txt` when present.
+The mod DLL must compile against the exact private Kingmaker 2.1.7b, Unity, Unity Mod Manager 0.32.4, Harmony 1.2.0.1, and Newtonsoft.Json references. Those private compiler inputs must never appear in the standalone UMM ZIP, milestone ZIP, or source ZIP.
diff --git a/VALIDATION-RESULTS-S22-REPAIR.txt b/VALIDATION-RESULTS-S22-REPAIR.txt
new file mode 100644
index 0000000000000000000000000000000000000000..74f14cc656addf4ad1e917f78693f80b555273ad
--- /dev/null
+++ b/VALIDATION-RESULTS-S22-REPAIR.txt
@@ -0,0 +1,74 @@
+Kingmaker Gunslinger — Sprint 22 repair validation results
+Version: 0.0.22.1-s22-attack-hook-repair
+
+0.0.22 runtime decision: FAIL — SPRINT 23 BLOCKED
+- Quicksave no longer removed the loaded-state token: pass for the supplied in-process observation.
+- Loaded attack consumed exactly one round: fail; the exact Test Musket remained at rounds=1.
+- Reload became available after firing: fail.
+- Firearm attack enforcement observed/fired counters advanced: fail; both remained zero.
+- Native token reconciliation remained fault-free: fail; faults=10, last on ItemEntityShield.
+- Complete save/desktop-restart/load durability: not established for 0.0.22.
+- Native Heavy Crossbow negative control: not established.
+
+Evidence-driven repair scope: PASS
+- Exact private Assembly-CSharp.dll inspection found one declared void OnTrigger(Kingmaker.RuleSystem.RulebookEventContext) on RuleAttackRoll, RuleAttackWithWeapon, and RuleCalculateAC.
+- The rejected 0.0.22 zero-argument matcher would accept none of those methods.
+- The repair requires exactly one non-static, non-generic, void OnTrigger with exactly one parameter of the exact RulebookEventContext type.
+- ItemEntity.ApplyEnchantments() remains the exact native reconciliation target.
+- Only ItemEntityWeapon enters firearm-state token inspection; non-weapons receive empty invocation state.
+- The item-owned inert BlueprintWeaponEnchantment token carrier remains authoritative.
+- The runtime-rejected ItemEntityWeapon.UniqueId vault was not revived.
+- No natural-roll, force-next-roll, misfire, explosion, automatic reload, or Gunslinger-class behavior was added.
+
+Portable source validation: PASS
+- Stable blueprint IDs: 12
+- Active blueprints: 11
+- Reserved blueprints: 1
+- Main-project compile items: 139 unique
+- Test-project compile items: 83 unique
+- Declared dependency-free cases: 455 unique
+- Original Sprint 22 discharge/reconciliation cases: 27
+- Exact patch-target repair cases: 9
+- SMOKE-TEST-GUIDE.md is byte-identical to SMOKE-TEST-GUIDE-0.0.22.1.md
+- Four supplied runtime screenshots are preserved with verified SHA-256 hashes.
+- Source tree contains no loose private runtime/compiler references.
+- Source invariant validator: PASS
+
+Exact private-reference Release compile: PASS
+- Target: supplied Pathfinder: Kingmaker 2.1.7b / Unity Mod Manager 0.32.4 / Harmony 1.2.0.1 reference set
+- Private reference manifest: all 10 compiler inputs matched expected sizes and SHA-256 hashes
+- Target framework reference surface: .NET Framework 4.7
+- C# language version: 7.3
+- Configuration: Release / AnyCPU
+- Warnings as errors: enabled
+- Deterministic compiler mode: enabled
+- Same-output-path DLL comparison: byte-identical
+- Same-output-path PDB comparison: byte-identical
+- Compiler exit code: 0
+- Compiler stderr: empty
+- Main DLL SHA-256: 36f46686856d2f0b04bf91ddd9665b47d9c6676de96af428bd0d697ec32fa463
+- PDB SHA-256: b76d3cb21da6d21154d756a95266cadb6e9c06b2790576be0b53b10a62ff2ee7
+- Blueprint manifest SHA-256: af56f2a35bd05e055e8feee1e996d37abcb20b76efb90762784f31ccfe933337
+
+Dependency-free regression tests: PASS
+- Declared/executed: 455
+- Runs: 3
+- Failures: 0
+- Repeated stdout byte-identical: yes
+- Test executable SHA-256: c613af5456939c225952014acb5b3927ead3f199628f731eb8d360baf48ec042
+- Repeated output SHA-256: 5e004b6f22411a12e4d4706b4b7c21822e43ffbfb3a47b1d57310429339d9148
+
+Standalone UMM install-package validation: PASS
+- Archive: KingmakerGunslinger-0.0.22.1-attack-hook-repair-smoke-test.zip
+- Single KingmakerGunslinger root
+- Info.json version: 0.0.22.1
+- Archive entries: 8
+- Exactly one binary: project-owned KingmakerGunslinger.dll
+- DLL hash matches the exact-reference build
+- No Kingmaker, Unity, Unity Mod Manager, Harmony, Newtonsoft, or private-reference DLL redistributed
+- ZIP CRC, duplicate-entry, traversal, symlink, root-layout, blueprint-ledger, and binary-allowlist checks: passed
+- Install ZIP SHA-256: 83423ad78a5e62dd4e329c07c1f5ddb20b9bd49567690e5bab6f9495e7263541
+
+Artifact classification: READY FOR KINGMAKER — SPRINT 22 REPAIR SMOKE TEST
+Runtime acceptance: PENDING
+Sprint 23 entry: BLOCKED until every criterion in planning/SPRINT-23-ENTRY-CRITERIA.md passes in Kingmaker with zero KMG/Harmony faults.
diff --git a/VALIDATION-RESULTS.txt b/VALIDATION-RESULTS.txt
index 9303790cec95280aed206bbc4579dc3ecaafadad..74f14cc656addf4ad1e917f78693f80b555273ad 100644
--- a/VALIDATION-RESULTS.txt
+++ b/VALIDATION-RESULTS.txt
@@ -1,63 +1,74 @@
-Kingmaker Gunslinger — Sprint 22 validation results
-Version: 0.0.22-s22-loaded-round-enforcement
+Kingmaker Gunslinger — Sprint 22 repair validation results
+Version: 0.0.22.1-s22-attack-hook-repair
 
-Sprint 21 runtime evidence entering this milestone: PASS WITH ONE IDENTIFIED DEFECT
-- Full-round Reload Test Musket ability executed in Kingmaker
-- Kingmaker animation observed
-- Exact equipped Test Musket became loaded
-- Exactly one Black Powder Charge and one Lead Ball consumed
-- Reload ability unavailable while loaded
-- Defect reproduced: quicksave removed the loaded-state token
+0.0.22 runtime decision: FAIL — SPRINT 23 BLOCKED
+- Quicksave no longer removed the loaded-state token: pass for the supplied in-process observation.
+- Loaded attack consumed exactly one round: fail; the exact Test Musket remained at rounds=1.
+- Reload became available after firing: fail.
+- Firearm attack enforcement observed/fired counters advanced: fail; both remained zero.
+- Native token reconciliation remained fault-free: fail; faults=10, last on ItemEntityShield.
+- Complete save/desktop-restart/load durability: not established for 0.0.22.
+- Native Heavy Crossbow negative control: not established.
 
-Portable Sprint 22 source validation: PASS
+Evidence-driven repair scope: PASS
+- Exact private Assembly-CSharp.dll inspection found one declared void OnTrigger(Kingmaker.RuleSystem.RulebookEventContext) on RuleAttackRoll, RuleAttackWithWeapon, and RuleCalculateAC.
+- The rejected 0.0.22 zero-argument matcher would accept none of those methods.
+- The repair requires exactly one non-static, non-generic, void OnTrigger with exactly one parameter of the exact RulebookEventContext type.
+- ItemEntity.ApplyEnchantments() remains the exact native reconciliation target.
+- Only ItemEntityWeapon enters firearm-state token inspection; non-weapons receive empty invocation state.
+- The item-owned inert BlueprintWeaponEnchantment token carrier remains authoritative.
+- The runtime-rejected ItemEntityWeapon.UniqueId vault was not revived.
+- No natural-roll, force-next-roll, misfire, explosion, automatic reload, or Gunslinger-class behavior was added.
+
+Portable source validation: PASS
 - Stable blueprint IDs: 12
 - Active blueprints: 11
 - Reserved blueprints: 1
-- Main-project compile items: 138 unique
-- Test-project compile items: 82 unique
-- Declared domain/integration-style cases: 446 unique
-- New Sprint 22 discharge/reconciliation cases: 27
-- Loaded-round enforcement precedes touch-AC attack-roll observation
-- Attack-time discharge contains no inventory-ammunition transaction
-- Native reconciliation patch targets the exact zero-argument ItemEntity.ApplyEnchantments method
-- Source tree contains no game, Unity, Unity Mod Manager, Harmony, or Newtonsoft binaries
-- Sprint 22 source invariant validator: PASS
+- Main-project compile items: 139 unique
+- Test-project compile items: 83 unique
+- Declared dependency-free cases: 455 unique
+- Original Sprint 22 discharge/reconciliation cases: 27
+- Exact patch-target repair cases: 9
+- SMOKE-TEST-GUIDE.md is byte-identical to SMOKE-TEST-GUIDE-0.0.22.1.md
+- Four supplied runtime screenshots are preserved with verified SHA-256 hashes.
+- Source tree contains no loose private runtime/compiler references.
+- Source invariant validator: PASS
 
 Exact private-reference Release compile: PASS
-- Target: user-supplied Pathfinder: Kingmaker 2.1.7b / UMM 0.32.4 reference set
+- Target: supplied Pathfinder: Kingmaker 2.1.7b / Unity Mod Manager 0.32.4 / Harmony 1.2.0.1 reference set
+- Private reference manifest: all 10 compiler inputs matched expected sizes and SHA-256 hashes
 - Target framework reference surface: .NET Framework 4.7
 - C# language version: 7.3
 - Configuration: Release / AnyCPU
-- Deterministic compiler mode: enabled (the final package hash below is authoritative)
-- Warnings as errors: yes
+- Warnings as errors: enabled
+- Deterministic compiler mode: enabled
+- Same-output-path DLL comparison: byte-identical
+- Same-output-path PDB comparison: byte-identical
 - Compiler exit code: 0
 - Compiler stderr: empty
-- Main DLL SHA-256: 25f8e1a25cf7871591f5dc3778b0732ebb5f03edec7b79c62775d05bf07b1660
+- Main DLL SHA-256: 36f46686856d2f0b04bf91ddd9665b47d9c6676de96af428bd0d697ec32fa463
+- PDB SHA-256: b76d3cb21da6d21154d756a95266cadb6e9c06b2790576be0b53b10a62ff2ee7
 - Blueprint manifest SHA-256: af56f2a35bd05e055e8feee1e996d37abcb20b76efb90762784f31ccfe933337
 
 Dependency-free regression tests: PASS
-- Declared/executed: 446
+- Declared/executed: 455
 - Runs: 3
 - Failures: 0
-- Repeated output byte-identical: yes
-- Test executable SHA-256: 736f2b37cbeb1629442228c7c3564d7f2c964eac36ee6d806d00b316f00b31eb
-- Repeated output SHA-256: 209f26caf959cc7a88015de5a98452a9d594a40962b982800b9a9e89cfdeb401
+- Repeated stdout byte-identical: yes
+- Test executable SHA-256: c613af5456939c225952014acb5b3927ead3f199628f731eb8d360baf48ec042
+- Repeated output SHA-256: 5e004b6f22411a12e4d4706b4b7c21822e43ffbfb3a47b1d57310429339d9148
 
-UMM install package validation: PASS
-- Archive: KingmakerGunslinger-0.0.22-loaded-round-smoke-test.zip
+Standalone UMM install-package validation: PASS
+- Archive: KingmakerGunslinger-0.0.22.1-attack-hook-repair-smoke-test.zip
 - Single KingmakerGunslinger root
-- Info.json version: 0.0.22
+- Info.json version: 0.0.22.1
 - Archive entries: 8
 - Exactly one binary: project-owned KingmakerGunslinger.dll
+- DLL hash matches the exact-reference build
 - No Kingmaker, Unity, Unity Mod Manager, Harmony, Newtonsoft, or private-reference DLL redistributed
-- ZIP CRC, duplicate-entry, traversal, symlink, root-layout, manifest, and DLL-hash checks: passed
-- Install ZIP SHA-256: 5a6fd105e78c0a8850b6988c4d1ee0edb7b4f6460d61a30a14db17d43d0447cd
+- ZIP CRC, duplicate-entry, traversal, symlink, root-layout, blueprint-ledger, and binary-allowlist checks: passed
+- Install ZIP SHA-256: 83423ad78a5e62dd4e329c07c1f5ddb20b9bd49567690e5bab6f9495e7263541
 
-Runtime status: READY FOR KINGMAKER LOADED-ROUND / TOKEN-DURABILITY SMOKE TEST
-Runtime claims pending:
-- quicksave does not unload a loaded Test Musket
-- complete save/restart preserves loaded state with the Sprint 22 token context/repair path
-- one loaded ordinary attack consumes exactly one round
-- a second empty ordinary attack is forced to miss
-- firing leaves Black Powder Charge and Lead Ball inventory counts unchanged
-- native Heavy Crossbows remain unaffected
+Artifact classification: READY FOR KINGMAKER — SPRINT 22 REPAIR SMOKE TEST
+Runtime acceptance: PENDING
+Sprint 23 entry: BLOCKED until every criterion in planning/SPRINT-23-ENTRY-CRITERIA.md passes in Kingmaker with zero KMG/Harmony faults.
diff --git a/docs/ARCHITECTURE.md b/docs/ARCHITECTURE.md
index 10aa4a53143d5b1bbf3caf305f2c39d07d55bbf8..035122a91a2549e6eb9ea5626f58a2cea85a6a7b 100644
--- a/docs/ARCHITECTURE.md
+++ b/docs/ARCHITECTURE.md
@@ -1,6 +1,17 @@
 # Current architecture
 
-## Sprint 17 executed-evidence and handoff layer
+## Sprint 22 repair current layer
+
+Version 0.0.22.1 keeps the proven Sprint 19–22 runtime architecture and repairs two exact Kingmaker 2.1.7b contracts:
+
+- rule-event Harmony targets bind exactly one declared `void OnTrigger(Kingmaker.RuleSystem.RulebookEventContext)` method; and
+- only `ItemEntityWeapon` instances enter firearm state-token inspection around `ItemEntity.ApplyEnchantments()`.
+
+The runtime firearm-state source of truth is the exact item's inert `BlueprintWeaponEnchantment` token. Absence means Empty/Normal. The four active state tokens encode Loaded/Normal, Broken/Empty, Broken/Loaded, and Wrecked. The runtime-rejected `ItemEntityWeapon.UniqueId` plus UnitPart vault remains historical code and documentation only; it must not be reactivated or used for new writes.
+
+The active vertical slice includes Firearm Proficiency, eleven active blueprints, stackable Black Powder Charge and Lead Ball items, atomic one-plus-one consumption, a full-round reload ability, quicksave token reconciliation, range-limited touch AC, and loaded-round attack enforcement. Natural-d20 misfire detection remains blocked until the 0.0.22.1 runtime gate passes.
+
+## Historical Sprint 17 executed-evidence and handoff layer
 
 Sprint 17 leaves the firearm persistence carrier, blueprint ledger, bootstrap, touch-AC rule, and runtime preflight unchanged. It adds three non-gameplay capabilities:
 
@@ -20,9 +31,11 @@ Sprint 16 leaves Sprint 14's engine-issued `UniqueId` plus save-owned `UnitPart`
 
 Kingmaker Gunslinger is a standalone Unity Mod Manager mod for **Pathfinder: Kingmaker Enhanced Plus Edition 2.1.7b**. It uses Kingmaker's blueprint lifecycle and legacy Harmony 1.2 integration rather than importing Wrath's BlueprintCore or modification-template stack.
 
-The first vertical slice is a Test Musket. The Gunslinger class does not become a dependency until firearm identity, combat events, per-item state, and full-attack behavior have passed their gates.
+The current Test Musket remains a real weapon using Kingmaker's native attack and damage pipeline. Firearm identity comes only from exactly one `FirearmDefinitionComponent` on the concrete weapon type. The Heavy Crossbow category, presentation, name, icon, and model are adapters and never sufficient firearm identity.
+
+The authoritative runtime state carrier is the exact item's inert enchantment token. The earlier weak repository, direct-reference UnitPart, and `UniqueId` vault implementations remain checked in for test history and migration research, but none is the current runtime source of truth and the rejected `UniqueId` design must not be revived.
 
-Sprint 15 retains Sprint 14's engine-item-identity UnitPart candidate and adds a build-fingerprinted external evidence recorder for the complete persistence matrix. Sprint 13 direct-reference records and all four Sprint 12 tokens remain only as one-way migration inputs. Evidence files are never firearm save data. Save/load durability remains a formal runtime gate; ammunition and reload feature work is blocked until it passes.
+Sprint 23 is gated on live proof that the repaired rule hook consumes one round, empty and Wrecked attacks are forced to miss, token reconciliation is fault-free, and native Heavy Crossbows remain unaffected.
 
 ## 2. Runtime boundaries
 
@@ -56,19 +69,19 @@ The current sequence is:
 2. A process-lifetime loader guard accepts only the first attempt.
 3. `ModContext` publishes the UMM entry, logger, assembly, and one Harmony instance.
 4. Harmony patches the executing assembly exactly once.
-5. Optional combat-trace patch classes resolve their installed rule-event targets. Missing or ambiguous diagnostic targets skip only that observer.
+5. Optional combat, discharge, and AC patch classes resolve the exact installed `OnTrigger(RulebookEventContext)` targets. Missing or ambiguous targets skip the affected patch and are logged.
 6. The `LoadDictionary` postfix passes the first library instance to `BlueprintBootstrap.Observe`.
 7. Initialization waits for both the library and a patch-ready context.
 8. The deployed stable-ID manifest is loaded and validated.
 9. An in-memory `FirearmDefinitionComponent` round-trip proves the marker can be constructed.
-10. One `BlueprintRegistry` transaction registers the diagnostic feature, Firearm Proficiency, Test Musket type/item, and four component-only firearm-state token enchantments.
+10. One `BlueprintRegistry` transaction registers the diagnostic feature, Firearm Proficiency, Test Musket type/item, four component-only firearm-state token enchantments, Black Powder Charge, Lead Ball, and Reload Test Musket.
 11. The Test Musket type is cloned from the native Heavy Crossbow type and receives exactly one firearm marker.
 12. The Test Musket item is cloned from the native Standard Heavy Crossbow, rewired to the custom type, and receives exactly one Firearm Proficiency restriction.
-13. Each legacy state-token blueprint contains exactly one passive marker component and no gameplay components.
+13. Each firearm-state token blueprint contains exactly one passive marker component and no gameplay components.
 14. Clone, marker, restriction, type-link, token, GUID, and native-source invariants are verified.
-15. The identity-keyed UnitPart vault, Sprint 13 reference-migration layer, and Sprint 12 token-migration wrapper are configured only after all eight registrations validate.
+15. The token-backed runtime repository is configured only after all eleven active registrations validate. Historical vault and direct-reference experiments are not configured for new runtime state writes.
 16. Any failure triggers best-effort reverse rollback of every owned registration.
-17. After successful bootstrap, the mod attaches manual development controls, the external persistence-evidence recorder, and an off-by-default trace toggle to the UMM panel.
+17. After successful bootstrap, the mod attaches manual development controls and an off-by-default trace toggle to the UMM panel.
 
 ### State machines
 
@@ -100,7 +113,7 @@ Rules:
 - dictionary insertion uses `Add`, never a replacing indexer;
 - save migrations will refer to symbolic names and schema versions.
 
-The manifest contains 12 stable identifiers: eight active entries and four reservations. Sprint 14 added no GUID; Sprint 15 also adds no GUID and changes none. The four Sprint 12 token GUIDs remain active solely so older saves can resolve and migrate them.
+The manifest contains 12 stable identifiers: eleven active and one reserved. The active ledger includes the diagnostic feature, Firearm Proficiency, Test Musket type/item, four firearm-state tokens, Reload Test Musket, Black Powder Charge, and Lead Ball. The touch-AC enchantment remains reserved because touch AC is implemented through a rule patch.
 
 ## 5. Firearm identity and category adaptation
 
@@ -142,17 +155,11 @@ Passing this item-level gate does not prove that Kingmaker will ignore inherited
 
 ## 7. Development controls
 
-The UMM panel exposes explicit manual controls to grant Firearm Proficiency, add or remove Test Muskets, print equipped and visible firearm-state diagnostics, assign two inventory muskets independent identity-vault states, seed either a Sprint 13 direct-reference record or a Sprint 12 token, and load, damage, repair, or reset the first equipped firearm through the two-layer migration repository.
+The UMM panel exposes explicit disposable-save controls to grant Firearm Proficiency, add or remove Test Muskets, inspect equipped firearm state, mutate the first equipped firearm among Empty/Loaded/Broken/Wrecked diagnostic states, add/count/consume ammunition, inspect reload readiness, and exercise the full-round reload ability.
 
-The development panel retains one non-persistent toggle:
+The panel also shows process-local diagnostics for reload delivery, attack enforcement, range-limited touch AC, optional combat tracing, and native state-token reconciliation. The trace toggle defaults to off and is not campaign state.
 
-```text
-Enable firearm combat tracing (verbose AC and attack log output)
-```
-
-The controls never run automatically, require successful blueprint initialization, fail closed outside an active campaign, catch exceptions at the command boundary, and remain development-only. The trace toggle defaults to off and does not persist in a campaign or UMM settings file.
-
-`KingmakerDevelopmentBridge` and `ReflectionAccess` isolate runtime API uncertainty from the UI. They are temporary development infrastructure, not the eventual production service layer.
+The controls never run automatically, require successful blueprint initialization, fail closed outside an active campaign, and catch exceptions at the command boundary. `KingmakerDevelopmentBridge` and `ReflectionAccess` isolate runtime API uncertainty from the UI; they are development infrastructure, not persistence keys or gameplay identity.
 
 ## 8. Blueprint transaction
 
@@ -160,25 +167,22 @@ The controls never run automatically, require successful blueprint initializatio
 BlueprintRegistry transaction
   -> DiagnosticBlueprints.Register
   -> FirearmProficiencyBlueprints.Register
-  -> resolve native Heavy Crossbow type/item
-  -> clone custom Test Musket weapon type
-  -> append one FirearmDefinitionComponent
-  -> register custom type
-  -> clone custom Test Musket item
-  -> rewire clone to custom type
-  -> append one FirearmProficiencyRestriction
-  -> register custom item
-  -> register four component-only legacy state-token enchantments
-  -> validate eight registrations, token integrity, and native-source immutability
-  -> configure identity-keyed UnitPart vault + reference/token migration layers
+  -> clone/register Test Musket weapon type and item
+  -> append exactly one FirearmDefinitionComponent
+  -> append exactly one FirearmProficiencyRestriction
+  -> register four inert firearm-state token enchantments
+  -> register Black Powder Charge and Lead Ball
+  -> register Reload Test Musket
+  -> validate eleven active registrations and native-source immutability
+  -> configure the token-backed runtime state repository
   -> commit, or RollbackAll in reverse order
 ```
 
-Collision checks happen before factories run and again immediately before dictionary insertion. `BlueprintsByAssetId.Add` prevents accidental replacement.
+Collision checks happen before factories run and again immediately before dictionary insertion. `BlueprintsByAssetId.Add` prevents accidental replacement. Stable GUIDs are never regenerated or repurposed.
 
 ## 9. Combat tracing and firearm AC integration
 
-Three optional Harmony patch classes observe the declared zero-argument `OnTrigger()` methods of:
+Three optional Harmony patch classes observe the exact declared callbacks whose installed signature is `void OnTrigger(Kingmaker.RuleSystem.RulebookEventContext)` on:
 
 ```text
 Kingmaker.RuleSystem.Rules.RuleAttackWithWeapon
@@ -186,7 +190,7 @@ Kingmaker.RuleSystem.Rules.RuleAttackRoll
 Kingmaker.RuleSystem.Rules.RuleCalculateAC
 ```
 
-Each class uses a `void Prefix(object __instance)` and `void Postfix(object __instance)`. The patches accept no `ref`, `out`, or `__result` parameters and return no Boolean control value. Trace code is read-only; the AC postfix delegates one authorized `TargetAC` write to `FirearmArmorClassRuntime`.
+Target resolution requires exactly one non-static, non-generic, `void OnTrigger(RulebookEventContext)` method declared on each intended rule-event type. Each class may omit the native context argument from its Harmony prefix/postfix because it needs only `__instance`; target resolution still requires the exact one-argument installed method. The patches accept no `ref`, `out`, or `__result` parameters and return no Boolean control value. Trace code is read-only; the AC postfix delegates one authorized `TargetAC` write to `FirearmArmorClassRuntime`.
 
 ### Eligibility
 
@@ -218,7 +222,7 @@ For an exact early firearm, the AC adapter applies `current TargetAC + (touch AC
 
 ## 10. Per-item firearm state
 
-Sprint 10 defines the pure immutable state and transition contract:
+The immutable state contract is:
 
 ```text
 FirearmState
@@ -228,57 +232,39 @@ FirearmState
   condition: Normal, Broken, Wrecked
 ```
 
-Capacity and compatibility are supplied through `FirearmStateRules`; pure transitions never retain a Kingmaker or Unity object. A primitive DTO and strict codec establish validation semantics without selecting a save mechanism. See [FIREARM-STATE-MACHINE.md](FIREARM-STATE-MACHINE.md) and [ADR-0017](decisions/ADR-0017-pure-immutable-firearm-state-machine.md).
-
-Sprint 11 supplied the first process-local runtime association through `WeakFirearmStateRepository`. Sprint 12 tested finite item-owned enchantment tokens. Sprint 13 tested direct item references inside a custom UnitPart. Sprint 14 keeps the same exact-firearm service boundary but changes the provisional durable key:
+The supported runtime path is:
 
 ```text
-KingmakerFirearmRuntimeItemResolver
+exact ItemEntityWeapon
+  -> exact BlueprintItemWeapon / BlueprintWeaponType
+  -> exactly one FirearmDefinitionComponent
   -> FirearmItemStateService
-  -> MigratingFirearmStateRepository                 # Sprint 12 token migration
-       -> VaultBackedFirearmStateRepository
-       -> IdentityBackedFirearmStateVaultStore
-            -> KingmakerFirearmItemIdentityProvider
-                 -> exact ItemEntityWeapon.UniqueId
-                 -> nonempty Guid/string only
-            -> KingmakerFirearmStateVaultStore
-                 -> UnitPartFirearmStateVault
-                 -> item GUID string + FirearmStateData
-
-Sprint 13 migration path only:
-  -> legacy direct ItemEntityWeapon reference + FirearmStateData
-  -> resolve exact engine item identity
-  -> verified identity-record write and legacy-record cleanup
-
-Sprint 12 migration path only:
-  -> four stable state-token blueprints
-  -> strict decode, verified identity-vault write, verified token cleanup
+  -> TokenBackedFirearmStateRepository
+  -> KingmakerFirearmStateTokenStore
+  -> zero or one known inert BlueprintWeaponEnchantment token on that item
 ```
 
-The supported service resolves a concrete weapon item and requires exactly one `FirearmDefinitionComponent` on its exact weapon type before any repository access. Native Heavy Crossbows and ambiguous weapons are rejected before state creation.
-
-The UnitPart is a save host, not a wielder-state container. Primary records contain only a canonical engine-issued item GUID string and a primitive DTO. The mod never generates, assigns, repairs, or guesses the identity. Blueprint, display name, owner, slot, inventory index, runtime hash, and value equality are never persistence keys. Canonical empty/Normal is represented by no identity record.
+Absence of a known token is canonical Empty/Normal. Exactly one known token encodes Loaded/Normal, Broken/Empty, Broken/Loaded, or Wrecked. Duplicate, foreign, or ambiguous known-token sets fail closed rather than being guessed or normalized. State writes verify the exact resulting token set.
 
-The identity vault uses expected-current replacement, defensive DTO copies, duplicate-identity rejection, post-write verification, and in-memory rollback. `ConditionalWeakTable` retains only process-local diagnostic IDs and revisions.
+New tokens receive a `MechanicsContext` when a current wielder or owner is available. A prefix/postfix around the exact zero-argument `ItemEntity.ApplyEnchantments()` method captures and verifies known tokens only for `ItemEntityWeapon`. If native reconciliation changes one unambiguous known token to zero, the exact token is restored and verified. Non-weapon items bypass firearm inspection.
 
-Sprint 13 direct-reference records remain in the serialized type only for one-way migration. Null or unresolvable legacy references are preserved rather than pruned. Equivalent state migrates and cleans up; conflicting state preserves both carriers and fails closed. The Sprint 12 token migration layer retains the same verified-write-before-cleanup behavior. Normal new writes create neither legacy carrier.
-
-This remains a provisional persistence carrier, not a durability claim. The installed `UniqueId` contract, identity stability through reconstruction, custom UnitPart serialization, process restart, stash, transfer, equipment, area, respec, sale, duplication, deletion, both migration layers, save growth, and uninstall behavior remain formal runtime gates. See [ENGINE-ITEM-IDENTITY-VAULT.md](ENGINE-ITEM-IDENTITY-VAULT.md), [PERSISTENCE-TEST-MATRIX.md](PERSISTENCE-TEST-MATRIX.md), [UNITPART-STATE-VAULT.md](UNITPART-STATE-VAULT.md), [ADR-0019](decisions/ADR-0019-item-enchantment-state-tokens.md), [ADR-0020](decisions/ADR-0020-save-owned-unitpart-state-vault.md), and [ADR-0021](decisions/ADR-0021-engine-issued-item-identity-vault.md).
+The earlier weak process-local repository, direct-reference UnitPart, and engine-identity vault remain in source for history and pure tests. The installed Kingmaker runtime disproved the assumed `ItemEntityWeapon.UniqueId` contract; those carriers are not configured as the runtime source of truth and must not be revived.
 
 ## 11. Future combat-rule integration
 
 Firearms remain real `BlueprintItemWeapon`/`ItemEntityWeapon` instances and use the normal weapon attack pipeline.
 
-| Event boundary | Future responsibility |
+| Event boundary | Current or next responsibility |
 |---|---|
-| AC calculation | Touch AC only within firearm penetration range |
-| Attack roll | Natural-roll misfire detection |
-| Weapon attack | Exact-item loaded-state validation and one-round consumption |
-| Weapon stats/damage | Gun Training and loaded ammunition effects |
-| Damage/death | Grit recovery for qualifying firearm events |
-| Save/inventory | Persistent item state without duplication |
+| `RuleCalculateAC.OnTrigger(RulebookEventContext)` | Current: touch AC only within firearm penetration range |
+| `RuleAttackRoll.OnTrigger(RulebookEventContext)` | Current: consume one loaded round or force Empty/Wrecked miss; next bounded sprint: natural-d20 misfire detection |
+| Weapon attack/damage | Preserve native modifiers, criticals, concealment, cover, damage, and other mod composition |
+| Reload ability delivery | Current: atomically consume one powder plus one Lead Ball and load the exact firearm |
+| Later class systems | Gun Training, deeds, grit, and class progression |
+
+Loaded Normal and Loaded Broken attacks consume one round at the start of the exact firearm attack roll. Empty, Wrecked, or state-faulted marked firearms are forced to miss. A weak reference-identity gate prevents duplicate callbacks from consuming twice. Firing never consumes shared-inventory ammunition again.
 
-Touch AC must not bypass concealment, images, cover, line of sight, range penalties, or ordinary damage calculation. Sprint 9 implemented the AC-calculation row. Sprint 14 supplies a provisional identity-keyed UnitPart vault and two legacy migration paths; Sprint 15 adds external lifecycle evidence collection. Save durability is unproven and weapon attacks still do not validate or consume loaded state.
+Natural-roll misfire detection, forced-next-roll diagnostics, condition transitions caused by misfires, explosions, and automatic iterative reloads are not part of 0.0.22.1.
 
 ## 12. Persistence evidence boundary
 
@@ -288,9 +274,9 @@ The evidence directory is never read by `FirearmRuntimeState`, any repository, m
 
 ## 13. Ammunition transaction boundary
 
-A loaded round and an inventory item are different states. A later reload service will resolve the exact firearm, validate capacity/action/hand restrictions, resolve compatible ammunition, reserve inputs, complete the action, atomically consume inventory inputs, and update only that firearm's state.
+A loaded round and an inventory item are different states. Black Powder Charge and Lead Ball are real stackable inventory items. The full-round `Reload Test Musket` ability resolves one exact eligible firearm and, only when delivery completes, uses a verified cross-resource transaction to consume exactly one of each component and write one loaded round to that item.
 
-Interrupted or rejected reloads consume nothing. Firing consumes loaded state, not the inventory stack again.
+Rejected or interrupted reloads consume nothing. Missing either component consumes neither. Best-effort rollback restores both sides after a partial or unverifiable mutation. Firing consumes only the already-loaded item state; it does not remove another powder charge or Lead Ball from shared inventory.
 
 ## 14. Save and migration safety
 
@@ -304,55 +290,56 @@ Core mechanics have no dependency on custom models or animation controllers. The
 
 ## 16. Custom blueprint roles
 
-| Symbol | Sprint 16 status | Role |
+| Symbol | Sprint 22 repair status | Role |
 |---|---|---|
 | `KMG.Diagnostic.InitializedFeature` | Active | Hidden initialization proof |
 | `KMG.Firearms.FirearmProficiency` | Active | Shared firearm permission fact |
-| `KMG.Test.TestMusketWeaponType` | Active | Heavy-Crossbow-derived firearm type |
-| `KMG.Test.TestMusketItem` | Active | Restricted test item |
-| `KMG.Test.LoadedStateToken` | Active | Legacy Loaded/Normal migration token |
-| `KMG.Test.BrokenEmptyStateToken` | Active | Legacy Empty/Broken migration token |
-| `KMG.Test.BrokenLoadedStateToken` | Active | Legacy Loaded/Broken migration token |
-| `KMG.Test.WreckedStateToken` | Active | Legacy Wrecked migration token |
-| `KMG.Test.TouchAcEnchantment` | Reserved | Optional future rule-attachment mechanism |
-| `KMG.Test.ReloadAbility` | Reserved | Future reload action |
-| `KMG.Test.BlackPowderItem` | Reserved | Future powder input |
-| `KMG.Test.LeadBulletItem` | Reserved | Future projectile input |
-
-A reservation is not an implemented blueprint. The touch-AC rule currently uses a Harmony rule adapter rather than the reserved enchantment.
+| `KMG.Test.TestMusketWeaponType` | Active | Heavy-Crossbow-derived exact firearm type |
+| `KMG.Test.TestMusketItem` | Active | Restricted Test Musket item |
+| `KMG.Test.LoadedStateToken` | Active | Loaded/Normal item state |
+| `KMG.Test.BrokenEmptyStateToken` | Active | Empty/Broken item state |
+| `KMG.Test.BrokenLoadedStateToken` | Active | Loaded/Broken item state |
+| `KMG.Test.WreckedStateToken` | Active | Wrecked item state |
+| `KMG.Test.ReloadAbility` | Active | Full-round Test Musket reload ability |
+| `KMG.Test.BlackPowderItem` | Active | Stackable powder component |
+| `KMG.Test.LeadBulletItem` | Active | Stackable Lead Ball component; stable symbol retained |
+| `KMG.Test.TouchAcEnchantment` | Reserved | Unused because touch AC is implemented by rule patch |
+
+Stable symbols and GUIDs are never regenerated or repurposed.
 
 ## 17. Dependency and packaging policy
 
-Build-time references come from the developer's local Kingmaker/UMM installation and remain `Private=False`. Build-output and package validators reject copied external DLLs.
+Build-time references come from the developer's exact private Kingmaker 2.1.7b / UMM 0.32.4 bundle and remain compiler input only. Build-output and package validators reject copied game, Unity, Unity Mod Manager, Harmony, Newtonsoft, or other private-reference binaries.
 
-A valid locally built install package contains only:
+A valid standalone UMM package contains one root and exactly one binary:
 
 ```text
 KingmakerGunslinger/
   KingmakerGunslinger.dll
   Info.json
+  CHANGELOG.md
+  LICENSE
+  README.md
+  SMOKE-TEST-GUIDE.md
   blueprints/
     blueprints.json
     blueprints.schema.json
 ```
 
-The Sprint 16 source milestone contains no compiled DLL and is not installable through UMM.
+The complete milestone ZIP embeds the validated standalone install ZIP and source ZIP with checksums and evidence. The private reference archive is never included.
 
 ## 18. Current non-goals
 
-Sprint 16 does not:
-
-- alter attack rolls, damage, criticals, concealment, images, or native Heavy Crossbow AC beyond the existing marker-scoped touch-AC adapter;
-- add ammunition items, reload actions, attack-time misfire handling, or explosion damage;
-- claim that the UnitPart vault, engine item identity, or either legacy migration path survives save/load or process restart before the lifecycle matrix passes;
-- claim that legacy token migration has run in a real save;
-- add the Gunslinger class or deeds;
-- add vendors, loot, or starting equipment;
-- add release-facing localization or assets;
-- support revolvers, dual wielding, scatter weapons, or firearm AI.
+Version 0.0.22.1 does not:
 
-## Sprint 20: basic ammunition inventory boundary
+- add natural-d20 misfire detection or a force-next-roll diagnostic;
+- apply Normal → Broken or Broken → Wrecked transitions from attack rolls;
+- add explosions, area damage, firearm repair gameplay, automatic reloads, or Rapid Reload;
+- change the accepted item-owned token carrier or revive `ItemEntityWeapon.UniqueId` persistence;
+- identify firearms by Heavy Crossbow category, name, slot, inventory position, owner, runtime hash, or value equality;
+- add pistols, scatter weapons, custom firearm assets, vendors, crafting, the Gunslinger class, deeds, grit, or enemy firearm AI;
+- claim runtime success before the repair smoke test passes in Kingmaker.
 
-The first consumable ammunition layer consists of stackable Black Powder Charge and Lead Ball `BlueprintItem` clones plus an engine-independent `IBasicAmmunitionInventory` boundary. `KingmakerBasicAmmunitionInventory` is the only class in this subsystem that knows `ItemsCollection`.
+## Historical subsystem notes
 
-`BasicAmmunitionTransactionService` checks both counts before mutation, removes exactly one of each, verifies exact post-state, and restores the pre-state after a partial or unverifiable failure. This transaction is not yet connected to a reload action. Loaded firearm state remains on the exact gun through the Sprint 19 item-owned enchantment token carrier, whose core implementation was not modified in this sprint.
+Earlier sprint-specific architecture and persistence experiments remain in dedicated documents and ADRs. Where they conflict with this file's Sprint 22 repair layer, the current token-backed runtime path and exact installed method contracts above are authoritative.
diff --git a/docs/BUILDING.md b/docs/BUILDING.md
index 1439fe5ba8fbc3b564a257fed4dce22a7b634648..d6fc9b3e37449aa8d38b391ba0cb37fc0eb568d1 100644
--- a/docs/BUILDING.md
+++ b/docs/BUILDING.md
@@ -1,27 +1,19 @@
-# Building Sprint 17
+# Building version 0.0.22.1
 
-## Sprint 17 preferred path
+## Required target
 
-The preferred Windows entry point is now `scripts\qualify-runtime-candidate.ps1`. It validates the source, fingerprints the exact installation, inspects runtime contracts, executes all 373 dependency-free tests, compiles Release, validates the UMM archive, and seals a local qualification bundle. The historical title is retained because the underlying .NET Framework 4.7 build instructions remain applicable.
+- Pathfinder: Kingmaker Enhanced Plus Edition 2.1.7b
+- Unity Mod Manager 0.32.4
+- Harmony 1.2.0.1 (`0Harmony12.dll`)
+- .NET Framework 4.7 reference surface
+- C# 7.3
+- Release / AnyCPU
+- deterministic compilation
+- warnings treated as errors
 
-## Status
+The private Kingmaker, Unity, Unity Mod Manager, Harmony, and Newtonsoft assemblies are compiler input only. They must never be committed to source or copied into a released UMM package.
 
-This repository is a source/evidence milestone. Its dependency-free suite has been compiled against the .NET Framework 4.7 reference surface and executed. It cannot produce an honest install package until the exact target Kingmaker, Unity Mod Manager, Harmony 1.2, Unity, and Newtonsoft.Json assemblies are supplied.
-
-## Requirements
-
-- Windows PowerShell 5.1 or later.
-- Visual Studio or Visual Studio Build Tools with the .NET Framework 4.7 targeting pack.
-- Pathfinder: Kingmaker Enhanced Plus Edition installed locally.
-- Unity Mod Manager installed for Kingmaker.
-
-The project does not redistribute any of those dependencies.
-
-For a private cross-environment handoff, use `scripts\export-private-build-references.ps1`; see [Private runtime-reference handoff](PRIVATE-RUNTIME-REFERENCE-BUNDLE.md).
-
-## Configure the local game path
-
-From the repository root:
+## Windows installed-game path
 
 ```powershell
 Set-ExecutionPolicy -Scope Process Bypass
@@ -30,9 +22,9 @@ Set-ExecutionPolicy -Scope Process Bypass
   -KingmakerInstallDir 'C:\Path\To\Pathfinder Kingmaker'
 ```
 
-`GamePath.props` is local and ignored. Do not commit or package it.
+`GamePath.props` is local and ignored.
 
-## Inspect the installed contracts
+## Verify the exact runtime contracts
 
 ```powershell
 .\scripts\fingerprint-environment.ps1 `
@@ -44,71 +36,68 @@ Set-ExecutionPolicy -Scope Process Bypass
   -KingmakerInstallDir 'C:\Path\To\Pathfinder Kingmaker'
 ```
 
-Sprint 17 requires every earlier bootstrap, blueprint, proficiency, inventory, attack, distance, armor-class, identity-vault, and evidence contract, plus:
+The Sprint 22 repair specifically requires exactly one declared callback on each intended rule-event type with this shape:
 
-- a non-sealed `Kingmaker.UnitLogic.UnitPart` base type;
-- a concrete `Kingmaker.Items.ItemEntityWeapon` runtime type;
-- exactly one readable inherited `UniqueId` member on that type;
-- a `UniqueId` value type of `System.Guid` or `System.String`;
-- a readable main-character entity from `Game.Instance.Player`;
-- a compatible generic `Get<TPart>()` operation for an existing unit part;
-- a compatible generic `Ensure<TPart>()` operation for creating a unit part;
-- the installed Newtonsoft.Json `JsonPropertyAttribute`;
-- the Sprint 12 item-enchantment read/add/remove contracts needed only for legacy migration.
+```text
+System.Void OnTrigger(Kingmaker.RuleSystem.RulebookEventContext)
+```
 
-Do not bypass a failed contract report. Contract discovery proves only that the API surface is present; save durability still requires the in-game lifecycle matrix.
+It also requires the exact zero-argument `Kingmaker.Items.ItemEntity.ApplyEnchantments()` base method and a concrete `Kingmaker.Items.ItemEntityWeapon` type. The rejected `ItemEntityWeapon.UniqueId` vault is not a build requirement and must not be revived.
 
-## Validate and run dependency-free tests
+## Validate and test
 
 ```powershell
 .\scripts\validate-repository.ps1
 .\scripts\test-domain.ps1 -Configuration Release -Clean
 ```
 
-Expected final test line:
+Expected final line:
 
 ```text
-Completed 373 tests; failures=0.
+Completed 455 tests; failures=0.
 ```
 
-The same dependency-free source list can also be run with a .NET 8 SDK:
-
-```powershell
-.\scripts\test-domain-portable.ps1 -Configuration Release
-```
+For milestone qualification, run the full suite three times and require byte-identical output.
 
 ## Build
 
 ```powershell
-.\scripts\build.ps1 -Configuration Debug -Clean
 .\scripts\build.ps1 -Configuration Release -Clean
 ```
 
-Warnings are treated as errors. External references remain `Private=False`; copied game, Unity, Harmony, UMM, or Newtonsoft.Json dependencies fail output validation.
+External references remain `Private=False`. Any copied game, Unity, Harmony, UMM, or Newtonsoft binary fails output validation.
 
-## Create the install package
+## Build from the supplied private reference bundle
 
-Only after a successful Release build and test run:
+The source also includes a cross-platform Roslyn evidence tool:
 
-```powershell
-.\scripts\build.ps1 -Configuration Release -Clean -Package
+```text
+python tools/build_mod_from_private_references.py \
+  --reference-bundle-dir <extracted-private-bundle> \
+  --dotnet <dotnet-host> \
+  --csc <Roslyn-csc.dll> \
+  --net47-ref-dir <.NETFramework/v4.7> \
+  --output-dir <output> \
+  --configuration Release
 ```
 
-The candidate UMM package is:
+This path compiles against the same exact private assemblies without redistributing them.
 
-```text
-artifacts\packages\KingmakerGunslinger-0.0.17.zip
-```
+## Package contract
 
-It is valid only if `scripts/validate-package.ps1` passes and the ZIP contains this single root:
+The standalone smoke-test ZIP contains one root and exactly one binary:
 
 ```text
 KingmakerGunslinger/
-  KingmakerGunslinger.dll
+  CHANGELOG.md
   Info.json
+  KingmakerGunslinger.dll
+  LICENSE
+  README.md
+  SMOKE-TEST-GUIDE.md
   blueprints/
     blueprints.json
     blueprints.schema.json
 ```
 
-A source milestone archive containing repository files is not an install package.
+A source or complete milestone archive is not itself a UMM package. Label a candidate **READY FOR KINGMAKER** only after the standalone ZIP exists and its package validator passes.
diff --git a/docs/COMBAT-TRACE-SCHEMA.md b/docs/COMBAT-TRACE-SCHEMA.md
index 886c42bdcc955095a90bbd9d9b1a9083146c1cb9..b9ee4ddbae0cba3c00b17b9cf07ec7f60dde7700 100644
--- a/docs/COMBAT-TRACE-SCHEMA.md
+++ b/docs/COMBAT-TRACE-SCHEMA.md
@@ -29,7 +29,7 @@ Every line carries the same trace ID, formatted as `KMG-000001`.
 |---|---|
 | `trace` | Process-lifetime diagnostic trace ID. |
 | `stage` | `WeaponAttack`, `AttackRoll`, or `ArmorClass`. |
-| `phase` | `Before` or `After` the event's `OnTrigger()`. |
+| `phase` | `Before` or `After` the event's `OnTrigger(RulebookEventContext)`. |
 | `event` | Runtime identity hash of the current event object. |
 | `parent` | Nested callback's parent event identity, or `<none>`. |
 | `callback` | Ordinal for duplicate callbacks with the same event/stage/phase. |
diff --git a/docs/LOADED-ROUND-ATTACK-AND-TOKEN-DURABILITY.md b/docs/LOADED-ROUND-ATTACK-AND-TOKEN-DURABILITY.md
index 9d14ace86190c7d3f5ce56fb8ca7f74ae0e56a34..240cafa9cda739fe34c1d92514755bd944fe4b58 100644
--- a/docs/LOADED-ROUND-ATTACK-AND-TOKEN-DURABILITY.md
+++ b/docs/LOADED-ROUND-ATTACK-AND-TOKEN-DURABILITY.md
@@ -1,8 +1,8 @@
-# Loaded-round attack enforcement and token durability
+# Loaded-round attack enforcement, token durability, and 0.0.22.1 hook repair
 
 ## Scope
 
-Sprint 22 closes the first complete basic-firearm loop for the Test Musket:
+Sprint 22 defines the first complete basic-firearm loop for the Test Musket. Version 0.0.22.1 repairs the exact runtime hooks needed to execute it:
 
 1. carry one Black Powder Charge and one Lead Ball;
 2. spend a full-round action to reload;
@@ -25,7 +25,7 @@ The Sprint 21 state token was a dynamic enchantment with a null parent context.
 
 New state tokens use `ItemEntity.AddEnchantment(...)` with a parent `MechanicsContext` whenever the item exposes a current wielder or owner. This makes the token an intentional dynamic enchantment rather than an apparently redundant null-context fact.
 
-A Harmony prefix/postfix also surrounds the exact zero-argument `ItemEntity.ApplyEnchantments()` method:
+A Harmony prefix/postfix also surrounds the exact zero-argument `ItemEntity.ApplyEnchantments()` method. Only `ItemEntityWeapon` instances enter firearm-state inspection:
 
 - the prefix records only the known firearm-state token IDs present on the exact item;
 - native reconciliation runs normally;
@@ -35,9 +35,19 @@ A Harmony prefix/postfix also surrounds the exact zero-argument `ItemEntity.Appl
 
 This fallback is primarily for tokens created by Sprint 21, which may still have a null parent context. The first native refresh may remove and restore such a token; the restored token receives a context when an owner is available.
 
+## Version 0.0.22.1 repair
+
+The installed Kingmaker 2.1.7b rule-event callbacks are `void OnTrigger(RulebookEventContext)`, not zero-argument methods. The repair resolves exactly that signature for `RuleAttackWithWeapon`, `RuleAttackRoll`, and `RuleCalculateAC`. It also rejects non-weapon instances in the `ItemEntity.ApplyEnchantments()` prefix before any firearm-state token inspection, preventing the observed `ItemEntityShield` faults.
+
+## Version 0.0.22 runtime failure and bounded repair
+
+The supplied 0.0.22 result established that quicksave no longer unloaded the Test Musket, but a native attack left it at one loaded round and the attack counters remained at zero. Exact private-assembly inspection showed that all three intended rule-event targets take one `RulebookEventContext`. The same runtime evidence showed `ItemEntityShield` faults because the base `ApplyEnchantments()` prefix inspected non-weapons.
+
+Version 0.0.22.1 changes only those boundaries: exact one-argument rule-event target selection and weapon-only reconciliation inspection. The state machine, item-token carrier, reload transaction, firearm marker, duplicate-event gate, and native Heavy Crossbow exclusion remain unchanged. Sprint 23 misfire work remains blocked pending live acceptance.
+
 ## Attack-time discharge
 
-The existing Harmony patch for `RuleAttackRoll.OnTrigger()` now invokes loaded-round enforcement before Kingmaker calculates the attack result.
+The Harmony patch for the exact installed `RuleAttackRoll.OnTrigger(RulebookEventContext)` callback invokes loaded-round enforcement before Kingmaker calculates the attack result. Version 0.0.22 incorrectly assumed a zero-argument callback and therefore never attached; version 0.0.22.1 repairs that target contract.
 
 Only an exact runtime weapon whose exact weapon type contains exactly one `FirearmDefinitionComponent` is eligible. The borrowed Heavy Crossbow category, icon, model, or name is insufficient. Native Heavy Crossbows are therefore ignored.
 
diff --git a/docs/PACKAGING.md b/docs/PACKAGING.md
index 0633ae06ac6a31dbc756fe596af49c9307d2f91a..486333c8bda9d75fcc26c8b599f4dd5fbc3c3659 100644
--- a/docs/PACKAGING.md
+++ b/docs/PACKAGING.md
@@ -1,43 +1,53 @@
-## Sprint 17 publication state
-
-The published Sprint 17 milestone is source-only. A UMM-installable ZIP is created only by a successful local build against the installed runtime assemblies. `qualify-runtime-candidate.ps1` copies that inner ZIP into a fingerprinted qualification directory and clearly distinguishes smoke-test readiness from persistence `Go`.
-
 # Packaging policy
 
-## Install-package contract
+## Standalone UMM package contract
 
-A Unity Mod Manager package must contain exactly one root named `KingmakerGunslinger` and only project-owned runtime files:
+The current standalone smoke-test package must contain exactly one root named `KingmakerGunslinger` and these eight project-owned files:
 
 ```text
 KingmakerGunslinger/
-  KingmakerGunslinger.dll
+  CHANGELOG.md
   Info.json
+  KingmakerGunslinger.dll
+  LICENSE
+  README.md
+  SMOKE-TEST-GUIDE.md
   blueprints/
     blueprints.json
     blueprints.schema.json
 ```
 
-A PDB is allowed only when explicitly requested for diagnostics. No Kingmaker, Unity, Harmony, UMM, Newtonsoft.Json, or third-party mod DLL may be copied into the package.
-
-`package.ps1` runs build-output validation before staging and `validate-package.ps1` after compression. The validators reject an unexpected root, a missing required file, a foreign DLL, or an undeclared payload.
+Exactly one binary is permitted: `KingmakerGunslinger.dll`. No PDB is included in the standalone candidate. No Kingmaker, Unity, Harmony, Unity Mod Manager, Newtonsoft.Json, private-reference, or third-party mod DLL may appear anywhere in the archive.
 
-## Sprint 17 publication state
+The package validator rejects duplicate entries, traversal paths, symlinks, an unexpected root, missing or extra files, a version mismatch, an altered blueprint ledger, a DLL-hash mismatch, CRC failure, or any foreign binary.
 
-Sprint 17 is published as source/evidence only because this environment lacks the target proprietary Kingmaker and Unity Mod Manager assemblies. The dependency-free suite has been compiled and executed, but the main mod has not. Its milestone ZIP and `source.zip` are review and continuation archives, not UMM packages.
+## Artifact set
 
-A future artifact that is actually installable will be labeled exactly:
+A qualified repair milestone produces:
 
-> **READY FOR KINGMAKER — INSTALL THIS ZIP THROUGH UNITY MOD MANAGER**
+1. a standalone UMM install ZIP and SHA-256 checksum;
+2. a complete milestone ZIP and checksum; and
+3. a source ZIP and checksum.
 
-No source archive receives that label.
+The complete milestone ZIP embeds the validated standalone install ZIP and source ZIP, reports, evidence, and checksums. It does not contain the private build-reference archive or loose private assemblies.
 
 ## Source-package exclusions
 
-The source package excludes:
+The source ZIP excludes:
+
+- `.git`, IDE state, and generated `artifacts` output;
+- `GamePath.props`, absolute install paths, and local environment fingerprints;
+- private reference archives and all game/Unity/UMM/Harmony/Newtonsoft binaries;
+- compiled DLL, EXE, PDB, MDB, PYC, and cache files;
+- local saves, logs, and test-campaign data; and
+- generated outer-package manifests or checksums that would create circular hashes.
+
+Source evidence may include plain-text compiler/test output, JSON reports, patches, and the user-supplied runtime screenshots.
+
+## Labeling
+
+A source or milestone archive is never the install target. Use this label only when the standalone UMM ZIP actually exists and passes validation:
+
+> **READY FOR KINGMAKER — INSTALL THIS ZIP THROUGH UNITY MOD MANAGER**
 
-- `.git`, IDE state, and `artifacts`;
-- `GamePath.props` and absolute local paths;
-- environment fingerprints and runtime-contract reports;
-- DLL, EXE, PDB, MDB, PYC, and copied dependencies;
-- generated outer-package manifests and checksums;
-- local saves, logs, and test-campaign data.
+Runtime acceptance remains separate from compile/package readiness. Version 0.0.22.1 still requires the full in-game repair smoke test before Sprint 23 can begin.
diff --git a/docs/decisions/ADR-0015-read-only-firearm-combat-tracing.md b/docs/decisions/ADR-0015-read-only-firearm-combat-tracing.md
index 992f16519367cacfafd16701714cc7ae9478a62c..9c709408031fa2fb768b87812ed63c0ae1035e7c 100644
--- a/docs/decisions/ADR-0015-read-only-firearm-combat-tracing.md
+++ b/docs/decisions/ADR-0015-read-only-firearm-combat-tracing.md
@@ -1,15 +1,17 @@
 # ADR-0015: Read-only marker-scoped firearm combat tracing
 
-- **Status:** Accepted for Sprint 8
+- **Status:** Accepted for Sprint 8; runtime signature corrected by ADR-0028; callback-signature detail superseded by ADR-0028
 - **Date:** 2026-07-12
 
+> **0.0.22.1 correction:** Kingmaker 2.1.7b declares these callbacks as `void OnTrigger(RulebookEventContext)`. The original zero-argument assumption caused the patches to be skipped. ADR-0028 supersedes only that target-signature detail; the marker-scoped, read-only tracing design remains accepted.
+
 ## Context
 
 Touch-AC and misfire mechanics depend on the exact order and contents of Kingmaker rule events. Implementing them from remembered APIs would risk modifying the wrong AC calculation, using an attack total as a natural d20, or applying firearm behavior to all Heavy Crossbows.
 
 ## Decision
 
-Install disabled-by-default prefix/postfix observers on the declared zero-argument `OnTrigger()` methods of `RuleAttackWithWeapon`, `RuleAttackRoll`, and `RuleCalculateAC`.
+Install disabled-by-default prefix/postfix observers on `RuleAttackWithWeapon`, `RuleAttackRoll`, and `RuleCalculateAC`. The original Sprint 8 text assumed zero-argument callbacks; exact Kingmaker 2.1.7b inspection later established `void OnTrigger(RulebookEventContext)`, now enforced by ADR-0028.
 
 A root trace begins only when the concrete weapon's exact `BlueprintWeaponType` has exactly one `FirearmDefinitionComponent`. Nested callbacks join by event identity and callback nesting. The runtime adapter copies values immediately into immutable diagnostic snapshots and retains no Kingmaker or Unity objects.
 
diff --git a/docs/decisions/ADR-0016-range-limited-touch-ac-delta.md b/docs/decisions/ADR-0016-range-limited-touch-ac-delta.md
index 69726abb6e5b401b8df1945b118765b655b32d13..2823c32b69cdefa063f058f32f97e757dd5538f5 100644
--- a/docs/decisions/ADR-0016-range-limited-touch-ac-delta.md
+++ b/docs/decisions/ADR-0016-range-limited-touch-ac-delta.md
@@ -20,7 +20,7 @@ For an exact early firearm in range increment one, with a 0.1-millimeter toleran
 selected TargetAC = current TargetAC + (touch AC - ordinary AC)
 ```
 
-Apply that selection in a guarded postfix after `RuleCalculateAC.OnTrigger()` and before the optional after-trace snapshot.
+Apply that selection in a guarded postfix after the exact installed `RuleCalculateAC.OnTrigger(RulebookEventContext)` callback and before the optional after-trace snapshot.
 
 Use a short-lived `RuleAttackRoll` context to carry only immutable marker metadata. Prefer a directly resolved weapon on the AC event when available. Use a weak per-event stamp to prevent duplicate application.
 
diff --git a/docs/decisions/ADR-0028-exact-rule-event-contract-and-weapon-only-reconciliation.md b/docs/decisions/ADR-0028-exact-rule-event-contract-and-weapon-only-reconciliation.md
new file mode 100644
index 0000000000000000000000000000000000000000..0f21cfb493c14cb087c1c30f7828919f57e1b4e4
--- /dev/null
+++ b/docs/decisions/ADR-0028-exact-rule-event-contract-and-weapon-only-reconciliation.md
@@ -0,0 +1,63 @@
+# ADR-0028: Bind exact rule-event callbacks and inspect reconciliation only on weapons
+
+- **Status:** Accepted for the Sprint 22 repair
+- **Date:** 2026-07-14
+
+## Context
+
+Version 0.0.22 preserved a loaded Test Musket through the supplied quicksave test, but an ordinary attack left the exact item loaded and `Firearm attack enforcement` remained at zero observations. The same runtime evidence showed repeated `MissingMemberException` faults while native token reconciliation inspected `Kingmaker.Items.ItemEntityShield`.
+
+Exact inspection of the private Kingmaker 2.1.7b `Assembly-CSharp.dll` established:
+
+- `RuleAttackWithWeapon.OnTrigger`, `RuleAttackRoll.OnTrigger`, and `RuleCalculateAC.OnTrigger` each declare `void OnTrigger(Kingmaker.RuleSystem.RulebookEventContext)`;
+- version 0.0.22 searched for zero-argument methods and therefore selected no target; and
+- `ItemEntity.ApplyEnchantments()` is a zero-argument base method invoked for non-weapon item subclasses as well as weapons.
+
+The attack pipeline itself was not the failure. The Harmony target contract and the reconciliation boundary were wrong.
+
+## Decision
+
+Resolve each intended rule-event target only when the declaring type exposes exactly one method satisfying all of these conditions:
+
+```text
+name:          OnTrigger
+instance:      yes
+static:        no
+generic:       no
+return type:   System.Void
+parameters:    exactly one
+parameter 0:   Kingmaker.RuleSystem.RulebookEventContext
+```
+
+Missing or ambiguous contracts skip the affected patch and produce a structured warning rather than binding a plausible overload.
+
+At the `ItemEntity.ApplyEnchantments()` prefix, cast `__instance` to `ItemEntityWeapon` before calling firearm-state reconciliation. Non-weapons receive an empty invocation state and proceed through native Kingmaker logic unchanged. The postfix exits immediately for that empty state.
+
+Keep the existing item-owned inert `BlueprintWeaponEnchantment` state carrier, exact firearm marker, loaded/empty/Broken/Wrecked decisions, duplicate-event gate, and reload transaction unchanged.
+
+## Rejected alternatives
+
+### Revive the `ItemEntityWeapon.UniqueId` vault
+
+Rejected. The installed Kingmaker runtime disproved the required member contract, and the handoff explicitly forbids returning to that design.
+
+### Match any method named `OnTrigger`
+
+Rejected because an overload or future runtime change could silently bind the wrong method and mutate combat incorrectly.
+
+### Patch a different attack method because the native shot completed
+
+Rejected because the diagnostics proved the intended callback never attached. The smallest evidence-driven repair is to bind the exact installed method, not replace Kingmaker's weapon pipeline.
+
+### Catch and ignore non-weapon reflection faults
+
+Rejected because non-weapons are outside the firearm-state domain. They should not enter token inspection at all.
+
+## Consequences
+
+- Loaded-round enforcement, touch-AC integration, and optional combat tracing can attach to the installed Kingmaker 2.1.7b rule-event methods.
+- Shield and other non-weapon `ItemEntity` refreshes cannot invoke firearm-token reflection.
+- The repair remains fail closed if the installed method contract changes or becomes ambiguous.
+- Nine dependency-free reflection-contract tests prevent the zero-argument assumption from returning.
+- The Windows runtime-contract inspector now checks the same one-argument signature.
+- Sprint 23 remains blocked until the repaired candidate passes the full in-game acceptance matrix, including native Heavy Crossbow isolation and zero KMG/Harmony faults.
diff --git a/docs/decisions/INDEX.md b/docs/decisions/INDEX.md
index a3868832f16d69a072f97a4186b4c7c36f00e1de..1d59037ec17b201d120a6d4a04a4c8dea7f29cd0 100644
--- a/docs/decisions/INDEX.md
+++ b/docs/decisions/INDEX.md
@@ -26,3 +26,4 @@
 - [ADR-0024: Execute the net47 harness and require exact private runtime references](ADR-0024-executed-evidence-and-private-reference-handoff.md)
 - [ADR-0026: Use a full-round delivery-time reload with a cross-resource transaction](ADR-0026-full-round-reload-cross-resource-transaction.md)
 - [ADR-0027: Enforce loaded rounds in RuleAttackRoll and guard native token reconciliation](ADR-0027-loaded-round-enforcement-and-native-token-reconciliation.md)
+- [ADR-0028: Bind exact rule-event callbacks and inspect reconciliation only on weapons](ADR-0028-exact-rule-event-contract-and-weapon-only-reconciliation.md)
diff --git a/evidence/sprint22-repair/compile/compile.stderr.txt b/evidence/sprint22-repair/compile/compile.stderr.txt
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/evidence/sprint22-repair/compile/compile.stdout.txt b/evidence/sprint22-repair/compile/compile.stdout.txt
new file mode 100644
index 0000000000000000000000000000000000000000..82c2290958f63596a09704fed372b1557bc9bb48
--- /dev/null
+++ b/evidence/sprint22-repair/compile/compile.stdout.txt
@@ -0,0 +1,3 @@
+Microsoft (R) Visual C# Compiler version 3.11.0-4.21403.6 (ae1fff34)
+Copyright (C) Microsoft Corporation. All rights reserved.
+
diff --git a/evidence/sprint22-repair/compile/compiled-assembly-metadata.json b/evidence/sprint22-repair/compile/compiled-assembly-metadata.json
new file mode 100644
index 0000000000000000000000000000000000000000..33f43032b5a16ed1d553fca331b9157003852b8b
--- /dev/null
+++ b/evidence/sprint22-repair/compile/compiled-assembly-metadata.json
@@ -0,0 +1,11 @@
+{
+  "assemblyName": "KingmakerGunslinger",
+  "assemblyVersion": "0.0.22.1",
+  "dllSha256": "36f46686856d2f0b04bf91ddd9665b47d9c6676de96af428bd0d697ec32fa463",
+  "dllSizeBytes": 339456,
+  "moduleName": "KingmakerGunslinger.dll",
+  "mvid": "b994b27e-9fb3-4229-acdf-cda1cd633610",
+  "pdbSha256": "b76d3cb21da6d21154d756a95266cadb6e9c06b2790576be0b53b10a62ff2ee7",
+  "pdbSizeBytes": 96984,
+  "schemaVersion": 1
+}
diff --git a/evidence/sprint22-repair/compile/exact-reference-compile.json b/evidence/sprint22-repair/compile/exact-reference-compile.json
new file mode 100644
index 0000000000000000000000000000000000000000..a8226a3a1ba7dcea14378d5878c0eee6983a1fd3
--- /dev/null
+++ b/evidence/sprint22-repair/compile/exact-reference-compile.json
@@ -0,0 +1,46 @@
+{
+  "blueprintManifestSha256": "af56f2a35bd05e055e8feee1e996d37abcb20b76efb90762784f31ccfe933337",
+  "classification": "READY FOR KINGMAKER SPRINT 22 REPAIR SMOKE TEST AFTER PACKAGE VALIDATION",
+  "compileExitCode": 0,
+  "compilerSha256": "f1b8f79f1761971b82a6d8a8f3fe82c45c6000b33a0a707fca532daebfca0740",
+  "compilerStderrEmpty": true,
+  "compilerStderrSha256": "e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855",
+  "compilerStdoutSha256": "39f29d01691b45513b14f82bc7be03dc1ecee82e57dd23d84f10975960e12835",
+  "compilerVersion": "3.11.0-4.21403.6",
+  "configuration": "Release",
+  "deterministicCompilerMode": true,
+  "harmonyVersion": "1.2.0.1",
+  "informationalVersion": "0.0.22.1-s22-attack-hook-repair",
+  "languageVersion": "7.3",
+  "modDllSha256": "36f46686856d2f0b04bf91ddd9665b47d9c6676de96af428bd0d697ec32fa463",
+  "modDllSizeBytes": 339456,
+  "modVersion": "0.0.22.1",
+  "pdbSha256": "b76d3cb21da6d21154d756a95266cadb6e9c06b2790576be0b53b10a62ff2ee7",
+  "pdbSizeBytes": 96984,
+  "platform": "AnyCPU",
+  "privateReferenceHashes": {
+    "Managed/Assembly-CSharp-firstpass.dll": "069a7362ce5e3ccd597206174aec13743c2db5a1bfbc2a42f15a5fbd1ea30d30",
+    "Managed/Assembly-CSharp.dll": "3b6450ffec440e296e586f71c711b195aed144b28d53e1cbb29406d18fef5afb",
+    "Managed/Newtonsoft.Json.dll": "3a23988f473e9e8304fbbcfff16872cca919bd1a1f3dee1e374acc1f0cbbbc80",
+    "Managed/UnityEngine.AnimationModule.dll": "18636cc226c7e6299e80210d4ae808eb2552cbcdb92c8713caee6a7f0e1be1a5",
+    "Managed/UnityEngine.AssetBundleModule.dll": "a11128e604a59eadd61ea870f2017ea18d4c19240dd1c1b8b6d4ffb7976b569b",
+    "Managed/UnityEngine.CoreModule.dll": "3a76df7f709d465e3273502e08edbffb536b1c2f78c3a132b8668e59fddd2803",
+    "Managed/UnityEngine.UI.dll": "4dee942e8974492511733e5482f89fde39e3964008f072ec9c12d05e40a9080d",
+    "Managed/UnityEngine.dll": "50b79c57f46bf4e060bb27461b6d4fac6f078e08b57e776c7a811554c4899fd6",
+    "Managed/UnityModManager/0Harmony12.dll": "aa1cd48317254985d8b700cc74953477d1b40c3022ce9aa4c95ed2b8327e1292",
+    "Managed/UnityModManager/UnityModManager.dll": "1387468bc3af41c50fe51859a3bb7af4922891aa8f13a6187e7a348ceaabfd88"
+  },
+  "privateReferenceManifestSha256": "c012266487888df1025577402234841e365ace5d1dc18e61cc1fa89cfa9c5c04",
+  "privateReferenceManifestVerified": true,
+  "privateReferencesRedistributed": false,
+  "runtimeGate": "Sprint23Blocked_Pending0.0.22.1AttackHookRepairRuntimeAcceptance",
+  "sameOutputPathCompileRuns": 2,
+  "sameOutputPathDllByteIdentical": true,
+  "sameOutputPathPdbByteIdentical": true,
+  "schemaVersion": 1,
+  "standaloneInstallPackageSha256": "83423ad78a5e62dd4e329c07c1f5ddb20b9bd49567690e5bab6f9495e7263541",
+  "targetFrameworkReferenceSurface": ".NET Framework 4.7",
+  "targetGame": "Pathfinder: Kingmaker 2.1.7b",
+  "unityModManagerVersion": "0.32.4",
+  "warningsAsErrors": true
+}
diff --git a/evidence/sprint22-repair/compile/private-reference-verification.json b/evidence/sprint22-repair/compile/private-reference-verification.json
new file mode 100644
index 0000000000000000000000000000000000000000..fd08ae26adcafe1bc08c54de0c6f541dcd614bdd
--- /dev/null
+++ b/evidence/sprint22-repair/compile/private-reference-verification.json
@@ -0,0 +1,110 @@
+{
+  "allFilesMatchedManifest": true,
+  "files": [
+    {
+      "actualSha256": "3b6450ffec440e296e586f71c711b195aed144b28d53e1cbb29406d18fef5afb",
+      "actualSizeBytes": 7262208,
+      "assemblyIdentity": "Assembly-CSharp, Version=0.0.0.0, Culture=neutral, PublicKeyToken=null",
+      "expectedSha256": "3b6450ffec440e296e586f71c711b195aed144b28d53e1cbb29406d18fef5afb",
+      "expectedSizeBytes": 7262208,
+      "fileVersion": "0.0.0.0",
+      "matchesManifest": true,
+      "relativePath": "Managed/Assembly-CSharp.dll"
+    },
+    {
+      "actualSha256": "069a7362ce5e3ccd597206174aec13743c2db5a1bfbc2a42f15a5fbd1ea30d30",
+      "actualSizeBytes": 3867136,
+      "assemblyIdentity": "Assembly-CSharp-firstpass, Version=0.0.0.0, Culture=neutral, PublicKeyToken=null",
+      "expectedSha256": "069a7362ce5e3ccd597206174aec13743c2db5a1bfbc2a42f15a5fbd1ea30d30",
+      "expectedSizeBytes": 3867136,
+      "fileVersion": "0.0.0.0",
+      "matchesManifest": true,
+      "relativePath": "Managed/Assembly-CSharp-firstpass.dll"
+    },
+    {
+      "actualSha256": "3a23988f473e9e8304fbbcfff16872cca919bd1a1f3dee1e374acc1f0cbbbc80",
+      "actualSizeBytes": 456704,
+      "assemblyIdentity": "Newtonsoft.Json, Version=8.0.0.0, Culture=neutral, PublicKeyToken=null",
+      "expectedSha256": "3a23988f473e9e8304fbbcfff16872cca919bd1a1f3dee1e374acc1f0cbbbc80",
+      "expectedSizeBytes": 456704,
+      "fileVersion": "8.0.0.0",
+      "matchesManifest": true,
+      "relativePath": "Managed/Newtonsoft.Json.dll"
+    },
+    {
+      "actualSha256": "50b79c57f46bf4e060bb27461b6d4fac6f078e08b57e776c7a811554c4899fd6",
+      "actualSizeBytes": 72192,
+      "assemblyIdentity": "UnityEngine, Version=0.0.0.0, Culture=neutral, PublicKeyToken=null",
+      "expectedSha256": "50b79c57f46bf4e060bb27461b6d4fac6f078e08b57e776c7a811554c4899fd6",
+      "expectedSizeBytes": 72192,
+      "fileVersion": "0.0.0.0",
+      "matchesManifest": true,
+      "relativePath": "Managed/UnityEngine.dll"
+    },
+    {
+      "actualSha256": "18636cc226c7e6299e80210d4ae808eb2552cbcdb92c8713caee6a7f0e1be1a5",
+      "actualSizeBytes": 137216,
+      "assemblyIdentity": "UnityEngine.AnimationModule, Version=0.0.0.0, Culture=neutral, PublicKeyToken=null",
+      "expectedSha256": "18636cc226c7e6299e80210d4ae808eb2552cbcdb92c8713caee6a7f0e1be1a5",
+      "expectedSizeBytes": 137216,
+      "fileVersion": "0.0.0.0",
+      "matchesManifest": true,
+      "relativePath": "Managed/UnityEngine.AnimationModule.dll"
+    },
+    {
+      "actualSha256": "a11128e604a59eadd61ea870f2017ea18d4c19240dd1c1b8b6d4ffb7976b569b",
+      "actualSizeBytes": 20992,
+      "assemblyIdentity": "UnityEngine.AssetBundleModule, Version=0.0.0.0, Culture=neutral, PublicKeyToken=null",
+      "expectedSha256": "a11128e604a59eadd61ea870f2017ea18d4c19240dd1c1b8b6d4ffb7976b569b",
+      "expectedSizeBytes": 20992,
+      "fileVersion": "0.0.0.0",
+      "matchesManifest": true,
+      "relativePath": "Managed/UnityEngine.AssetBundleModule.dll"
+    },
+    {
+      "actualSha256": "3a76df7f709d465e3273502e08edbffb536b1c2f78c3a132b8668e59fddd2803",
+      "actualSizeBytes": 848896,
+      "assemblyIdentity": "UnityEngine.CoreModule, Version=0.0.0.0, Culture=neutral, PublicKeyToken=null",
+      "expectedSha256": "3a76df7f709d465e3273502e08edbffb536b1c2f78c3a132b8668e59fddd2803",
+      "expectedSizeBytes": 848896,
+      "fileVersion": "0.0.0.0",
+      "matchesManifest": true,
+      "relativePath": "Managed/UnityEngine.CoreModule.dll"
+    },
+    {
+      "actualSha256": "4dee942e8974492511733e5482f89fde39e3964008f072ec9c12d05e40a9080d",
+      "actualSizeBytes": 252928,
+      "assemblyIdentity": "UnityEngine.UI, Version=1.0.0.0, Culture=neutral, PublicKeyToken=null",
+      "expectedSha256": "4dee942e8974492511733e5482f89fde39e3964008f072ec9c12d05e40a9080d",
+      "expectedSizeBytes": 252928,
+      "fileVersion": "1.0.0.0",
+      "matchesManifest": true,
+      "relativePath": "Managed/UnityEngine.UI.dll"
+    },
+    {
+      "actualSha256": "1387468bc3af41c50fe51859a3bb7af4922891aa8f13a6187e7a348ceaabfd88",
+      "actualSizeBytes": 204288,
+      "assemblyIdentity": "UnityModManager, Version=0.32.4.0, Culture=neutral, PublicKeyToken=null",
+      "expectedSha256": "1387468bc3af41c50fe51859a3bb7af4922891aa8f13a6187e7a348ceaabfd88",
+      "expectedSizeBytes": 204288,
+      "fileVersion": "0.32.4",
+      "matchesManifest": true,
+      "relativePath": "Managed/UnityModManager/UnityModManager.dll"
+    },
+    {
+      "actualSha256": "aa1cd48317254985d8b700cc74953477d1b40c3022ce9aa4c95ed2b8327e1292",
+      "actualSizeBytes": 102400,
+      "assemblyIdentity": "0Harmony12, Version=1.2.0.1, Culture=neutral, PublicKeyToken=null",
+      "expectedSha256": "aa1cd48317254985d8b700cc74953477d1b40c3022ce9aa4c95ed2b8327e1292",
+      "expectedSizeBytes": 102400,
+      "fileVersion": "1.2.0.1",
+      "matchesManifest": true,
+      "relativePath": "Managed/UnityModManager/0Harmony12.dll"
+    }
+  ],
+  "privateCompilerInputsOnly": true,
+  "schemaVersion": 1,
+  "sourceManifestCreatedAtUtc": "2026-07-13T23:37:50.1140622Z",
+  "sourceManifestSha256": "c012266487888df1025577402234841e365ace5d1dc18e61cc1fa89cfa9c5c04",
+  "targetGameDisplayedVersion": "2.1.7b"
+}
diff --git a/evidence/sprint22-repair/compile/same-output-path-determinism.txt b/evidence/sprint22-repair/compile/same-output-path-determinism.txt
new file mode 100644
index 0000000000000000000000000000000000000000..a2110474563cb0bff1722bfc8f4d79af9e3e3284
--- /dev/null
+++ b/evidence/sprint22-repair/compile/same-output-path-determinism.txt
@@ -0,0 +1,6 @@
+First same-output-path DLL SHA-256: 36f46686856d2f0b04bf91ddd9665b47d9c6676de96af428bd0d697ec32fa463
+Second same-output-path DLL SHA-256: 36f46686856d2f0b04bf91ddd9665b47d9c6676de96af428bd0d697ec32fa463
+DLL byte-identical: yes
+First same-output-path PDB SHA-256: b76d3cb21da6d21154d756a95266cadb6e9c06b2790576be0b53b10a62ff2ee7
+Second same-output-path PDB SHA-256: b76d3cb21da6d21154d756a95266cadb6e9c06b2790576be0b53b10a62ff2ee7
+PDB byte-identical: yes
diff --git a/evidence/sprint22-repair/exact-runtime-contracts.json b/evidence/sprint22-repair/exact-runtime-contracts.json
new file mode 100644
index 0000000000000000000000000000000000000000..e735723b51bbe913f429791d35d92ab08c6b4197
--- /dev/null
+++ b/evidence/sprint22-repair/exact-runtime-contracts.json
@@ -0,0 +1,147 @@
+{
+  "assembly": {
+    "pathLabel": "private/Managed/Assembly-CSharp.dll",
+    "sha256": "3b6450ffec440e296e586f71c711b195aed144b28d53e1cbb29406d18fef5afb"
+  },
+  "conclusions": [
+    "All three relevant rule events declare void OnTrigger(Kingmaker.RuleSystem.RulebookEventContext), not a zero-argument OnTrigger.",
+    "The 0.0.22 zero-argument resolver therefore selected zero candidates and Harmony skipped attack enforcement, touch-AC integration, and combat tracing at those targets.",
+    "ItemEntity.ApplyEnchantments is the intended zero-argument base target, but it executes for non-weapon ItemEntity subclasses including shields.",
+    "The repair binds the exact one-argument rule-event contract and limits state-token inspection to ItemEntityWeapon instances.",
+    "No Sprint 23 misfire behavior is present in this repair."
+  ],
+  "itemApplyEnchantmentsContract": [
+    {
+      "declaringType": "Kingmaker.Items.ItemEntity",
+      "isPublic": false,
+      "isStatic": false,
+      "isVirtual": false,
+      "metadataToken": "0x06007b5a",
+      "name": "ApplyEnchantments",
+      "oldZeroArgumentMatcherWouldAccept": true,
+      "parameters": [],
+      "repairedExactMatcherWouldAccept": false,
+      "rva": "0x1fd9c4",
+      "signature": {
+        "explicitThis": false,
+        "generic": false,
+        "hasThis": true,
+        "parameterCount": 0,
+        "parameterTypes": [],
+        "rawHex": "200001",
+        "returnType": "System.Void"
+      }
+    }
+  ],
+  "observedRuntimeFailure": {
+    "attackEnforcementFired": 0,
+    "attackEnforcementObserved": 0,
+    "lastTokenFault": "MissingMemberException: Runtime item type 'Kingmaker.Items.ItemEntityShield' exposes no readable enumerable enchantment collection.",
+    "loadedRoundRemainedAfterAttack": true,
+    "reloadRemainedUnavailable": true,
+    "tokenReconciliationFaults": 10
+  },
+  "purpose": "Sprint 22 repair exact-runtime contract evidence",
+  "ruleEventContracts": {
+    "Kingmaker.RuleSystem.Rules.RuleAttackRoll": [
+      {
+        "declaringType": "Kingmaker.RuleSystem.Rules.RuleAttackRoll",
+        "isPublic": true,
+        "isStatic": false,
+        "isVirtual": true,
+        "metadataToken": "0x06007173",
+        "name": "OnTrigger",
+        "oldZeroArgumentMatcherWouldAccept": false,
+        "parameters": [
+          {
+            "name": "context",
+            "sequence": 1
+          }
+        ],
+        "repairedExactMatcherWouldAccept": true,
+        "rva": "0x1e051c",
+        "signature": {
+          "explicitThis": false,
+          "generic": false,
+          "hasThis": true,
+          "parameterCount": 1,
+          "parameterTypes": [
+            "Kingmaker.RuleSystem.RulebookEventContext"
+          ],
+          "rawHex": "20010112b4fc",
+          "returnType": "System.Void"
+        }
+      }
+    ],
+    "Kingmaker.RuleSystem.Rules.RuleAttackWithWeapon": [
+      {
+        "declaringType": "Kingmaker.RuleSystem.Rules.RuleAttackWithWeapon",
+        "isPublic": true,
+        "isStatic": false,
+        "isVirtual": true,
+        "metadataToken": "0x0600719d",
+        "name": "OnTrigger",
+        "oldZeroArgumentMatcherWouldAccept": false,
+        "parameters": [
+          {
+            "name": "context",
+            "sequence": 1
+          }
+        ],
+        "repairedExactMatcherWouldAccept": true,
+        "rva": "0x1e0c90",
+        "signature": {
+          "explicitThis": false,
+          "generic": false,
+          "hasThis": true,
+          "parameterCount": 1,
+          "parameterTypes": [
+            "Kingmaker.RuleSystem.RulebookEventContext"
+          ],
+          "rawHex": "20010112b4fc",
+          "returnType": "System.Void"
+        }
+      }
+    ],
+    "Kingmaker.RuleSystem.Rules.RuleCalculateAC": [
+      {
+        "declaringType": "Kingmaker.RuleSystem.Rules.RuleCalculateAC",
+        "isPublic": true,
+        "isStatic": false,
+        "isVirtual": true,
+        "metadataToken": "0x060071b8",
+        "name": "OnTrigger",
+        "oldZeroArgumentMatcherWouldAccept": false,
+        "parameters": [
+          {
+            "name": "context",
+            "sequence": 1
+          }
+        ],
+        "repairedExactMatcherWouldAccept": true,
+        "rva": "0x1e1174",
+        "signature": {
+          "explicitThis": false,
+          "generic": false,
+          "hasThis": true,
+          "parameterCount": 1,
+          "parameterTypes": [
+            "Kingmaker.RuleSystem.RulebookEventContext"
+          ],
+          "rawHex": "20010112b4fc",
+          "returnType": "System.Void"
+        }
+      }
+    ]
+  },
+  "schemaVersion": 1,
+  "source": {
+    "ruleEventPatchContractSha256": "352c4c241d6bcb203b6231ec677c5e0a9ff2f9d3177cf211ebc4775eba49d894",
+    "ruleEventPatchTargetSha256": "0ed7f2c70d5025da8d7e25d10bc46918076d3bb4afa49570b698ad437ab751ab",
+    "runtimeInspectionScriptSha256": "28a52e3533fc0251f1b31b268769749dce4047ae9a62174e74b6d4b304cc8ae1",
+    "tokenReconciliationPatchSha256": "5c83e90135f98959af9d8d97d2b140c143ba7dac4c90cc94cff04d93f71d8ade"
+  },
+  "tooling": {
+    "dnfileVersion": "0.18.0"
+  }
+}
diff --git a/evidence/sprint22-repair/package/install-package-validation.txt b/evidence/sprint22-repair/package/install-package-validation.txt
new file mode 100644
index 0000000000000000000000000000000000000000..4ccd40b06b708c4bdbf4fc3036cb63e794e69130
--- /dev/null
+++ b/evidence/sprint22-repair/package/install-package-validation.txt
@@ -0,0 +1,4 @@
+Sprint 22 repair UMM package validation passed.
+Archive SHA-256: 83423ad78a5e62dd4e329c07c1f5ddb20b9bd49567690e5bab6f9495e7263541
+DLL SHA-256: 36f46686856d2f0b04bf91ddd9665b47d9c6676de96af428bd0d697ec32fa463
+Entries: 8; binaries: project-owned KingmakerGunslinger.dll only.
diff --git a/evidence/sprint22-repair/package/install-package.json b/evidence/sprint22-repair/package/install-package.json
new file mode 100644
index 0000000000000000000000000000000000000000..d9cc2abb12496facee2405399a286c468a5de75a
--- /dev/null
+++ b/evidence/sprint22-repair/package/install-package.json
@@ -0,0 +1,78 @@
+{
+  "archiveValidation": {
+    "crcPassed": true,
+    "duplicateEntries": 0,
+    "expectedLayout": true,
+    "onlyProjectOwnedBinary": true,
+    "pathTraversalEntries": 0,
+    "symlinkEntries": 0
+  },
+  "binaryEntries": [
+    "KingmakerGunslinger/KingmakerGunslinger.dll"
+  ],
+  "blueprintManifestSha256": "af56f2a35bd05e055e8feee1e996d37abcb20b76efb90762784f31ccfe933337",
+  "classification": "READY FOR KINGMAKER SPRINT 22 REPAIR SMOKE TEST",
+  "entries": [
+    {
+      "path": "KingmakerGunslinger/CHANGELOG.md",
+      "sha256": "fd72640125431ac667ce86c029c274d6d74a8de32aca3a7b5e66cc0e55ff7044",
+      "sizeBytes": 30978
+    },
+    {
+      "path": "KingmakerGunslinger/Info.json",
+      "sha256": "185a18496fd30ff4d5cecda95a219d8f95e6d48f03a01aadca394a23633db004",
+      "sizeBytes": 268
+    },
+    {
+      "path": "KingmakerGunslinger/KingmakerGunslinger.dll",
+      "sha256": "36f46686856d2f0b04bf91ddd9665b47d9c6676de96af428bd0d697ec32fa463",
+      "sizeBytes": 339456
+    },
+    {
+      "path": "KingmakerGunslinger/LICENSE",
+      "sha256": "18bfbd53a938959e77483994b4445f10e17b0cf79eb3308b0c416c14b33903c2",
+      "sizeBytes": 1100
+    },
+    {
+      "path": "KingmakerGunslinger/README.md",
+      "sha256": "a64f3cdc72fc5ddaf77ac9886bfe6467f33a8cbbf417f820b3034600670f4e3c",
+      "sizeBytes": 1714
+    },
+    {
+      "path": "KingmakerGunslinger/SMOKE-TEST-GUIDE.md",
+      "sha256": "4f0bd4a62cb7c925405b805f95c633089decdd85bb8937b8ac4e501cc30ab66f",
+      "sizeBytes": 7123
+    },
+    {
+      "path": "KingmakerGunslinger/blueprints/blueprints.json",
+      "sha256": "af56f2a35bd05e055e8feee1e996d37abcb20b76efb90762784f31ccfe933337",
+      "sizeBytes": 4557
+    },
+    {
+      "path": "KingmakerGunslinger/blueprints/blueprints.schema.json",
+      "sha256": "60433421fc40be98373401ebd1103dff366fcc62609dc300bd0a79d033852374",
+      "sizeBytes": 1930
+    }
+  ],
+  "entryCount": 8,
+  "fileName": "KingmakerGunslinger-0.0.22.1-attack-hook-repair-smoke-test.zip",
+  "modDllSha256": "36f46686856d2f0b04bf91ddd9665b47d9c6676de96af428bd0d697ec32fa463",
+  "modVersion": "0.0.22.1",
+  "redistributedPrivateDependencies": false,
+  "runtimeQualificationRequired": [
+    "quicksave preserves loaded token with zero reconciliation faults",
+    "complete save/desktop restart preserves loaded state",
+    "one loaded shot consumes exactly one round and re-enables reload",
+    "one empty shot is forced to miss without inventory-ammunition consumption",
+    "loaded Broken firearm fires once and remains Broken",
+    "Wrecked firearm is forced to miss",
+    "native Heavy Crossbow remains outside firearm enforcement",
+    "no bootstrap, Harmony, firearm-state, reconciliation, or attack-enforcement faults"
+  ],
+  "schemaVersion": 1,
+  "sha256": "83423ad78a5e62dd4e329c07c1f5ddb20b9bd49567690e5bab6f9495e7263541",
+  "sizeBytes": 136303,
+  "sprint23Status": "blocked-pending-all-runtime-criteria",
+  "targetGame": "Pathfinder: Kingmaker 2.1.7b",
+  "unityModManager": "0.32.4"
+}
diff --git a/evidence/sprint22-repair/runtime-result-assessment.md b/evidence/sprint22-repair/runtime-result-assessment.md
new file mode 100644
index 0000000000000000000000000000000000000000..aa8c459d9c379a81a67c324bcfaa5a2710908512
--- /dev/null
+++ b/evidence/sprint22-repair/runtime-result-assessment.md
@@ -0,0 +1,55 @@
+# Version 0.0.22 runtime-result assessment
+
+## Decision
+
+**Sprint 23 entry: BLOCKED. Remain on the Sprint 22 repair branch.**
+
+Version 0.0.22 fixed the original quicksave regression, but it did not enforce loaded-round consumption during attacks. The supplied screenshots also show a new reconciliation fault on non-weapon items. Natural-roll misfire work must not begin until the repaired attack hook and weapon-only reconciliation path pass in Kingmaker.
+
+## Evidence supplied
+
+The four original screenshots are preserved byte-for-byte under `runtime-screenshots/`.
+
+| Screenshot | SHA-256 | Relevant observation |
+|---|---|---|
+| `01-empty-ready-to-reload.png` | `ebcb4ba75ec3c44ca53a86ecbb6fbec10464444f3af610b7415932b5376d7844` | Empty/Normal Test Musket is recognized and reload readiness is available. |
+| `02-loaded-after-quicksave.png` | `ef23645bf52e3b272226c197a49a0f2f21acbe591ba5690e876b513b42c562d6` | Reload completed; the item reports `rounds=1`; the token was observed and preserved with zero faults at that point. |
+| `03-native-heavy-crossbow-pipeline-attack.png` | `8e63e998216606b8005ac7ba13032d4cc1a06adea01e69b4b899326613aea529` | Kingmaker completed an ordinary Heavy-Crossbow-presented weapon attack through its native combat pipeline. |
+| `04-post-attack-loaded-state-and-shield-faults.png` | `b3e9fbd6816dfdebd6001fdf11ca6d7a814e2145fc266f472dc3f85773d8c828` | After the attack the Test Musket still reports `rounds=1`; reload remains unavailable; attack enforcement remains `observed=0; fired=0`; reconciliation reports ten faults, last on `ItemEntityShield`. |
+
+## Sprint 23 gate assessment
+
+| Blocking criterion | Result | Evidence |
+|---|---|---|
+| Loaded Test Musket remains loaded immediately after quicksave | **Pass** | The loaded token remained present and reload stayed unavailable. |
+| Reconciliation has no unresolved conflict or fault | **Fail** | `faults=10`; last fault is a `MissingMemberException` while inspecting `Kingmaker.Items.ItemEntityShield`. |
+| Loaded state survives save, complete process exit, restart, and reload | Not established by this result | The supplied result confirms saving/quicksave no longer unloads in-process, but does not establish this exact build's full process-restart path. |
+| Loaded Test Musket attacks through the native weapon pipeline | **Pass** | The combat log shows the ordinary Heavy Crossbow presentation completing an attack and damage. |
+| The attack consumes exactly one loaded round from the firing item | **Fail** | The exact item remains at `rounds=1` after the attack. |
+| Hit or miss does not change one-round consumption | Not testable after the failure | The observed hit consumed zero rounds. |
+| Second attack while empty is forced to miss | **Fail / unreachable** | The first attack never made the weapon empty. |
+| Empty-fire attempt leaves inventory ammunition unchanged | Not established | No valid empty-fire enforcement occurred. |
+| Loaded Broken firearm discharges once and remains Broken | Not tested | Blocked by the first loaded-shot failure. |
+| Wrecked Test Musket is forced to miss | Not tested | Blocked by the attack-hook failure. |
+| Duplicate callbacks cannot consume twice | Not runtime-qualified | The attack callback was never observed. |
+| Native Heavy Crossbow remains unaffected | Not tested as a negative control | The screenshot shows the Test Musket's borrowed presentation, not a confirmed native Heavy Crossbow item. |
+| No bootstrap, token, attack-enforcement, or Harmony fault | **Fail** | Reconciliation faults are explicit; attack enforcement was never attached to a live roll. |
+
+## Exact-reference diagnosis
+
+Inspection of the supplied private `Assembly-CSharp.dll` established two independent contract errors in 0.0.22:
+
+1. `RuleAttackRoll.OnTrigger`, `RuleAttackWithWeapon.OnTrigger`, and `RuleCalculateAC.OnTrigger` each have the exact installed signature `void OnTrigger(Kingmaker.RuleSystem.RulebookEventContext)`. The 0.0.22 resolver required zero parameters, selected no target, and caused Harmony to skip the attack-enforcement, touch-AC, and combat-trace patches.
+2. `ItemEntity.ApplyEnchantments()` is correctly a zero-argument base method, but it runs for every `ItemEntity` subtype. The 0.0.22 prefix attempted firearm-token reflection on non-weapons, including shields, which explains the observed `ItemEntityShield` faults.
+
+Machine-readable method metadata and source hashes are in `exact-runtime-contracts.json`.
+
+## Bounded repair
+
+Version 0.0.22.1 makes only the evidence-driven Sprint 22 corrections:
+
+- bind exactly one non-static, non-generic, `void OnTrigger(RulebookEventContext)` declared on each intended rule-event type;
+- let only `ItemEntityWeapon` instances enter firearm state-token inspection around `ItemEntity.ApplyEnchantments()`; and
+- retain the existing fail-closed discharge, exact firearm marker, per-item token state, inventory boundary, and native Heavy Crossbow isolation logic unchanged.
+
+No natural-roll reading, forced-roll diagnostic, misfire detection, condition transition, explosion, or other Sprint 23 behavior is present.
diff --git a/evidence/sprint22-repair/tests/compile.stderr.txt b/evidence/sprint22-repair/tests/compile.stderr.txt
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/evidence/sprint22-repair/tests/compile.stdout.txt b/evidence/sprint22-repair/tests/compile.stdout.txt
new file mode 100644
index 0000000000000000000000000000000000000000..82c2290958f63596a09704fed372b1557bc9bb48
--- /dev/null
+++ b/evidence/sprint22-repair/tests/compile.stdout.txt
@@ -0,0 +1,3 @@
+Microsoft (R) Visual C# Compiler version 3.11.0-4.21403.6 (ae1fff34)
+Copyright (C) Microsoft Corporation. All rights reserved.
+
diff --git a/evidence/sprint22-repair/tests/executed-test-evidence.json b/evidence/sprint22-repair/tests/executed-test-evidence.json
new file mode 100644
index 0000000000000000000000000000000000000000..9d2207067a66ee31630c072a0cf4f03ee8a9879a
--- /dev/null
+++ b/evidence/sprint22-repair/tests/executed-test-evidence.json
@@ -0,0 +1,20 @@
+{
+  "compileExitCode": 0,
+  "compilerSha256": "f1b8f79f1761971b82a6d8a8f3fe82c45c6000b33a0a707fca532daebfca0740",
+  "declaredTests": 455,
+  "failures": 0,
+  "languageVersion": "7.3",
+  "referenceAssemblies": {
+    "System.Core.dll": "d2570c87ba2f6b7e9d577cf2f9c864bd523a34dcb13165122b2dc0eef8cf0278",
+    "System.dll": "e31e3988eaff4fdc9cb28ca6ba0980f7204c5be0cb79e2dc4e56682833fd55da",
+    "mscorlib.dll": "d33b4c6f04319d41348244f2506bb787abe7afa0e2ee464693082817d4124de1"
+  },
+  "repeatedOutputIdentical": true,
+  "runs": 3,
+  "schemaVersion": 1,
+  "sourceSetSha256": "5030a858d2a39199062733ee75655cafebf2e1436fb4481fdeb3024385d67207",
+  "targetFrameworkReferenceSurface": ".NET Framework 4.7",
+  "testExecutableSha256": "c613af5456939c225952014acb5b3927ead3f199628f731eb8d360baf48ec042",
+  "testOutputSha256": "5e004b6f22411a12e4d4706b4b7c21822e43ffbfb3a47b1d57310429339d9148",
+  "warningsAsErrors": true
+}
diff --git a/evidence/sprint22-repair/tests/run1.stderr.txt b/evidence/sprint22-repair/tests/run1.stderr.txt
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/evidence/sprint22-repair/tests/run1.stdout.txt b/evidence/sprint22-repair/tests/run1.stdout.txt
new file mode 100644
index 0000000000000000000000000000000000000000..28233790164001bc4d86177b737eb3902c5bb7fe
--- /dev/null
+++ b/evidence/sprint22-repair/tests/run1.stdout.txt
@@ -0,0 +1,457 @@
+Kingmaker Gunslinger domain, firearm-state, and combat-rule tests
+PASS valid.early-musket
+PASS factory.early-musket-fresh-instances
+PASS factory.early-musket-canonical-equality
+PASS valid.early-pistol
+PASS valid.early-blunderbuss
+PASS valid.advanced-pistol
+PASS valid.advanced-rifle
+PASS valid.advanced-revolver
+PASS equality.firearm-definition
+PASS equality.reload-profile
+PASS equality.different-definition
+PASS format.deterministic
+PASS invalid.unknown-era
+PASS invalid.undefined-era
+PASS invalid.unknown-kind
+PASS invalid.undefined-kind
+PASS invalid.capacity-zero
+PASS invalid.capacity-too-large
+PASS invalid.range-too-small
+PASS invalid.range-not-five-foot-step
+PASS invalid.range-too-large
+PASS invalid.misfire-zero
+PASS invalid.misfire-too-large
+PASS invalid.null-reload
+PASS invalid.reload-unknown-action
+PASS invalid.reload-undefined-action
+PASS invalid.reload-zero-rounds
+PASS invalid.reload-too-many-rounds
+PASS invalid.reload-exceeds-capacity
+PASS invalid.scatter-non-blunderbuss
+PASS invalid.blunderbuss-without-scatter
+PASS invalid.advanced-musket
+PASS invalid.advanced-blunderbuss
+PASS invalid.early-rifle
+PASS invalid.early-revolver
+PASS invalid.revolver-capacity-one
+PASS invalid.early-pistol-wrong-reload
+PASS invalid.early-musket-wrong-reload
+PASS invalid.early-blunderbuss-wrong-reload
+PASS invalid.advanced-wrong-reload
+PASS ammo.snapshot.valid
+PASS ammo.snapshot.capture
+PASS ammo.snapshot.value-equality
+PASS ammo.snapshot.format
+PASS ammo.snapshot.negative-powder
+PASS ammo.snapshot.negative-ball
+PASS ammo.snapshot.unknown-component
+PASS ammo.snapshot.null-inventory
+PASS ammo.snapshot.negative-store-count
+PASS ammo.transaction.success
+PASS ammo.transaction.multiple-success
+PASS ammo.transaction.missing-powder
+PASS ammo.transaction.missing-ball
+PASS ammo.transaction.empty
+PASS ammo.transaction.null-inventory
+PASS ammo.transaction.first-remove-failure-rolls-back
+PASS ammo.transaction.second-remove-failure-rolls-back
+PASS ammo.transaction.after-mutation-failure-rolls-back
+PASS ammo.transaction.verification-failure-rolls-back
+PASS ammo.transaction.rollback-failure-surfaced
+PASS ammo.result.success-validation
+PASS ammo.result.insufficient-validation
+PASS ammo.result.unknown-status
+PASS ammo.result.null-snapshots
+PASS ammo.result.format
+PASS reload.transaction.success
+PASS reload.transaction.already-loaded
+PASS reload.transaction.broken
+PASS reload.transaction.wrecked
+PASS reload.transaction.missing-powder
+PASS reload.transaction.missing-ball
+PASS reload.transaction.null-state-store
+PASS reload.transaction.null-inventory
+PASS reload.transaction.null-rules
+PASS reload.transaction.null-ammunition
+PASS reload.transaction.null-state
+PASS reload.transaction.incompatible-ammunition
+PASS reload.transaction.state-write-failure-restores-inventory
+PASS reload.transaction.post-state-mutation-failure-restores-both
+PASS reload.transaction.state-rollback-failure-surfaced
+PASS reload.transaction.inventory-rollback-failure-surfaced
+PASS reload.result.success-validation
+PASS reload.result.rejected-validation
+PASS reload.result.unknown-status
+PASS reload.result.null-values
+PASS reload.result.format
+PASS discharge.loaded-normal
+PASS discharge.loaded-broken
+PASS discharge.multiple-rounds
+PASS discharge.empty-normal
+PASS discharge.empty-broken
+PASS discharge.wrecked
+PASS discharge.null-state
+PASS discharge.result-fired-validation
+PASS discharge.result-empty-validation
+PASS discharge.result-wrecked-validation
+PASS discharge.result-unknown-status
+PASS discharge.result-null-state
+PASS discharge.result-format
+PASS event-gate.first-and-duplicate
+PASS event-gate.reference-identity
+PASS event-gate.null
+PASS event-gate.value-type
+PASS patch-target.exact-contract
+PASS patch-target.null-method
+PASS patch-target.zero-argument
+PASS patch-target.wrong-context
+PASS patch-target.multiple-arguments
+PASS patch-target.static
+PASS patch-target.generic
+PASS patch-target.non-void
+PASS patch-target.null-context
+PASS token-reconcile.none
+PASS token-reconcile.preserved
+PASS token-reconcile.restore
+PASS token-reconcile.appeared-conflict
+PASS token-reconcile.changed-conflict
+PASS token-reconcile.multiple-before-conflict
+PASS token-reconcile.multiple-after-conflict
+PASS token-reconcile.null-before
+PASS token-reconcile.blank-token
+PASS token-reconcile.format-defensive-copy
+PASS state.ammunition-id.valid
+PASS state.ammunition-id.value-equality
+PASS state.ammunition-id.uppercase-rejected
+PASS state.ammunition-id.null
+PASS state.ammunition-id.empty
+PASS state.ammunition-id.leading-separator
+PASS state.ammunition-id.whitespace
+PASS state.ammunition-id.too-long
+PASS state.rules.valid
+PASS state.rules.sorted-copy
+PASS state.rules.capacity-zero
+PASS state.rules.capacity-too-large
+PASS state.rules.null-collection
+PASS state.rules.empty-collection
+PASS state.rules.null-entry
+PASS state.rules.duplicate
+PASS state.empty.canonical
+PASS state.value-equality
+PASS state.format.deterministic
+PASS state.invalid-schema
+PASS state.invalid-negative-rounds
+PASS state.invalid-loaded-without-ammo
+PASS state.invalid-empty-with-ammo
+PASS state.invalid-unknown-condition
+PASS state.invalid-wrecked-loaded
+PASS state.load.empty
+PASS state.load.partial-to-capacity
+PASS state.load.broken
+PASS state.load.over-capacity
+PASS state.load.incompatible
+PASS state.load.mixed
+PASS state.load.zero-rounds
+PASS state.load.null-state
+PASS state.load.null-rules
+PASS state.load.invalid-existing-state
+PASS state.fire.consumes-one
+PASS state.fire.final-clears-ammo
+PASS state.fire.broken
+PASS state.fire.empty-rejected
+PASS state.fire.wrecked-rejected
+PASS state.misfire.normal-to-broken
+PASS state.misfire.broken-to-wrecked
+PASS state.misfire.wrecked-rejected
+PASS state.repair.broken-to-normal
+PASS state.repair.normal-rejected
+PASS state.repair.wrecked-rejected
+PASS state.wreck.normal-clears-load
+PASS state.wreck.idempotent
+PASS state.codec.empty-roundtrip
+PASS state.codec.loaded-broken-roundtrip
+PASS state.codec.canonical-dto
+PASS state.codec.wrong-schema
+PASS state.codec.unknown-condition
+PASS state.codec.case-sensitive-condition
+PASS state.codec.over-capacity
+PASS state.codec.incompatible-ammo
+PASS state.codec.loaded-missing-ammo
+PASS state.codec.empty-with-ammo
+PASS state.codec.wrecked-loaded
+PASS state.codec.null-data
+PASS state.codec.null-rules
+PASS token.definition.valid
+PASS token.definition.value-equality
+PASS token.definition.null-state
+PASS token.definition.empty-id
+PASS token.definition.uppercase-id
+PASS token.catalog.count
+PASS token.catalog.order
+PASS token.catalog.defensive-copy
+PASS token.catalog.absence-default
+PASS token.catalog.loaded-normal
+PASS token.catalog.broken-empty
+PASS token.catalog.broken-loaded
+PASS token.catalog.wrecked
+PASS token.catalog.unknown
+PASS token.catalog.duplicate-payload
+PASS token.catalog.null-payload
+PASS token.catalog.null-entry
+PASS token.catalog.encode-null
+PASS token.catalog.unsupported-state
+PASS token.catalog.duplicate-id
+PASS token.catalog.duplicate-state
+PASS token.catalog.default-token
+PASS token.catalog.require-unknown
+PASS token.catalog.contains
+PASS repository.reference-comparer.same
+PASS repository.reference-comparer.distinct-value-equal
+PASS repository.unseen-empty
+PASS repository.same-reference-stable-entry
+PASS repository.distinct-equal-separate-entries
+PASS repository.set-isolated
+PASS repository.set-increments-revision
+PASS repository.noop-set-no-revision
+PASS repository.transition-increments-revision
+PASS repository.transition-rejected-preserves
+PASS repository.transition-null-preserves
+PASS repository.try-get-missing-no-create
+PASS repository.snapshot-immutable
+PASS repository.remove-existing
+PASS repository.remove-missing
+PASS repository.remove-readd-new-entry
+PASS repository.null-key
+PASS repository.value-key
+PASS repository.null-state
+PASS repository.null-transition
+PASS repository.counters
+PASS item-state.exact-initializes-empty
+PASS item-state.native-rejected-no-entry
+PASS item-state.ambiguous-rejected-no-entry
+PASS item-state.blueprint-rejected-no-entry
+PASS item-state.canonical-key
+PASS item-state.two-firearms-independent
+PASS item-state.set-metadata
+PASS item-state.transition
+PASS item-state.get-existing-missing-no-create
+PASS item-state.forget
+PASS item-state.format-deterministic
+PASS token-repository.unseen-empty
+PASS token-repository.set-loaded
+PASS token-repository.set-broken
+PASS token-repository.reset-clears
+PASS token-repository.two-independent
+PASS token-repository.value-equal-independent
+PASS token-repository.revision
+PASS token-repository.noop
+PASS token-repository.transition
+PASS token-repository.transition-rejected
+PASS token-repository.transition-null
+PASS token-repository.try-get-empty
+PASS token-repository.try-get-token
+PASS token-repository.unknown-token
+PASS token-repository.duplicate-token
+PASS token-repository.replace-failure
+PASS token-repository.concurrent-change
+PASS token-repository.corrupt-write
+PASS token-repository.unsupported-state
+PASS token-repository.remove-token
+PASS token-repository.remove-missing
+PASS token-repository.remove-metadata
+PASS token-repository.snapshot-immutable
+PASS token-repository.counters
+PASS token-repository.null-key
+PASS token-repository.value-key
+PASS token-repository.null-state
+PASS token-repository.null-transition
+PASS vault-data.clone-null
+PASS vault-data.clone-independent
+PASS vault-data.equal
+PASS vault-data.not-equal
+PASS vault-data.describe
+PASS vault-repository.unseen-empty
+PASS vault-repository.set-loaded
+PASS vault-repository.reconstruct
+PASS vault-repository.reset-clears
+PASS vault-repository.two-independent
+PASS vault-repository.value-equal-independent
+PASS vault-repository.revision
+PASS vault-repository.noop
+PASS vault-repository.transition
+PASS vault-repository.transition-rejected
+PASS vault-repository.transition-null
+PASS vault-repository.try-get-empty
+PASS vault-repository.corrupt-read
+PASS vault-repository.replace-failure
+PASS vault-repository.concurrent-change
+PASS vault-repository.corrupt-write
+PASS vault-repository.remove-record
+PASS vault-repository.remove-missing
+PASS vault-repository.remove-metadata
+PASS vault-repository.snapshot-immutable
+PASS vault-repository.counters
+PASS vault-repository.null-key
+PASS vault-repository.value-key
+PASS vault-repository.null-state
+PASS vault-repository.null-transition
+PASS migration.no-token
+PASS migration.loaded-normal
+PASS migration.broken-empty
+PASS migration.broken-loaded
+PASS migration.wrecked
+PASS migration.same-state-cleanup
+PASS migration.conflict
+PASS migration.unknown-token
+PASS migration.duplicate-token
+PASS migration.vault-write-failure
+PASS migration.token-clear-failure
+PASS migration.rollback-failure
+PASS migration.set-after
+PASS migration.transition-after
+PASS migration.try-get
+PASS migration.remove
+PASS migration.two-independent
+PASS migration.value-equal-independent
+PASS migration.no-repeat
+PASS migration.snapshot-format
+PASS migration.null-key
+PASS migration.null-state
+PASS migration.null-transition
+PASS identity.valid
+PASS identity.uppercase-canonical
+PASS identity.guid-constructor
+PASS identity.value-equality
+PASS identity.inequality
+PASS identity.order
+PASS identity.empty
+PASS identity.compact
+PASS identity.braces
+PASS identity.whitespace
+PASS identity.null
+PASS identity.try-create-valid
+PASS identity.try-create-invalid
+PASS identity.operator-null
+PASS identity-vault.write-read
+PASS identity-vault.reconstruct-same-id
+PASS identity-vault.different-ids
+PASS identity-vault.value-equal-different-ids
+PASS identity-vault.value-equal-same-id
+PASS identity-vault.reset-removes
+PASS identity-vault.compare-failure
+PASS identity-vault.remove
+PASS identity-vault.remove-missing
+PASS identity-vault.provider-reject-read
+PASS identity-vault.provider-reject-write
+PASS identity-vault.provider-null
+PASS identity-vault.defensive-read
+PASS identity-vault.defensive-write
+PASS identity-vault.count
+PASS identity-repository.reconstruct
+PASS identity-repository.two-independent
+PASS identity-repository.same-id
+PASS identity-repository.noop
+PASS identity-migration.snapshot-format
+PASS identity-migration.snapshot-properties
+PASS reflection.private-field
+PASS reflection.static-property
+PASS reflection.first-non-null-member
+PASS reflection.path
+PASS reflection.enumerate
+PASS reflection.invoke-exact
+PASS reflection.invoke-optional-default
+PASS reflection.invoke-trailing-null
+PASS reflection.invoke-required-bool
+PASS reflection.invoke-incompatible
+PASS range.zero-is-first
+PASS range.within-first
+PASS range.exact-boundary
+PASS range.second
+PASS range.multiple
+PASS range.invalid-negative
+PASS range.invalid-zero-increment
+PASS range.invalid-infinite
+PASS trace.native-marker-ignored
+PASS trace.ambiguous-marker-ignored
+PASS trace.exact-firearm-starts
+PASS trace.child-joins-parent
+PASS trace.duplicate-callback
+PASS trace.unrelated-child-ignored
+PASS trace.weapon-root-completes
+PASS trace.attack-roll-root-completes
+PASS trace.reset-clears-active
+PASS trace.id-source-must-be-unique
+PASS observation.copies-fields
+PASS observation.exact-requires-one-marker
+PASS formatter.fields-sorted
+PASS formatter.sanitizes-one-line
+PASS formatter.completion-counts-duplicates
+PASS ac.native-ordinary
+PASS ac.ambiguous-ordinary
+PASS ac.missing-definition
+PASS ac.zero-distance-touch
+PASS ac.close-touch
+PASS ac.boundary-touch
+PASS ac.boundary-float-noise-touch
+PASS ac.distant-ordinary
+PASS ac.cover-preserved
+PASS ac.flat-footed-preserved
+PASS ac.equal-no-write
+PASS ac.already-applied
+PASS ac.advanced-fails-closed
+PASS ac.invalid-distance
+PASS ac.negative-distance
+PASS ac.infinite-distance
+PASS ac.overflow
+PASS ac-access.participants
+PASS ac-access.distance
+PASS ac-access.values
+PASS ac-access.private-property
+PASS ac-access.field
+PASS ac-access.ambiguous-rejected
+PASS evidence.catalog-count
+PASS evidence.catalog-severity-counts
+PASS evidence.catalog-order
+PASS evidence.catalog-unique-ids
+PASS evidence.catalog-reproduction-ids
+PASS evidence.catalog-require-known
+PASS evidence.catalog-require-unknown
+PASS evidence.observation-valid
+PASS evidence.observation-invalid-sequence
+PASS evidence.observation-unknown-step
+PASS evidence.observation-nonutc
+PASS evidence.observation-invalid-hash
+PASS evidence.observation-uppercase-hash
+PASS evidence.evaluation-empty
+PASS evidence.evaluation-critical-fail
+PASS evidence.evaluation-blocked
+PASS evidence.evaluation-single-pass-incomplete
+PASS evidence.evaluation-reproduced-go
+PASS evidence.evaluation-high-fail-warning
+PASS evidence.evaluation-latest-fail
+PASS evidence.evaluation-same-run-not-reproduced
+PASS evidence.evaluation-null-observation
+PASS evidence.evaluation-duplicate-sequence
+PASS evidence.evaluation-format
+PASS preflight.probe-negative-bootstrap-count
+PASS preflight.probe-negative-blueprint-count
+PASS preflight.probe-invalid-expected-count
+PASS preflight.probe-negative-identity-count
+PASS preflight.check-only-i01-i02
+PASS preflight.report-order-required
+PASS preflight.report-all-passed
+PASS preflight.report-require
+PASS preflight.evaluate-pass-guid
+PASS preflight.evaluate-pass-string
+PASS preflight.evaluate-bootstrap-blocked
+PASS preflight.evaluate-bootstrap-not-initialized
+PASS preflight.evaluate-bootstrap-duplicate-init
+PASS preflight.evaluate-bootstrap-count-mismatch
+PASS preflight.evaluate-identity-blocked
+PASS preflight.evaluate-identity-missing
+PASS preflight.evaluate-identity-duplicate
+PASS preflight.evaluate-identity-unreadable
+PASS preflight.evaluate-identity-unsupported-type
+PASS preflight.format
+Completed 455 tests; failures=0.
diff --git a/evidence/sprint22-repair/tests/run2.stderr.txt b/evidence/sprint22-repair/tests/run2.stderr.txt
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/evidence/sprint22-repair/tests/run2.stdout.txt b/evidence/sprint22-repair/tests/run2.stdout.txt
new file mode 100644
index 0000000000000000000000000000000000000000..28233790164001bc4d86177b737eb3902c5bb7fe
--- /dev/null
+++ b/evidence/sprint22-repair/tests/run2.stdout.txt
@@ -0,0 +1,457 @@
+Kingmaker Gunslinger domain, firearm-state, and combat-rule tests
+PASS valid.early-musket
+PASS factory.early-musket-fresh-instances
+PASS factory.early-musket-canonical-equality
+PASS valid.early-pistol
+PASS valid.early-blunderbuss
+PASS valid.advanced-pistol
+PASS valid.advanced-rifle
+PASS valid.advanced-revolver
+PASS equality.firearm-definition
+PASS equality.reload-profile
+PASS equality.different-definition
+PASS format.deterministic
+PASS invalid.unknown-era
+PASS invalid.undefined-era
+PASS invalid.unknown-kind
+PASS invalid.undefined-kind
+PASS invalid.capacity-zero
+PASS invalid.capacity-too-large
+PASS invalid.range-too-small
+PASS invalid.range-not-five-foot-step
+PASS invalid.range-too-large
+PASS invalid.misfire-zero
+PASS invalid.misfire-too-large
+PASS invalid.null-reload
+PASS invalid.reload-unknown-action
+PASS invalid.reload-undefined-action
+PASS invalid.reload-zero-rounds
+PASS invalid.reload-too-many-rounds
+PASS invalid.reload-exceeds-capacity
+PASS invalid.scatter-non-blunderbuss
+PASS invalid.blunderbuss-without-scatter
+PASS invalid.advanced-musket
+PASS invalid.advanced-blunderbuss
+PASS invalid.early-rifle
+PASS invalid.early-revolver
+PASS invalid.revolver-capacity-one
+PASS invalid.early-pistol-wrong-reload
+PASS invalid.early-musket-wrong-reload
+PASS invalid.early-blunderbuss-wrong-reload
+PASS invalid.advanced-wrong-reload
+PASS ammo.snapshot.valid
+PASS ammo.snapshot.capture
+PASS ammo.snapshot.value-equality
+PASS ammo.snapshot.format
+PASS ammo.snapshot.negative-powder
+PASS ammo.snapshot.negative-ball
+PASS ammo.snapshot.unknown-component
+PASS ammo.snapshot.null-inventory
+PASS ammo.snapshot.negative-store-count
+PASS ammo.transaction.success
+PASS ammo.transaction.multiple-success
+PASS ammo.transaction.missing-powder
+PASS ammo.transaction.missing-ball
+PASS ammo.transaction.empty
+PASS ammo.transaction.null-inventory
+PASS ammo.transaction.first-remove-failure-rolls-back
+PASS ammo.transaction.second-remove-failure-rolls-back
+PASS ammo.transaction.after-mutation-failure-rolls-back
+PASS ammo.transaction.verification-failure-rolls-back
+PASS ammo.transaction.rollback-failure-surfaced
+PASS ammo.result.success-validation
+PASS ammo.result.insufficient-validation
+PASS ammo.result.unknown-status
+PASS ammo.result.null-snapshots
+PASS ammo.result.format
+PASS reload.transaction.success
+PASS reload.transaction.already-loaded
+PASS reload.transaction.broken
+PASS reload.transaction.wrecked
+PASS reload.transaction.missing-powder
+PASS reload.transaction.missing-ball
+PASS reload.transaction.null-state-store
+PASS reload.transaction.null-inventory
+PASS reload.transaction.null-rules
+PASS reload.transaction.null-ammunition
+PASS reload.transaction.null-state
+PASS reload.transaction.incompatible-ammunition
+PASS reload.transaction.state-write-failure-restores-inventory
+PASS reload.transaction.post-state-mutation-failure-restores-both
+PASS reload.transaction.state-rollback-failure-surfaced
+PASS reload.transaction.inventory-rollback-failure-surfaced
+PASS reload.result.success-validation
+PASS reload.result.rejected-validation
+PASS reload.result.unknown-status
+PASS reload.result.null-values
+PASS reload.result.format
+PASS discharge.loaded-normal
+PASS discharge.loaded-broken
+PASS discharge.multiple-rounds
+PASS discharge.empty-normal
+PASS discharge.empty-broken
+PASS discharge.wrecked
+PASS discharge.null-state
+PASS discharge.result-fired-validation
+PASS discharge.result-empty-validation
+PASS discharge.result-wrecked-validation
+PASS discharge.result-unknown-status
+PASS discharge.result-null-state
+PASS discharge.result-format
+PASS event-gate.first-and-duplicate
+PASS event-gate.reference-identity
+PASS event-gate.null
+PASS event-gate.value-type
+PASS patch-target.exact-contract
+PASS patch-target.null-method
+PASS patch-target.zero-argument
+PASS patch-target.wrong-context
+PASS patch-target.multiple-arguments
+PASS patch-target.static
+PASS patch-target.generic
+PASS patch-target.non-void
+PASS patch-target.null-context
+PASS token-reconcile.none
+PASS token-reconcile.preserved
+PASS token-reconcile.restore
+PASS token-reconcile.appeared-conflict
+PASS token-reconcile.changed-conflict
+PASS token-reconcile.multiple-before-conflict
+PASS token-reconcile.multiple-after-conflict
+PASS token-reconcile.null-before
+PASS token-reconcile.blank-token
+PASS token-reconcile.format-defensive-copy
+PASS state.ammunition-id.valid
+PASS state.ammunition-id.value-equality
+PASS state.ammunition-id.uppercase-rejected
+PASS state.ammunition-id.null
+PASS state.ammunition-id.empty
+PASS state.ammunition-id.leading-separator
+PASS state.ammunition-id.whitespace
+PASS state.ammunition-id.too-long
+PASS state.rules.valid
+PASS state.rules.sorted-copy
+PASS state.rules.capacity-zero
+PASS state.rules.capacity-too-large
+PASS state.rules.null-collection
+PASS state.rules.empty-collection
+PASS state.rules.null-entry
+PASS state.rules.duplicate
+PASS state.empty.canonical
+PASS state.value-equality
+PASS state.format.deterministic
+PASS state.invalid-schema
+PASS state.invalid-negative-rounds
+PASS state.invalid-loaded-without-ammo
+PASS state.invalid-empty-with-ammo
+PASS state.invalid-unknown-condition
+PASS state.invalid-wrecked-loaded
+PASS state.load.empty
+PASS state.load.partial-to-capacity
+PASS state.load.broken
+PASS state.load.over-capacity
+PASS state.load.incompatible
+PASS state.load.mixed
+PASS state.load.zero-rounds
+PASS state.load.null-state
+PASS state.load.null-rules
+PASS state.load.invalid-existing-state
+PASS state.fire.consumes-one
+PASS state.fire.final-clears-ammo
+PASS state.fire.broken
+PASS state.fire.empty-rejected
+PASS state.fire.wrecked-rejected
+PASS state.misfire.normal-to-broken
+PASS state.misfire.broken-to-wrecked
+PASS state.misfire.wrecked-rejected
+PASS state.repair.broken-to-normal
+PASS state.repair.normal-rejected
+PASS state.repair.wrecked-rejected
+PASS state.wreck.normal-clears-load
+PASS state.wreck.idempotent
+PASS state.codec.empty-roundtrip
+PASS state.codec.loaded-broken-roundtrip
+PASS state.codec.canonical-dto
+PASS state.codec.wrong-schema
+PASS state.codec.unknown-condition
+PASS state.codec.case-sensitive-condition
+PASS state.codec.over-capacity
+PASS state.codec.incompatible-ammo
+PASS state.codec.loaded-missing-ammo
+PASS state.codec.empty-with-ammo
+PASS state.codec.wrecked-loaded
+PASS state.codec.null-data
+PASS state.codec.null-rules
+PASS token.definition.valid
+PASS token.definition.value-equality
+PASS token.definition.null-state
+PASS token.definition.empty-id
+PASS token.definition.uppercase-id
+PASS token.catalog.count
+PASS token.catalog.order
+PASS token.catalog.defensive-copy
+PASS token.catalog.absence-default
+PASS token.catalog.loaded-normal
+PASS token.catalog.broken-empty
+PASS token.catalog.broken-loaded
+PASS token.catalog.wrecked
+PASS token.catalog.unknown
+PASS token.catalog.duplicate-payload
+PASS token.catalog.null-payload
+PASS token.catalog.null-entry
+PASS token.catalog.encode-null
+PASS token.catalog.unsupported-state
+PASS token.catalog.duplicate-id
+PASS token.catalog.duplicate-state
+PASS token.catalog.default-token
+PASS token.catalog.require-unknown
+PASS token.catalog.contains
+PASS repository.reference-comparer.same
+PASS repository.reference-comparer.distinct-value-equal
+PASS repository.unseen-empty
+PASS repository.same-reference-stable-entry
+PASS repository.distinct-equal-separate-entries
+PASS repository.set-isolated
+PASS repository.set-increments-revision
+PASS repository.noop-set-no-revision
+PASS repository.transition-increments-revision
+PASS repository.transition-rejected-preserves
+PASS repository.transition-null-preserves
+PASS repository.try-get-missing-no-create
+PASS repository.snapshot-immutable
+PASS repository.remove-existing
+PASS repository.remove-missing
+PASS repository.remove-readd-new-entry
+PASS repository.null-key
+PASS repository.value-key
+PASS repository.null-state
+PASS repository.null-transition
+PASS repository.counters
+PASS item-state.exact-initializes-empty
+PASS item-state.native-rejected-no-entry
+PASS item-state.ambiguous-rejected-no-entry
+PASS item-state.blueprint-rejected-no-entry
+PASS item-state.canonical-key
+PASS item-state.two-firearms-independent
+PASS item-state.set-metadata
+PASS item-state.transition
+PASS item-state.get-existing-missing-no-create
+PASS item-state.forget
+PASS item-state.format-deterministic
+PASS token-repository.unseen-empty
+PASS token-repository.set-loaded
+PASS token-repository.set-broken
+PASS token-repository.reset-clears
+PASS token-repository.two-independent
+PASS token-repository.value-equal-independent
+PASS token-repository.revision
+PASS token-repository.noop
+PASS token-repository.transition
+PASS token-repository.transition-rejected
+PASS token-repository.transition-null
+PASS token-repository.try-get-empty
+PASS token-repository.try-get-token
+PASS token-repository.unknown-token
+PASS token-repository.duplicate-token
+PASS token-repository.replace-failure
+PASS token-repository.concurrent-change
+PASS token-repository.corrupt-write
+PASS token-repository.unsupported-state
+PASS token-repository.remove-token
+PASS token-repository.remove-missing
+PASS token-repository.remove-metadata
+PASS token-repository.snapshot-immutable
+PASS token-repository.counters
+PASS token-repository.null-key
+PASS token-repository.value-key
+PASS token-repository.null-state
+PASS token-repository.null-transition
+PASS vault-data.clone-null
+PASS vault-data.clone-independent
+PASS vault-data.equal
+PASS vault-data.not-equal
+PASS vault-data.describe
+PASS vault-repository.unseen-empty
+PASS vault-repository.set-loaded
+PASS vault-repository.reconstruct
+PASS vault-repository.reset-clears
+PASS vault-repository.two-independent
+PASS vault-repository.value-equal-independent
+PASS vault-repository.revision
+PASS vault-repository.noop
+PASS vault-repository.transition
+PASS vault-repository.transition-rejected
+PASS vault-repository.transition-null
+PASS vault-repository.try-get-empty
+PASS vault-repository.corrupt-read
+PASS vault-repository.replace-failure
+PASS vault-repository.concurrent-change
+PASS vault-repository.corrupt-write
+PASS vault-repository.remove-record
+PASS vault-repository.remove-missing
+PASS vault-repository.remove-metadata
+PASS vault-repository.snapshot-immutable
+PASS vault-repository.counters
+PASS vault-repository.null-key
+PASS vault-repository.value-key
+PASS vault-repository.null-state
+PASS vault-repository.null-transition
+PASS migration.no-token
+PASS migration.loaded-normal
+PASS migration.broken-empty
+PASS migration.broken-loaded
+PASS migration.wrecked
+PASS migration.same-state-cleanup
+PASS migration.conflict
+PASS migration.unknown-token
+PASS migration.duplicate-token
+PASS migration.vault-write-failure
+PASS migration.token-clear-failure
+PASS migration.rollback-failure
+PASS migration.set-after
+PASS migration.transition-after
+PASS migration.try-get
+PASS migration.remove
+PASS migration.two-independent
+PASS migration.value-equal-independent
+PASS migration.no-repeat
+PASS migration.snapshot-format
+PASS migration.null-key
+PASS migration.null-state
+PASS migration.null-transition
+PASS identity.valid
+PASS identity.uppercase-canonical
+PASS identity.guid-constructor
+PASS identity.value-equality
+PASS identity.inequality
+PASS identity.order
+PASS identity.empty
+PASS identity.compact
+PASS identity.braces
+PASS identity.whitespace
+PASS identity.null
+PASS identity.try-create-valid
+PASS identity.try-create-invalid
+PASS identity.operator-null
+PASS identity-vault.write-read
+PASS identity-vault.reconstruct-same-id
+PASS identity-vault.different-ids
+PASS identity-vault.value-equal-different-ids
+PASS identity-vault.value-equal-same-id
+PASS identity-vault.reset-removes
+PASS identity-vault.compare-failure
+PASS identity-vault.remove
+PASS identity-vault.remove-missing
+PASS identity-vault.provider-reject-read
+PASS identity-vault.provider-reject-write
+PASS identity-vault.provider-null
+PASS identity-vault.defensive-read
+PASS identity-vault.defensive-write
+PASS identity-vault.count
+PASS identity-repository.reconstruct
+PASS identity-repository.two-independent
+PASS identity-repository.same-id
+PASS identity-repository.noop
+PASS identity-migration.snapshot-format
+PASS identity-migration.snapshot-properties
+PASS reflection.private-field
+PASS reflection.static-property
+PASS reflection.first-non-null-member
+PASS reflection.path
+PASS reflection.enumerate
+PASS reflection.invoke-exact
+PASS reflection.invoke-optional-default
+PASS reflection.invoke-trailing-null
+PASS reflection.invoke-required-bool
+PASS reflection.invoke-incompatible
+PASS range.zero-is-first
+PASS range.within-first
+PASS range.exact-boundary
+PASS range.second
+PASS range.multiple
+PASS range.invalid-negative
+PASS range.invalid-zero-increment
+PASS range.invalid-infinite
+PASS trace.native-marker-ignored
+PASS trace.ambiguous-marker-ignored
+PASS trace.exact-firearm-starts
+PASS trace.child-joins-parent
+PASS trace.duplicate-callback
+PASS trace.unrelated-child-ignored
+PASS trace.weapon-root-completes
+PASS trace.attack-roll-root-completes
+PASS trace.reset-clears-active
+PASS trace.id-source-must-be-unique
+PASS observation.copies-fields
+PASS observation.exact-requires-one-marker
+PASS formatter.fields-sorted
+PASS formatter.sanitizes-one-line
+PASS formatter.completion-counts-duplicates
+PASS ac.native-ordinary
+PASS ac.ambiguous-ordinary
+PASS ac.missing-definition
+PASS ac.zero-distance-touch
+PASS ac.close-touch
+PASS ac.boundary-touch
+PASS ac.boundary-float-noise-touch
+PASS ac.distant-ordinary
+PASS ac.cover-preserved
+PASS ac.flat-footed-preserved
+PASS ac.equal-no-write
+PASS ac.already-applied
+PASS ac.advanced-fails-closed
+PASS ac.invalid-distance
+PASS ac.negative-distance
+PASS ac.infinite-distance
+PASS ac.overflow
+PASS ac-access.participants
+PASS ac-access.distance
+PASS ac-access.values
+PASS ac-access.private-property
+PASS ac-access.field
+PASS ac-access.ambiguous-rejected
+PASS evidence.catalog-count
+PASS evidence.catalog-severity-counts
+PASS evidence.catalog-order
+PASS evidence.catalog-unique-ids
+PASS evidence.catalog-reproduction-ids
+PASS evidence.catalog-require-known
+PASS evidence.catalog-require-unknown
+PASS evidence.observation-valid
+PASS evidence.observation-invalid-sequence
+PASS evidence.observation-unknown-step
+PASS evidence.observation-nonutc
+PASS evidence.observation-invalid-hash
+PASS evidence.observation-uppercase-hash
+PASS evidence.evaluation-empty
+PASS evidence.evaluation-critical-fail
+PASS evidence.evaluation-blocked
+PASS evidence.evaluation-single-pass-incomplete
+PASS evidence.evaluation-reproduced-go
+PASS evidence.evaluation-high-fail-warning
+PASS evidence.evaluation-latest-fail
+PASS evidence.evaluation-same-run-not-reproduced
+PASS evidence.evaluation-null-observation
+PASS evidence.evaluation-duplicate-sequence
+PASS evidence.evaluation-format
+PASS preflight.probe-negative-bootstrap-count
+PASS preflight.probe-negative-blueprint-count
+PASS preflight.probe-invalid-expected-count
+PASS preflight.probe-negative-identity-count
+PASS preflight.check-only-i01-i02
+PASS preflight.report-order-required
+PASS preflight.report-all-passed
+PASS preflight.report-require
+PASS preflight.evaluate-pass-guid
+PASS preflight.evaluate-pass-string
+PASS preflight.evaluate-bootstrap-blocked
+PASS preflight.evaluate-bootstrap-not-initialized
+PASS preflight.evaluate-bootstrap-duplicate-init
+PASS preflight.evaluate-bootstrap-count-mismatch
+PASS preflight.evaluate-identity-blocked
+PASS preflight.evaluate-identity-missing
+PASS preflight.evaluate-identity-duplicate
+PASS preflight.evaluate-identity-unreadable
+PASS preflight.evaluate-identity-unsupported-type
+PASS preflight.format
+Completed 455 tests; failures=0.
diff --git a/evidence/sprint22-repair/tests/run3.stderr.txt b/evidence/sprint22-repair/tests/run3.stderr.txt
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/evidence/sprint22-repair/tests/run3.stdout.txt b/evidence/sprint22-repair/tests/run3.stdout.txt
new file mode 100644
index 0000000000000000000000000000000000000000..28233790164001bc4d86177b737eb3902c5bb7fe
--- /dev/null
+++ b/evidence/sprint22-repair/tests/run3.stdout.txt
@@ -0,0 +1,457 @@
+Kingmaker Gunslinger domain, firearm-state, and combat-rule tests
+PASS valid.early-musket
+PASS factory.early-musket-fresh-instances
+PASS factory.early-musket-canonical-equality
+PASS valid.early-pistol
+PASS valid.early-blunderbuss
+PASS valid.advanced-pistol
+PASS valid.advanced-rifle
+PASS valid.advanced-revolver
+PASS equality.firearm-definition
+PASS equality.reload-profile
+PASS equality.different-definition
+PASS format.deterministic
+PASS invalid.unknown-era
+PASS invalid.undefined-era
+PASS invalid.unknown-kind
+PASS invalid.undefined-kind
+PASS invalid.capacity-zero
+PASS invalid.capacity-too-large
+PASS invalid.range-too-small
+PASS invalid.range-not-five-foot-step
+PASS invalid.range-too-large
+PASS invalid.misfire-zero
+PASS invalid.misfire-too-large
+PASS invalid.null-reload
+PASS invalid.reload-unknown-action
+PASS invalid.reload-undefined-action
+PASS invalid.reload-zero-rounds
+PASS invalid.reload-too-many-rounds
+PASS invalid.reload-exceeds-capacity
+PASS invalid.scatter-non-blunderbuss
+PASS invalid.blunderbuss-without-scatter
+PASS invalid.advanced-musket
+PASS invalid.advanced-blunderbuss
+PASS invalid.early-rifle
+PASS invalid.early-revolver
+PASS invalid.revolver-capacity-one
+PASS invalid.early-pistol-wrong-reload
+PASS invalid.early-musket-wrong-reload
+PASS invalid.early-blunderbuss-wrong-reload
+PASS invalid.advanced-wrong-reload
+PASS ammo.snapshot.valid
+PASS ammo.snapshot.capture
+PASS ammo.snapshot.value-equality
+PASS ammo.snapshot.format
+PASS ammo.snapshot.negative-powder
+PASS ammo.snapshot.negative-ball
+PASS ammo.snapshot.unknown-component
+PASS ammo.snapshot.null-inventory
+PASS ammo.snapshot.negative-store-count
+PASS ammo.transaction.success
+PASS ammo.transaction.multiple-success
+PASS ammo.transaction.missing-powder
+PASS ammo.transaction.missing-ball
+PASS ammo.transaction.empty
+PASS ammo.transaction.null-inventory
+PASS ammo.transaction.first-remove-failure-rolls-back
+PASS ammo.transaction.second-remove-failure-rolls-back
+PASS ammo.transaction.after-mutation-failure-rolls-back
+PASS ammo.transaction.verification-failure-rolls-back
+PASS ammo.transaction.rollback-failure-surfaced
+PASS ammo.result.success-validation
+PASS ammo.result.insufficient-validation
+PASS ammo.result.unknown-status
+PASS ammo.result.null-snapshots
+PASS ammo.result.format
+PASS reload.transaction.success
+PASS reload.transaction.already-loaded
+PASS reload.transaction.broken
+PASS reload.transaction.wrecked
+PASS reload.transaction.missing-powder
+PASS reload.transaction.missing-ball
+PASS reload.transaction.null-state-store
+PASS reload.transaction.null-inventory
+PASS reload.transaction.null-rules
+PASS reload.transaction.null-ammunition
+PASS reload.transaction.null-state
+PASS reload.transaction.incompatible-ammunition
+PASS reload.transaction.state-write-failure-restores-inventory
+PASS reload.transaction.post-state-mutation-failure-restores-both
+PASS reload.transaction.state-rollback-failure-surfaced
+PASS reload.transaction.inventory-rollback-failure-surfaced
+PASS reload.result.success-validation
+PASS reload.result.rejected-validation
+PASS reload.result.unknown-status
+PASS reload.result.null-values
+PASS reload.result.format
+PASS discharge.loaded-normal
+PASS discharge.loaded-broken
+PASS discharge.multiple-rounds
+PASS discharge.empty-normal
+PASS discharge.empty-broken
+PASS discharge.wrecked
+PASS discharge.null-state
+PASS discharge.result-fired-validation
+PASS discharge.result-empty-validation
+PASS discharge.result-wrecked-validation
+PASS discharge.result-unknown-status
+PASS discharge.result-null-state
+PASS discharge.result-format
+PASS event-gate.first-and-duplicate
+PASS event-gate.reference-identity
+PASS event-gate.null
+PASS event-gate.value-type
+PASS patch-target.exact-contract
+PASS patch-target.null-method
+PASS patch-target.zero-argument
+PASS patch-target.wrong-context
+PASS patch-target.multiple-arguments
+PASS patch-target.static
+PASS patch-target.generic
+PASS patch-target.non-void
+PASS patch-target.null-context
+PASS token-reconcile.none
+PASS token-reconcile.preserved
+PASS token-reconcile.restore
+PASS token-reconcile.appeared-conflict
+PASS token-reconcile.changed-conflict
+PASS token-reconcile.multiple-before-conflict
+PASS token-reconcile.multiple-after-conflict
+PASS token-reconcile.null-before
+PASS token-reconcile.blank-token
+PASS token-reconcile.format-defensive-copy
+PASS state.ammunition-id.valid
+PASS state.ammunition-id.value-equality
+PASS state.ammunition-id.uppercase-rejected
+PASS state.ammunition-id.null
+PASS state.ammunition-id.empty
+PASS state.ammunition-id.leading-separator
+PASS state.ammunition-id.whitespace
+PASS state.ammunition-id.too-long
+PASS state.rules.valid
+PASS state.rules.sorted-copy
+PASS state.rules.capacity-zero
+PASS state.rules.capacity-too-large
+PASS state.rules.null-collection
+PASS state.rules.empty-collection
+PASS state.rules.null-entry
+PASS state.rules.duplicate
+PASS state.empty.canonical
+PASS state.value-equality
+PASS state.format.deterministic
+PASS state.invalid-schema
+PASS state.invalid-negative-rounds
+PASS state.invalid-loaded-without-ammo
+PASS state.invalid-empty-with-ammo
+PASS state.invalid-unknown-condition
+PASS state.invalid-wrecked-loaded
+PASS state.load.empty
+PASS state.load.partial-to-capacity
+PASS state.load.broken
+PASS state.load.over-capacity
+PASS state.load.incompatible
+PASS state.load.mixed
+PASS state.load.zero-rounds
+PASS state.load.null-state
+PASS state.load.null-rules
+PASS state.load.invalid-existing-state
+PASS state.fire.consumes-one
+PASS state.fire.final-clears-ammo
+PASS state.fire.broken
+PASS state.fire.empty-rejected
+PASS state.fire.wrecked-rejected
+PASS state.misfire.normal-to-broken
+PASS state.misfire.broken-to-wrecked
+PASS state.misfire.wrecked-rejected
+PASS state.repair.broken-to-normal
+PASS state.repair.normal-rejected
+PASS state.repair.wrecked-rejected
+PASS state.wreck.normal-clears-load
+PASS state.wreck.idempotent
+PASS state.codec.empty-roundtrip
+PASS state.codec.loaded-broken-roundtrip
+PASS state.codec.canonical-dto
+PASS state.codec.wrong-schema
+PASS state.codec.unknown-condition
+PASS state.codec.case-sensitive-condition
+PASS state.codec.over-capacity
+PASS state.codec.incompatible-ammo
+PASS state.codec.loaded-missing-ammo
+PASS state.codec.empty-with-ammo
+PASS state.codec.wrecked-loaded
+PASS state.codec.null-data
+PASS state.codec.null-rules
+PASS token.definition.valid
+PASS token.definition.value-equality
+PASS token.definition.null-state
+PASS token.definition.empty-id
+PASS token.definition.uppercase-id
+PASS token.catalog.count
+PASS token.catalog.order
+PASS token.catalog.defensive-copy
+PASS token.catalog.absence-default
+PASS token.catalog.loaded-normal
+PASS token.catalog.broken-empty
+PASS token.catalog.broken-loaded
+PASS token.catalog.wrecked
+PASS token.catalog.unknown
+PASS token.catalog.duplicate-payload
+PASS token.catalog.null-payload
+PASS token.catalog.null-entry
+PASS token.catalog.encode-null
+PASS token.catalog.unsupported-state
+PASS token.catalog.duplicate-id
+PASS token.catalog.duplicate-state
+PASS token.catalog.default-token
+PASS token.catalog.require-unknown
+PASS token.catalog.contains
+PASS repository.reference-comparer.same
+PASS repository.reference-comparer.distinct-value-equal
+PASS repository.unseen-empty
+PASS repository.same-reference-stable-entry
+PASS repository.distinct-equal-separate-entries
+PASS repository.set-isolated
+PASS repository.set-increments-revision
+PASS repository.noop-set-no-revision
+PASS repository.transition-increments-revision
+PASS repository.transition-rejected-preserves
+PASS repository.transition-null-preserves
+PASS repository.try-get-missing-no-create
+PASS repository.snapshot-immutable
+PASS repository.remove-existing
+PASS repository.remove-missing
+PASS repository.remove-readd-new-entry
+PASS repository.null-key
+PASS repository.value-key
+PASS repository.null-state
+PASS repository.null-transition
+PASS repository.counters
+PASS item-state.exact-initializes-empty
+PASS item-state.native-rejected-no-entry
+PASS item-state.ambiguous-rejected-no-entry
+PASS item-state.blueprint-rejected-no-entry
+PASS item-state.canonical-key
+PASS item-state.two-firearms-independent
+PASS item-state.set-metadata
+PASS item-state.transition
+PASS item-state.get-existing-missing-no-create
+PASS item-state.forget
+PASS item-state.format-deterministic
+PASS token-repository.unseen-empty
+PASS token-repository.set-loaded
+PASS token-repository.set-broken
+PASS token-repository.reset-clears
+PASS token-repository.two-independent
+PASS token-repository.value-equal-independent
+PASS token-repository.revision
+PASS token-repository.noop
+PASS token-repository.transition
+PASS token-repository.transition-rejected
+PASS token-repository.transition-null
+PASS token-repository.try-get-empty
+PASS token-repository.try-get-token
+PASS token-repository.unknown-token
+PASS token-repository.duplicate-token
+PASS token-repository.replace-failure
+PASS token-repository.concurrent-change
+PASS token-repository.corrupt-write
+PASS token-repository.unsupported-state
+PASS token-repository.remove-token
+PASS token-repository.remove-missing
+PASS token-repository.remove-metadata
+PASS token-repository.snapshot-immutable
+PASS token-repository.counters
+PASS token-repository.null-key
+PASS token-repository.value-key
+PASS token-repository.null-state
+PASS token-repository.null-transition
+PASS vault-data.clone-null
+PASS vault-data.clone-independent
+PASS vault-data.equal
+PASS vault-data.not-equal
+PASS vault-data.describe
+PASS vault-repository.unseen-empty
+PASS vault-repository.set-loaded
+PASS vault-repository.reconstruct
+PASS vault-repository.reset-clears
+PASS vault-repository.two-independent
+PASS vault-repository.value-equal-independent
+PASS vault-repository.revision
+PASS vault-repository.noop
+PASS vault-repository.transition
+PASS vault-repository.transition-rejected
+PASS vault-repository.transition-null
+PASS vault-repository.try-get-empty
+PASS vault-repository.corrupt-read
+PASS vault-repository.replace-failure
+PASS vault-repository.concurrent-change
+PASS vault-repository.corrupt-write
+PASS vault-repository.remove-record
+PASS vault-repository.remove-missing
+PASS vault-repository.remove-metadata
+PASS vault-repository.snapshot-immutable
+PASS vault-repository.counters
+PASS vault-repository.null-key
+PASS vault-repository.value-key
+PASS vault-repository.null-state
+PASS vault-repository.null-transition
+PASS migration.no-token
+PASS migration.loaded-normal
+PASS migration.broken-empty
+PASS migration.broken-loaded
+PASS migration.wrecked
+PASS migration.same-state-cleanup
+PASS migration.conflict
+PASS migration.unknown-token
+PASS migration.duplicate-token
+PASS migration.vault-write-failure
+PASS migration.token-clear-failure
+PASS migration.rollback-failure
+PASS migration.set-after
+PASS migration.transition-after
+PASS migration.try-get
+PASS migration.remove
+PASS migration.two-independent
+PASS migration.value-equal-independent
+PASS migration.no-repeat
+PASS migration.snapshot-format
+PASS migration.null-key
+PASS migration.null-state
+PASS migration.null-transition
+PASS identity.valid
+PASS identity.uppercase-canonical
+PASS identity.guid-constructor
+PASS identity.value-equality
+PASS identity.inequality
+PASS identity.order
+PASS identity.empty
+PASS identity.compact
+PASS identity.braces
+PASS identity.whitespace
+PASS identity.null
+PASS identity.try-create-valid
+PASS identity.try-create-invalid
+PASS identity.operator-null
+PASS identity-vault.write-read
+PASS identity-vault.reconstruct-same-id
+PASS identity-vault.different-ids
+PASS identity-vault.value-equal-different-ids
+PASS identity-vault.value-equal-same-id
+PASS identity-vault.reset-removes
+PASS identity-vault.compare-failure
+PASS identity-vault.remove
+PASS identity-vault.remove-missing
+PASS identity-vault.provider-reject-read
+PASS identity-vault.provider-reject-write
+PASS identity-vault.provider-null
+PASS identity-vault.defensive-read
+PASS identity-vault.defensive-write
+PASS identity-vault.count
+PASS identity-repository.reconstruct
+PASS identity-repository.two-independent
+PASS identity-repository.same-id
+PASS identity-repository.noop
+PASS identity-migration.snapshot-format
+PASS identity-migration.snapshot-properties
+PASS reflection.private-field
+PASS reflection.static-property
+PASS reflection.first-non-null-member
+PASS reflection.path
+PASS reflection.enumerate
+PASS reflection.invoke-exact
+PASS reflection.invoke-optional-default
+PASS reflection.invoke-trailing-null
+PASS reflection.invoke-required-bool
+PASS reflection.invoke-incompatible
+PASS range.zero-is-first
+PASS range.within-first
+PASS range.exact-boundary
+PASS range.second
+PASS range.multiple
+PASS range.invalid-negative
+PASS range.invalid-zero-increment
+PASS range.invalid-infinite
+PASS trace.native-marker-ignored
+PASS trace.ambiguous-marker-ignored
+PASS trace.exact-firearm-starts
+PASS trace.child-joins-parent
+PASS trace.duplicate-callback
+PASS trace.unrelated-child-ignored
+PASS trace.weapon-root-completes
+PASS trace.attack-roll-root-completes
+PASS trace.reset-clears-active
+PASS trace.id-source-must-be-unique
+PASS observation.copies-fields
+PASS observation.exact-requires-one-marker
+PASS formatter.fields-sorted
+PASS formatter.sanitizes-one-line
+PASS formatter.completion-counts-duplicates
+PASS ac.native-ordinary
+PASS ac.ambiguous-ordinary
+PASS ac.missing-definition
+PASS ac.zero-distance-touch
+PASS ac.close-touch
+PASS ac.boundary-touch
+PASS ac.boundary-float-noise-touch
+PASS ac.distant-ordinary
+PASS ac.cover-preserved
+PASS ac.flat-footed-preserved
+PASS ac.equal-no-write
+PASS ac.already-applied
+PASS ac.advanced-fails-closed
+PASS ac.invalid-distance
+PASS ac.negative-distance
+PASS ac.infinite-distance
+PASS ac.overflow
+PASS ac-access.participants
+PASS ac-access.distance
+PASS ac-access.values
+PASS ac-access.private-property
+PASS ac-access.field
+PASS ac-access.ambiguous-rejected
+PASS evidence.catalog-count
+PASS evidence.catalog-severity-counts
+PASS evidence.catalog-order
+PASS evidence.catalog-unique-ids
+PASS evidence.catalog-reproduction-ids
+PASS evidence.catalog-require-known
+PASS evidence.catalog-require-unknown
+PASS evidence.observation-valid
+PASS evidence.observation-invalid-sequence
+PASS evidence.observation-unknown-step
+PASS evidence.observation-nonutc
+PASS evidence.observation-invalid-hash
+PASS evidence.observation-uppercase-hash
+PASS evidence.evaluation-empty
+PASS evidence.evaluation-critical-fail
+PASS evidence.evaluation-blocked
+PASS evidence.evaluation-single-pass-incomplete
+PASS evidence.evaluation-reproduced-go
+PASS evidence.evaluation-high-fail-warning
+PASS evidence.evaluation-latest-fail
+PASS evidence.evaluation-same-run-not-reproduced
+PASS evidence.evaluation-null-observation
+PASS evidence.evaluation-duplicate-sequence
+PASS evidence.evaluation-format
+PASS preflight.probe-negative-bootstrap-count
+PASS preflight.probe-negative-blueprint-count
+PASS preflight.probe-invalid-expected-count
+PASS preflight.probe-negative-identity-count
+PASS preflight.check-only-i01-i02
+PASS preflight.report-order-required
+PASS preflight.report-all-passed
+PASS preflight.report-require
+PASS preflight.evaluate-pass-guid
+PASS preflight.evaluate-pass-string
+PASS preflight.evaluate-bootstrap-blocked
+PASS preflight.evaluate-bootstrap-not-initialized
+PASS preflight.evaluate-bootstrap-duplicate-init
+PASS preflight.evaluate-bootstrap-count-mismatch
+PASS preflight.evaluate-identity-blocked
+PASS preflight.evaluate-identity-missing
+PASS preflight.evaluate-identity-duplicate
+PASS preflight.evaluate-identity-unreadable
+PASS preflight.evaluate-identity-unsupported-type
+PASS preflight.format
+Completed 455 tests; failures=0.
diff --git a/evidence/sprint22-repair/validation/source-validation.stderr.txt b/evidence/sprint22-repair/validation/source-validation.stderr.txt
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/evidence/sprint22-repair/validation/source-validation.stdout.txt b/evidence/sprint22-repair/validation/source-validation.stdout.txt
new file mode 100644
index 0000000000000000000000000000000000000000..ec7f19a9ed2be10d46dcc6f32a518ccbb3b2819a
--- /dev/null
+++ b/evidence/sprint22-repair/validation/source-validation.stdout.txt
@@ -0,0 +1,5 @@
+Sprint 22 repair source invariant validation passed.
+Blueprints: 12 stable, 11 active, 1 reserved.
+Compile items: 139 main, 83 test.
+Tests: 455 declared, including 27 Sprint 22 and 9 patch-target repair cases.
+Repair invariants: exact RulebookEventContext hooks and weapon-only token inspection.
diff --git a/planning/SPRINT-23-ENTRY-CRITERIA.md b/planning/SPRINT-23-ENTRY-CRITERIA.md
index 849e08f3837afad651a7f6d10a14454e7f555c64..7aceece40cb9ef67f92f847d5f3c84e714b44f8c 100644
--- a/planning/SPRINT-23-ENTRY-CRITERIA.md
+++ b/planning/SPRINT-23-ENTRY-CRITERIA.md
@@ -1,6 +1,6 @@
 # Sprint 23 entry criteria — natural-roll misfire detection
 
-Sprint 23 may connect natural attack rolls to firearm misfires only after version 0.0.22 demonstrates all of the following in Kingmaker:
+Sprint 23 may connect natural attack rolls to firearm misfires only after version 0.0.22.1 (or a later bounded Sprint 22 repair) demonstrates all of the following in Kingmaker:
 
 - the loaded Test Musket remains loaded immediately after quicksave;
 - state-token reconciliation reports no unresolved conflict or fault;
diff --git a/scripts/README.md b/scripts/README.md
index eaf802f0b6bba2a3e53c3ed1936a1368bf8ff16d..2dc39817afbf386241226db2a7a98f163ae4c81f 100644
--- a/scripts/README.md
+++ b/scripts/README.md
@@ -2,20 +2,27 @@
 
 - `new-game-path-props.ps1` writes an ignored local `GamePath.props` for an installed Kingmaker directory.
 - `fingerprint-environment.ps1` records local version and assembly hashes without copying game files.
-- `inspect-runtime-contracts.ps1` verifies lifecycle, blueprint, cloning, equipment, combat-event, firearm-AC, Sprint 17 runtime-qualification, strict `UniqueId`, UnitPart-vault, and both legacy-migration contracts.
-- `test-domain.ps1` builds and runs 373 dependency-free firearm-domain, state-transition, repository, identity, vault, migration, evidence, preflight, reflection, range, correlation, immutability, and formatting tests.
-- `test-domain-portable.ps1` runs the same explicit source set through a temporary .NET 8 SDK project without requiring Kingmaker assemblies.
-- `validate-repository.ps1` checks versioning, stable IDs, pure-state boundaries, strict identity constraints, evidence/preflight restrictions, A-D fixture invariants, named tests, and source cleanliness.
-- `qualify-runtime-candidate.ps1` is the one-command Windows path from source to a fingerprinted, tested, validated local UMM smoke-test candidate.
+- `inspect-runtime-contracts.ps1` verifies the exact Kingmaker/UMM/Harmony contracts, including `void OnTrigger(RulebookEventContext)`, item-owned enchantment access, reload delivery, and the current token-backed firearm path.
+- `test-domain.ps1` builds and runs the dependency-free firearm-domain suite against .NET Framework 4.7.
+- `test-domain-portable.ps1` runs the same explicit source set through a temporary modern .NET SDK project when one is available.
+- `validate-repository.ps1` runs the current Sprint 22 repair invariant validator.
+- `qualify-runtime-candidate.ps1` is the Windows source-to-candidate path for an exact installed Kingmaker environment; it runs the current contract gate, 455 tests three times, two same-output-path Release compiles, and strict package validation.
 - `build.ps1` validates, tests, builds, and optionally packages using local game references.
-- `package.ps1` creates a UMM install archive only from a successful Release build.
-- `package-source.ps1` creates a source-only archive.
-- `validate-build-output.ps1` rejects copied game, Unity, Harmony, UMM, and other dependency binaries.
-- `validate-package.ps1` validates the generated UMM archive layout and allowlist.
+- `package.ps1` creates the strict eight-file standalone UMM archive only from a successful build and rejects symbol inclusion.
+- `package-source.ps1` creates a source-only archive and excludes private references and build output.
+- `validate-build-output.ps1` rejects copied game, Unity, Harmony, UMM, Newtonsoft, and other dependency binaries.
+- `validate-package.ps1` validates the generated UMM archive against the strict eight-file allowlist and one-binary rule.
 
-A source milestone ZIP is not installable through Unity Mod Manager. Only the inner archive generated by `qualify-runtime-candidate.ps1` or `build.ps1 -Package` after a successful local build is a candidate UMM package.
+The repository also includes cross-platform evidence tools under `tools/`:
 
-## Local qualification
+- `build_mod_from_private_references.py` compiles the mod against an extracted private reference bundle using Roslyn and the .NET Framework 4.7 reference surface.
+- `run_exact_net47_domain_tests.py` compiles the dependency-free harness with warnings as errors and executes all 455 tests three times.
+- `validate_sprint22_repair.py` validates source, version, exact target-contract, weapon-only reconciliation, test, evidence, and private-reference exclusion invariants.
+- `validate_sprint22_repair_package.py` validates the standalone 0.0.22.1 UMM ZIP and proves that its only binary is `KingmakerGunslinger.dll`.
+
+A source or milestone ZIP is not installable through Unity Mod Manager. Only the standalone archive whose root is `KingmakerGunslinger/` and which contains `KingmakerGunslinger.dll` is an install candidate.
+
+## Windows local qualification
 
 ```powershell
 .\scripts\qualify-runtime-candidate.ps1 `
@@ -24,4 +31,4 @@ A source milestone ZIP is not installable through Unity Mod Manager. Only the in
   -DisplayedGameVersion 2.1.7b
 ```
 
-The generated candidate is installable for smoke testing but does not become a persistence `Go` result until the full in-game matrix passes.
+Do not bypass a failed contract report. Exact compilation and tests establish candidate quality; the in-game smoke guide establishes runtime behavior. Sprint 23 remains blocked until every entry criterion passes.
diff --git a/scripts/inspect-runtime-contracts.ps1 b/scripts/inspect-runtime-contracts.ps1
index 4d3ccef297a04b0b5f96b8c169284177a0b1537e..d47ef0854ad4b3ff851822b7cc3b4b8b1b98f00a 100644
--- a/scripts/inspect-runtime-contracts.ps1
+++ b/scripts/inspect-runtime-contracts.ps1
@@ -298,6 +298,7 @@ try {
     $unitDescriptorType = $gameAssembly.GetType('Kingmaker.UnitLogic.UnitDescriptor', $false, $false)
     $unitPartType = $gameAssembly.GetType('Kingmaker.UnitLogic.UnitPart', $false, $false)
     $gameType = $gameAssembly.GetType('Kingmaker.Game', $false, $false)
+    $rulebookEventContextType = $gameAssembly.GetType('Kingmaker.RuleSystem.RulebookEventContext', $false, $false)
     $ruleAttackWithWeaponType = $gameAssembly.GetType('Kingmaker.RuleSystem.Rules.RuleAttackWithWeapon', $false, $false)
     $ruleAttackRollType = $gameAssembly.GetType('Kingmaker.RuleSystem.Rules.RuleAttackRoll', $false, $false)
     $ruleCalculateAcType = $gameAssembly.GetType('Kingmaker.RuleSystem.Rules.RuleCalculateAC', $false, $false)
@@ -305,6 +306,20 @@ try {
     $itemEntityType = $gameAssembly.GetType('Kingmaker.Items.ItemEntity', $false, $false)
     $itemEntityWeaponType = $gameAssembly.GetType('Kingmaker.Items.ItemEntityWeapon', $false, $false)
     $itemEnchantmentType = $gameAssembly.GetType('Kingmaker.Items.ItemEnchantment', $false, $false)
+    $itemApplyEnchantmentsMethods = if ($itemEntityType) {
+        @(
+            $itemEntityType.GetMethods($bindingFlags) |
+                Where-Object {
+                    $_.Name -eq 'ApplyEnchantments' -and
+                    -not $_.IsStatic -and
+                    -not $_.IsGenericMethodDefinition -and
+                    $_.DeclaringType.FullName -eq $itemEntityType.FullName -and
+                    $_.ReturnType.FullName -eq 'System.Void' -and
+                    $_.GetParameters().Count -eq 0
+                }
+        )
+    }
+    else { @() }
     foreach ($requiredType in @($libraryType, $blueprintBaseType, $blueprintComponentType, $blueprintFeatureType, $blueprintWeaponType, $blueprintItemWeaponType, $equipmentRestrictionType, $unitDescriptorType, $unitPartType, $gameType)) {
         if (-not $requiredType) {
             throw 'A required Kingmaker blueprint type was not found in Assembly-CSharp.dll.'
@@ -601,21 +616,21 @@ try {
     $ruleAttackWithWeaponOnTriggerMethods = if ($ruleAttackWithWeaponType) {
         @(
             $ruleAttackWithWeaponType.GetMethods($bindingFlags) |
-                Where-Object { $_.Name -eq 'OnTrigger' -and -not $_.IsStatic -and $_.DeclaringType.FullName -eq $ruleAttackWithWeaponType.FullName -and $_.GetParameters().Count -eq 0 }
+                Where-Object { $parameters = $_.GetParameters(); $_.Name -eq 'OnTrigger' -and -not $_.IsStatic -and -not $_.IsGenericMethodDefinition -and $_.ReturnType.FullName -eq 'System.Void' -and $_.DeclaringType.FullName -eq $ruleAttackWithWeaponType.FullName -and $parameters.Count -eq 1 -and $rulebookEventContextType -ne $null -and $parameters[0].ParameterType.FullName -eq $rulebookEventContextType.FullName }
         )
     }
     else { @() }
     $ruleAttackRollOnTriggerMethods = if ($ruleAttackRollType) {
         @(
             $ruleAttackRollType.GetMethods($bindingFlags) |
-                Where-Object { $_.Name -eq 'OnTrigger' -and -not $_.IsStatic -and $_.DeclaringType.FullName -eq $ruleAttackRollType.FullName -and $_.GetParameters().Count -eq 0 }
+                Where-Object { $parameters = $_.GetParameters(); $_.Name -eq 'OnTrigger' -and -not $_.IsStatic -and -not $_.IsGenericMethodDefinition -and $_.ReturnType.FullName -eq 'System.Void' -and $_.DeclaringType.FullName -eq $ruleAttackRollType.FullName -and $parameters.Count -eq 1 -and $rulebookEventContextType -ne $null -and $parameters[0].ParameterType.FullName -eq $rulebookEventContextType.FullName }
         )
     }
     else { @() }
     $ruleCalculateAcOnTriggerMethods = if ($ruleCalculateAcType) {
         @(
             $ruleCalculateAcType.GetMethods($bindingFlags) |
-                Where-Object { $_.Name -eq 'OnTrigger' -and -not $_.IsStatic -and $_.DeclaringType.FullName -eq $ruleCalculateAcType.FullName -and $_.GetParameters().Count -eq 0 }
+                Where-Object { $parameters = $_.GetParameters(); $_.Name -eq 'OnTrigger' -and -not $_.IsStatic -and -not $_.IsGenericMethodDefinition -and $_.ReturnType.FullName -eq 'System.Void' -and $_.DeclaringType.FullName -eq $ruleCalculateAcType.FullName -and $parameters.Count -eq 1 -and $rulebookEventContextType -ne $null -and $parameters[0].ParameterType.FullName -eq $rulebookEventContextType.FullName }
         )
     }
     else { @() }
@@ -796,6 +811,7 @@ try {
         $itemEntityType -ne $null -and
         $itemEntityWeaponType -ne $null -and
         $itemEntityType.IsAssignableFrom($itemEntityWeaponType) -and
+        $itemApplyEnchantmentsMethods.Count -eq 1 -and
         $itemEnchantmentType -ne $null -and
         $itemEnchantmentCollectionMembers.Count -ge 1 -and
         $itemEnchantmentBlueprintMembers.Count -ge 1 -and
@@ -821,6 +837,7 @@ try {
         $unitPartEnsureContractPassed
     )
     $combatTracePatchContractPassed = (
+        $rulebookEventContextType -ne $null -and
         $ruleAttackWithWeaponType -ne $null -and
         $ruleAttackRollType -ne $null -and
         $ruleCalculateAcType -ne $null -and
@@ -838,6 +855,7 @@ try {
     )
     $combatTraceContractPassed = $combatTracePatchContractPassed -and $combatTraceDataContractPassed
     $firearmArmorClassContractPassed = (
+        $rulebookEventContextType -ne $null -and
         $ruleAttackRollType -ne $null -and
         $ruleCalculateAcType -ne $null -and
         $ruleAttackRollOnTriggerMethods.Count -eq 1 -and
@@ -867,8 +885,6 @@ try {
         $developmentUiContractPassed -and
         $developmentBridgeContractPassed -and
         $runtimeItemStateContractPassed -and
-        $firearmItemIdentityContractPassed -and
-        $unitPartVaultContractPassed -and
         $persistenceTokenContractPassed -and
         $combatTraceContractPassed -and
         $firearmArmorClassContractPassed
@@ -877,7 +893,8 @@ try {
     $result = [ordered]@{
         capturedAtUtc = [DateTime]::UtcNow.ToString('o')
         kingmakerInstallDirectory = $KingmakerInstallDir
-        sprint = 17
+        sprint = 22
+        milestone = '0.0.22.1-s22-attack-hook-repair'
         contractPassed = $contractPassed
         assemblies = @(
             [ordered]@{
@@ -945,14 +962,14 @@ try {
                 itemEntityWeaponType = if ($itemEntityWeaponType) { $itemEntityWeaponType.FullName } else { $null }
                 blueprintMembers = @($itemEntityWeaponBlueprintMembers | ForEach-Object { Convert-KmgMemberDescription -Member $_ })
                 runtimeIdMembers = @($itemEntityWeaponRuntimeIdMembers | ForEach-Object { Convert-KmgMemberDescription -Member $_ })
-                identityPolicy = 'Sprint 14 persistence accepts only the inherited UniqueId member when it is exactly one readable System.Guid or System.String. The mod never generates or writes an identity.'
-                persistenceCandidateImplemented = $true
-                persistenceRuntimeProven = $false
+                identityPolicy = 'Runtime firearm identity is the exact ItemEntityWeapon reference plus exactly one FirearmDefinitionComponent marker. Item UniqueId is not required.'
+                authoritativeCarrier = 'item-owned inert BlueprintWeaponEnchantment token'
+                absenceMeans = 'Empty/Normal'
                 contractPassed = $runtimeItemStateContractPassed
             }
-            firearmItemIdentity = [ordered]@{
-                requiredMemberName = 'UniqueId'
-                acceptedValueTypes = @('System.Guid', 'System.String')
+            rejectedHistoricalItemIdentity = [ordered]@{
+                memberName = 'UniqueId'
+                historicallyAcceptedValueTypes = @('System.Guid', 'System.String')
                 matchingMembers = @($itemEntityWeaponUniqueIdMembers | ForEach-Object { Convert-KmgMemberDescription -Member $_ })
                 matchingMemberCount = $itemEntityWeaponUniqueIdMembers.Count
                 memberReadable = $itemEntityWeaponUniqueIdReadable
@@ -960,28 +977,21 @@ try {
                 memberValueType = if ($itemEntityWeaponUniqueIdType) { $itemEntityWeaponUniqueIdType.FullName } else { $null }
                 generatedByMod = $false
                 fallbackMembersAccepted = $false
-                contractPassed = $firearmItemIdentityContractPassed
+                requiredForCurrentBuild = $false
+                rejectedByInstalledRuntimeEvidence = -not $firearmItemIdentityContractPassed
             }
-            firearmStatePersistence = [ordered]@{
-                carrier = 'save-owned UnitPart on the main character containing canonical engine item GUID strings and primitive state DTOs'
+            rejectedHistoricalIdentityVault = [ordered]@{
+                carrier = 'save-owned UnitPart keyed by ItemEntityWeapon.UniqueId'
+                requiredForCurrentBuild = $false
                 unitPartType = if ($unitPartType) { $unitPartType.FullName } else { $null }
-                unitPartIsSealed = if ($unitPartType) { $unitPartType.IsSealed } else { $null }
-                jsonPropertyAttributeType = if ($jsonPropertyAttributeType) { $jsonPropertyAttributeType.FullName } else { $null }
-                instanceGetMethods = @($unitPartInstanceGetMethods | ForEach-Object { Convert-KmgMethodDescription -Method $_ })
-                extensionGetMethods = @($unitPartExtensionGetMethods | ForEach-Object { Convert-KmgMethodDescription -Method $_ })
-                instanceEnsureMethods = @($unitPartInstanceEnsureMethods | ForEach-Object { Convert-KmgMethodDescription -Method $_ })
-                extensionEnsureMethods = @($unitPartExtensionEnsureMethods | ForEach-Object { Convert-KmgMethodDescription -Method $_ })
-                mainCharacterMembers = @($mainCharacterMembers | ForEach-Object { Convert-KmgMemberDescription -Member $_ })
-                primaryIdentityType = 'canonical System.Guid D-form string'
-                legacyDirectItemReferenceType = if ($itemEntityWeaponType) { $itemEntityWeaponType.FullName } else { $null }
-                legacyDirectReferencePurpose = 'one-way Sprint 13 migration only; new state writes never create direct-reference records'
-                saveLoadDurability = 'PENDING IN-GAME LIFECYCLE MATRIX'
-                contractPassed = $unitPartVaultContractPassed
+                historicalContractPassed = $unitPartVaultContractPassed -and $firearmItemIdentityContractPassed
+                policy = 'Compiler diagnostics only. Do not reactivate this carrier or emit new writes.'
             }
-            legacyStateTokenMigration = [ordered]@{
-                purpose = 'Read, verify, migrate into the Sprint 14 identity vault, and then remove the four stable Sprint 12 item-enchantment tokens. New writes never use this carrier.'
+            firearmStateTokenCarrier = [ordered]@{
+                purpose = 'Authoritative per-item firearm state carrier. Inspect and mutate only ItemEntityWeapon enchantments; reconcile around the exact ItemEntity.ApplyEnchantments() method.'
                 blueprintWeaponEnchantmentType = if ($blueprintWeaponEnchantmentType) { $blueprintWeaponEnchantmentType.FullName } else { $null }
                 blueprintWeaponEnchantmentIsAbstract = if ($blueprintWeaponEnchantmentType) { $blueprintWeaponEnchantmentType.IsAbstract } else { $null }
+                itemEntityApplyEnchantmentsMethods = @($itemApplyEnchantmentsMethods | ForEach-Object { Convert-KmgMethodDescription -Method $_ })
                 itemEnchantmentType = if ($itemEnchantmentType) { $itemEnchantmentType.FullName } else { $null }
                 enchantmentCollectionMembers = @($itemEnchantmentCollectionMembers | ForEach-Object { Convert-KmgMemberDescription -Member $_ })
                 itemEnchantmentBlueprintMembers = @($itemEnchantmentBlueprintMembers | ForEach-Object { Convert-KmgMemberDescription -Member $_ })
@@ -992,6 +1002,8 @@ try {
                 contractPassed = $persistenceTokenContractPassed
             }
             combatTracing = [ordered]@{
+                rulebookEventContextType = if ($rulebookEventContextType) { $rulebookEventContextType.FullName } else { $null }
+                requiredOnTriggerSignature = 'System.Void OnTrigger(Kingmaker.RuleSystem.RulebookEventContext)'
                 ruleAttackWithWeaponType = if ($ruleAttackWithWeaponType) { $ruleAttackWithWeaponType.FullName } else { $null }
                 ruleAttackWithWeaponOnTriggerMethods = @($ruleAttackWithWeaponOnTriggerMethods | ForEach-Object { Convert-KmgMethodDescription -Method $_ })
                 ruleAttackWithWeaponMembers = @($weaponAttackMembers | ForEach-Object { Convert-KmgMemberDescription -Member $_ })
@@ -1039,7 +1051,7 @@ try {
     Write-Host "Wrote runtime contract report: $OutputPath"
 
     if (-not $contractPassed) {
-        throw 'The installed assemblies do not satisfy the Sprint 17 runtime-qualification build contract, including the Sprint 14 engine-item-identity vault and legacy-migration runtime contract. Review the generated JSON report.'
+        throw 'The installed assemblies do not satisfy the Sprint 22 repair runtime contract. Review the generated JSON report; do not fall back to ItemEntityWeapon.UniqueId or a guessed rule-event overload.'
     }
 }
 finally {
diff --git a/scripts/package-source.ps1 b/scripts/package-source.ps1
index a20ea31280b57f6dc0aa8c060ce3cacabc2f062e..aa52c787e9e4218f66611ddf758288d1ac7f3bdb 100644
--- a/scripts/package-source.ps1
+++ b/scripts/package-source.ps1
@@ -20,8 +20,8 @@ if (Test-Path -LiteralPath $stagingDirectory) {
 New-Item -ItemType Directory -Path $sourceRoot -Force | Out-Null
 New-Item -ItemType Directory -Path $packagesDirectory -Force | Out-Null
 
-$excludedRootNames = @('.git', '.vs', '.idea', 'artifacts')
-$excludedFileNames = @('GamePath.props', 'environment.json', 'runtime-contracts.json', 'source.zip', 'SHA256SUMS.txt', 'manifest.json')
+$excludedRootNames = @('.git', '.vs', '.idea', 'artifacts', '__pycache__')
+$excludedFileNames = @('GamePath.props', 'environment.json', 'runtime-contracts.json', 'source.zip', 'SHA256SUMS.txt', 'manifest.json', 'Assembly-CSharp.dll', 'Assembly-CSharp-firstpass.dll', 'UnityModManager.dll', '0Harmony12.dll', 'Newtonsoft.Json.dll')
 
 foreach ($file in Get-ChildItem -LiteralPath $repositoryRoot -Recurse -File) {
     $relativePath = $file.FullName.Substring($repositoryRoot.Length).TrimStart('\', '/')
@@ -32,7 +32,7 @@ foreach ($file in Get-ChildItem -LiteralPath $repositoryRoot -Recurse -File) {
     if ($file.Name -in $excludedFileNames) {
         continue
     }
-    if ($file.Extension -in @('.dll', '.pdb', '.mdb')) {
+    if ($file.Extension -in @('.dll', '.exe', '.pdb', '.mdb', '.pyc')) {
         continue
     }
 
diff --git a/scripts/package.ps1 b/scripts/package.ps1
index a27ce8b8ec688fc38e925ae5b8c8a2d2c2d0bb55..e54b04891f66f4bae3c7f683c530cd566ac7a2d6 100644
--- a/scripts/package.ps1
+++ b/scripts/package.ps1
@@ -13,13 +13,17 @@ $ErrorActionPreference = 'Stop'
 $repositoryRoot = Get-KmgRepositoryRoot -ScriptDirectory $PSScriptRoot
 $info = Get-KmgModInfo -RepositoryRoot $repositoryRoot
 
+if ($IncludeSymbols) {
+    throw 'The standalone UMM package contract permits exactly one binary. PDB symbols must not be included.'
+}
+
 & (Join-Path $PSScriptRoot 'validate-build-output.ps1') -Configuration $Configuration
 
 $outputDirectory = Join-Path $repositoryRoot "artifacts\bin\$Configuration\KingmakerGunslinger"
 $stagingDirectory = Join-Path $repositoryRoot 'artifacts\staging\install'
 $modDirectory = Join-Path $stagingDirectory $info.Id
 $packagesDirectory = Join-Path $repositoryRoot 'artifacts\packages'
-$packagePath = Join-Path $packagesDirectory "$($info.Id)-$($info.Version).zip"
+$packagePath = Join-Path $packagesDirectory "$($info.Id)-$($info.Version)-attack-hook-repair-smoke-test.zip"
 $checksumPath = "$packagePath.sha256"
 
 if (Test-Path -LiteralPath $stagingDirectory) {
@@ -28,17 +32,21 @@ if (Test-Path -LiteralPath $stagingDirectory) {
 New-Item -ItemType Directory -Path (Join-Path $modDirectory 'blueprints') -Force | Out-Null
 New-Item -ItemType Directory -Path $packagesDirectory -Force | Out-Null
 
-Copy-Item -LiteralPath (Join-Path $outputDirectory 'KingmakerGunslinger.dll') -Destination $modDirectory
-Copy-Item -LiteralPath (Join-Path $repositoryRoot 'Info.json') -Destination $modDirectory
-Copy-Item -LiteralPath (Join-Path $repositoryRoot 'blueprints\blueprints.json') -Destination (Join-Path $modDirectory 'blueprints')
-Copy-Item -LiteralPath (Join-Path $repositoryRoot 'blueprints\blueprints.schema.json') -Destination (Join-Path $modDirectory 'blueprints')
-
-if ($IncludeSymbols) {
-    $pdbPath = Join-Path $outputDirectory 'KingmakerGunslinger.pdb'
-    if (-not (Test-Path -LiteralPath $pdbPath -PathType Leaf)) {
-        throw "-IncludeSymbols was supplied, but the PDB does not exist: $pdbPath"
+$copies = @(
+    [ordered]@{ Source = (Join-Path $outputDirectory 'KingmakerGunslinger.dll'); Destination = $modDirectory },
+    [ordered]@{ Source = (Join-Path $repositoryRoot 'Info.json'); Destination = $modDirectory },
+    [ordered]@{ Source = (Join-Path $repositoryRoot 'CHANGELOG.md'); Destination = $modDirectory },
+    [ordered]@{ Source = (Join-Path $repositoryRoot 'LICENSE'); Destination = $modDirectory },
+    [ordered]@{ Source = (Join-Path $repositoryRoot 'README.md'); Destination = $modDirectory },
+    [ordered]@{ Source = (Join-Path $repositoryRoot 'SMOKE-TEST-GUIDE.md'); Destination = $modDirectory },
+    [ordered]@{ Source = (Join-Path $repositoryRoot 'blueprints\blueprints.json'); Destination = (Join-Path $modDirectory 'blueprints') },
+    [ordered]@{ Source = (Join-Path $repositoryRoot 'blueprints\blueprints.schema.json'); Destination = (Join-Path $modDirectory 'blueprints') }
+)
+foreach ($copy in $copies) {
+    if (-not (Test-Path -LiteralPath $copy.Source -PathType Leaf)) {
+        throw "Required package input is missing: $($copy.Source)"
     }
-    Copy-Item -LiteralPath $pdbPath -Destination $modDirectory
+    Copy-Item -LiteralPath $copy.Source -Destination $copy.Destination
 }
 
 if (Test-Path -LiteralPath $packagePath) {
@@ -54,5 +62,5 @@ Compress-Archive -LiteralPath $modDirectory -DestinationPath $packagePath -Compr
 $checksum = Get-KmgSha256 -Path $packagePath
 Set-Content -LiteralPath $checksumPath -Value "$checksum  $([IO.Path]::GetFileName($packagePath))" -Encoding ASCII
 
-Write-Host "Created install package: $packagePath"
+Write-Host "Created standalone UMM package: $packagePath"
 Write-Host "Created checksum: $checksumPath"
diff --git a/scripts/qualify-runtime-candidate.ps1 b/scripts/qualify-runtime-candidate.ps1
index 506300731c8419b76f9c5f157e75037ca2355e12..33b3511aabb5a13f9bfac34158202093c980e4f8 100644
--- a/scripts/qualify-runtime-candidate.ps1
+++ b/scripts/qualify-runtime-candidate.ps1
@@ -22,174 +22,190 @@ $ErrorActionPreference = 'Stop'
 . (Join-Path $PSScriptRoot 'common.ps1')
 $repositoryRoot = Get-KmgRepositoryRoot -ScriptDirectory $PSScriptRoot
 $info = Get-KmgModInfo -RepositoryRoot $repositoryRoot
-$qualificationRoot = Join-Path $repositoryRoot "artifacts\qualification\$($info.Id)-$($info.Version)-runtime-candidate"
+
+if ($IncludeSymbols) {
+    throw 'The standalone UMM package contract permits exactly one binary. PDB symbols must not be included.'
+}
+if (-not (Test-Path -LiteralPath $KingmakerInstallDir -PathType Container)) {
+    throw "KingmakerInstallDir does not exist: $KingmakerInstallDir"
+}
+$KingmakerInstallDir = (Resolve-Path -LiteralPath $KingmakerInstallDir).Path
+
+$qualificationRoot = Join-Path $repositoryRoot "artifacts\qualification\$($info.Id)-$($info.Version)-s22-repair"
 $environmentPath = Join-Path $qualificationRoot 'environment.json'
 $contractsPath = Join-Path $qualificationRoot 'runtime-contracts.json'
-$candidatePath = Join-Path $qualificationRoot "$($info.Id)-$($info.Version).zip"
+$testsDirectory = Join-Path $qualificationRoot 'tests'
+$compileDirectory = Join-Path $qualificationRoot 'compile'
+$candidateName = "$($info.Id)-$($info.Version)-attack-hook-repair-smoke-test.zip"
+$candidatePath = Join-Path $qualificationRoot $candidateName
 $candidateChecksumPath = "$candidatePath.sha256"
 $qualificationJsonPath = Join-Path $qualificationRoot 'runtime-candidate.json'
 $qualificationMarkdownPath = Join-Path $qualificationRoot 'RUNTIME-CANDIDATE.md'
-$bundlePath = Join-Path $repositoryRoot "artifacts\qualification\$($info.Id)-$($info.Version)-runtime-candidate-bundle.zip"
+$bundlePath = Join-Path $repositoryRoot "artifacts\qualification\$($info.Id)-$($info.Version)-s22-repair-bundle.zip"
 $bundleChecksumPath = "$bundlePath.sha256"
 
-if (-not (Test-Path -LiteralPath $KingmakerInstallDir -PathType Container)) {
-    throw "KingmakerInstallDir does not exist: $KingmakerInstallDir"
-}
-$KingmakerInstallDir = (Resolve-Path -LiteralPath $KingmakerInstallDir).Path
-
 if (Test-Path -LiteralPath $qualificationRoot) {
     Remove-Item -LiteralPath $qualificationRoot -Recurse -Force
 }
-New-Item -ItemType Directory -Path $qualificationRoot -Force | Out-Null
+New-Item -ItemType Directory -Path $testsDirectory -Force | Out-Null
+New-Item -ItemType Directory -Path $compileDirectory -Force | Out-Null
 
-Write-Host 'Step 1/7: validating source repository.'
+Write-Host 'Step 1/8: validating the Sprint 22 repair source repository.'
 & (Join-Path $PSScriptRoot 'validate-repository.ps1')
 
-Write-Host 'Step 2/7: validating the explicit Kingmaker installation path without writing GamePath.props.'
+Write-Host 'Step 2/8: validating the explicit Kingmaker installation path.'
 $managedDirectory = Join-Path $KingmakerInstallDir 'Kingmaker_Data\Managed'
 if (-not (Test-Path -LiteralPath (Join-Path $managedDirectory 'Assembly-CSharp.dll') -PathType Leaf)) {
     throw "Assembly-CSharp.dll was not found under the supplied Kingmaker installation: $managedDirectory"
 }
 
-Write-Host 'Step 3/7: capturing exact local environment fingerprint.'
+Write-Host 'Step 3/8: capturing the exact local environment and current runtime contracts.'
 & (Join-Path $PSScriptRoot 'fingerprint-environment.ps1') `
     -KingmakerInstallDir $KingmakerInstallDir `
     -Storefront $Storefront `
     -DisplayedGameVersion $DisplayedGameVersion `
     -EnabledMods $EnabledMods `
     -OutputPath $environmentPath
-
-Write-Host 'Step 4/7: inspecting installed Kingmaker, UMM, Harmony, and persistence contracts.'
 & (Join-Path $PSScriptRoot 'inspect-runtime-contracts.ps1') `
     -KingmakerInstallDir $KingmakerInstallDir `
     -OutputPath $contractsPath
-
 $contracts = Get-Content -LiteralPath $contractsPath -Raw | ConvertFrom-Json
 if (-not $contracts.contractPassed) {
-    throw 'The runtime-contract report did not pass. No install candidate will be produced.'
+    throw 'The current Sprint 22 repair runtime-contract report did not pass.'
 }
 
-Write-Host 'Step 5/7: compiling and executing the dependency-free C# test harness.'
+Write-Host 'Step 4/8: building the dependency-free test harness.'
 & (Join-Path $PSScriptRoot 'test-domain.ps1') `
     -Configuration Release `
     -MSBuildPath $MSBuildPath `
     -Clean
+$testExecutable = Join-Path $repositoryRoot 'artifacts\tests\Release\KingmakerGunslinger.DomainTests\KingmakerGunslinger.DomainTests.exe'
+if (-not (Test-Path -LiteralPath $testExecutable -PathType Leaf)) {
+    throw "Domain test executable was not produced: $testExecutable"
+}
+
+Write-Host 'Step 5/8: executing the full test suite three times with byte-identical output.'
+$testOutputHashes = @()
+for ($run = 1; $run -le 3; $run++) {
+    $runOutput = @(& $testExecutable 2>&1)
+    if ($LASTEXITCODE -ne 0) {
+        throw "Domain test run $run failed with exit code $LASTEXITCODE."
+    }
+    $runText = ($runOutput -join [Environment]::NewLine) + [Environment]::NewLine
+    $runPath = Join-Path $testsDirectory ("run{0}.stdout.txt" -f $run)
+    [IO.File]::WriteAllText($runPath, $runText, (New-Object Text.UTF8Encoding($false)))
+    if (-not $runText.TrimEnd().EndsWith('Completed 455 tests; failures=0.')) {
+        throw "Domain test run $run did not report the expected 455-case zero-failure summary."
+    }
+    $testOutputHashes += Get-KmgSha256 -Path $runPath
+}
+if (@($testOutputHashes | Select-Object -Unique).Count -ne 1) {
+    throw 'The three domain-test outputs were not byte-identical.'
+}
 
-Write-Host 'Step 6/7: compiling the mod and producing the UMM ZIP.'
+Write-Host 'Step 6/8: compiling twice at the same Release output path and comparing DLL bytes.'
 $buildArguments = @{
     Configuration = 'Release'
     KingmakerInstallDir = $KingmakerInstallDir
     MSBuildPath = $MSBuildPath
     Clean = $true
-    Package = $true
     SkipDomainTests = $true
 }
-if ($IncludeSymbols) {
-    $buildArguments.IncludeSymbols = $true
+& (Join-Path $PSScriptRoot 'build.ps1') @buildArguments
+$modDllPath = Join-Path $repositoryRoot 'artifacts\bin\Release\KingmakerGunslinger\KingmakerGunslinger.dll'
+if (-not (Test-Path -LiteralPath $modDllPath -PathType Leaf)) {
+    throw "The first Release build did not produce the mod DLL: $modDllPath"
 }
+$firstDllHash = Get-KmgSha256 -Path $modDllPath
+Set-Content -LiteralPath (Join-Path $compileDirectory 'first-dll.sha256') -Value "$firstDllHash  KingmakerGunslinger.dll" -Encoding ASCII
+
 & (Join-Path $PSScriptRoot 'build.ps1') @buildArguments
+if (-not (Test-Path -LiteralPath $modDllPath -PathType Leaf)) {
+    throw "The second Release build did not produce the mod DLL: $modDllPath"
+}
+$secondDllHash = Get-KmgSha256 -Path $modDllPath
+Set-Content -LiteralPath (Join-Path $compileDirectory 'second-dll.sha256') -Value "$secondDllHash  KingmakerGunslinger.dll" -Encoding ASCII
+if ($firstDllHash -ne $secondDllHash) {
+    throw 'Same-output-path deterministic compilation failed: the two mod DLL hashes differ.'
+}
 
-$builtPackagePath = Join-Path $repositoryRoot "artifacts\packages\$($info.Id)-$($info.Version).zip"
-$builtChecksumPath = "$builtPackagePath.sha256"
+Write-Host 'Step 7/8: producing and validating the strict standalone UMM ZIP.'
+& (Join-Path $PSScriptRoot 'package.ps1') -Configuration Release
+$builtPackagePath = Join-Path $repositoryRoot "artifacts\packages\$candidateName"
 if (-not (Test-Path -LiteralPath $builtPackagePath -PathType Leaf)) {
     throw "The expected UMM package was not produced: $builtPackagePath"
 }
-
 & (Join-Path $PSScriptRoot 'validate-package.ps1') -PackagePath $builtPackagePath
 Copy-Item -LiteralPath $builtPackagePath -Destination $candidatePath
-if (Test-Path -LiteralPath $builtChecksumPath -PathType Leaf) {
-    Copy-Item -LiteralPath $builtChecksumPath -Destination $candidateChecksumPath
-}
-else {
-    Set-Content -LiteralPath $candidateChecksumPath `
-        -Value "$(Get-KmgSha256 -Path $candidatePath)  $([IO.Path]::GetFileName($candidatePath))" `
-        -Encoding ASCII
-}
-
-Write-Host 'Step 7/7: sealing runtime-candidate evidence and test instructions.'
-$modDllPath = Join-Path $repositoryRoot 'artifacts\bin\Release\KingmakerGunslinger\KingmakerGunslinger.dll'
-$manifestPath = Join-Path $repositoryRoot 'blueprints\blueprints.json'
-$domainTestPath = Join-Path $repositoryRoot 'artifacts\tests\Release\KingmakerGunslinger.DomainTests\KingmakerGunslinger.DomainTests.exe'
-foreach ($required in @($modDllPath, $manifestPath, $domainTestPath)) {
-    if (-not (Test-Path -LiteralPath $required -PathType Leaf)) {
-        throw "Qualification artifact is missing: $required"
-    }
-}
+$candidateSha256 = Get-KmgSha256 -Path $candidatePath
+Set-Content -LiteralPath $candidateChecksumPath -Value "$candidateSha256  $candidateName" -Encoding ASCII
 
+Write-Host 'Step 8/8: sealing qualification evidence and instructions.'
+$blueprintManifestPath = Join-Path $repositoryRoot 'blueprints\blueprints.json'
 $qualification = [ordered]@{
     schemaVersion = 1
     createdAtUtc = [DateTime]::UtcNow.ToString('o')
     modId = $info.Id
     modVersion = $info.Version
-    informationalVersion = '0.0.18-s18-runtime-smoke-candidate'
+    informationalVersion = '0.0.22.1-s22-attack-hook-repair'
+    classification = 'READY FOR KINGMAKER Sprint 22 repair smoke test; runtime acceptance pending'
     readyForKingmakerSmokeTest = $true
-    persistenceGateDecision = 'NoGoIncomplete'
-    blockedRowsBeforeRuntimeTest = @('I01', 'I02', 'I03-I30')
-    package = [ordered]@{
-        fileName = [IO.Path]::GetFileName($candidatePath)
-        sha256 = Get-KmgSha256 -Path $candidatePath
-        validatedAsUmmPackage = $true
-    }
-    modAssembly = [ordered]@{
-        fileName = [IO.Path]::GetFileName($modDllPath)
-        sha256 = Get-KmgSha256 -Path $modDllPath
-    }
-    domainTests = [ordered]@{
-        executableSha256 = Get-KmgSha256 -Path $domainTestPath
-        passed = $true
-    }
-    blueprintManifestSha256 = Get-KmgSha256 -Path $manifestPath
+    sprint23Blocked = $true
+    targetGame = $DisplayedGameVersion
+    environmentSha256 = Get-KmgSha256 -Path $environmentPath
     runtimeContracts = [ordered]@{
-        fileName = [IO.Path]::GetFileName($contractsPath)
         sha256 = Get-KmgSha256 -Path $contractsPath
         passed = [bool]$contracts.contractPassed
+        requiredRuleEventSignature = 'System.Void OnTrigger(Kingmaker.RuleSystem.RulebookEventContext)'
+        authoritativeStateCarrier = 'item-owned inert BlueprintWeaponEnchantment token'
+        uniqueIdVaultRequired = $false
     }
-    environment = [ordered]@{
-        fileName = [IO.Path]::GetFileName($environmentPath)
-        sha256 = Get-KmgSha256 -Path $environmentPath
-        containsLocalInstallPath = $true
+    domainTests = [ordered]@{
+        declaredAndExecuted = 455
+        runs = 3
+        failures = 0
+        repeatedOutputIdentical = $true
+        executableSha256 = Get-KmgSha256 -Path $testExecutable
+        outputSha256 = $testOutputHashes[0]
+    }
+    compile = [ordered]@{
+        configuration = 'Release'
+        targetFramework = '.NET Framework 4.7'
+        languageVersion = '7.3'
+        warningsAsErrors = $true
+        sameOutputPathDeterministic = $true
+        modDllSha256 = $secondDllHash
     }
-    firstRuntimeActions = @(
-        'Install the UMM ZIP from this qualification directory.',
-        'Use a disposable campaign and disable unrelated gameplay mods.',
-        'Start a new evidence session in the UMM panel.',
-        'Record the trusted I01/I02 runtime preflight.',
-        'For I03, capture BEFORE, create the A-D fixture, then record PASS only after the AFTER snapshot verifies four distinct engine IDs and the expected states.'
-    )
+    package = [ordered]@{
+        fileName = $candidateName
+        sha256 = $candidateSha256
+        validatedAsStrictStandaloneUmmPackage = $true
+        entryCount = 8
+        binaryCount = 1
+    }
+    blueprintManifestSha256 = Get-KmgSha256 -Path $blueprintManifestPath
+    runtimeAcceptance = 'Pending the complete SMOKE-TEST-GUIDE-0.0.22.1.md gate; do not begin Sprint 23 yet.'
 }
 $qualification | ConvertTo-Json -Depth 8 | Set-Content -LiteralPath $qualificationJsonPath -Encoding UTF8
-$packageSha256 = [string]$qualification['package']['sha256']
-$modAssemblySha256 = [string]$qualification['modAssembly']['sha256']
-$blueprintManifestSha256 = [string]$qualification['blueprintManifestSha256']
-$runtimeContractsPassed = [bool]$qualification['runtimeContracts']['passed']
-
-$markdown = @"
-# Kingmaker Gunslinger runtime candidate
 
-> **READY FOR KINGMAKER SMOKE TEST — INSTALL ``$([IO.Path]::GetFileName($candidatePath))`` THROUGH UNITY MOD MANAGER**
+Copy-Item -LiteralPath (Join-Path $repositoryRoot 'SMOKE-TEST-GUIDE-0.0.22.1.md') -Destination $qualificationRoot
+Copy-Item -LiteralPath (Join-Path $repositoryRoot 'SPRINT-22-REPAIR-REPORT-0.0.22.1.md') -Destination $qualificationRoot
 
-This candidate was compiled against the installed Kingmaker, Unity Mod Manager, and Harmony assemblies recorded in the adjacent JSON files. It passed the dependency-free C# test executable, runtime-contract inspection, build-output validation, and UMM-package validation.
-
-It is **not** a persistence GO result. The lifecycle matrix remains `NoGoIncomplete` until all Critical rows pass in the game.
+$markdown = @"
+# Kingmaker Gunslinger 0.0.22.1 runtime candidate
 
-## Exact artifacts
+> **READY FOR KINGMAKER — INSTALL ``$candidateName`` THROUGH UNITY MOD MANAGER**
 
-- UMM package SHA-256: ``$packageSha256``
-- Mod DLL SHA-256: ``$modAssemblySha256``
-- Blueprint manifest SHA-256: ``$blueprintManifestSha256``
-- Runtime contracts passed: ``$runtimeContractsPassed``
+This standalone package passed source validation, the current installed-runtime contract inspection, 455 tests three times with zero failures and byte-identical output, two same-output-path deterministic Release compiles, build-output validation, and strict eight-file UMM-package validation.
 
-## First in-game sequence
+It is not yet runtime-accepted. Use only a disposable campaign and follow ``SMOKE-TEST-GUIDE-0.0.22.1.md``. Sprint 23 remains blocked until the complete live gate passes with no KMG or Harmony fault.
 
-1. Install the UMM ZIP from this directory.
-2. Use a disposable campaign and disable unrelated gameplay mods.
-3. Launch Kingmaker and open the mod's UMM options panel.
-4. Start a new evidence session.
-5. Select **Record trusted I01/I02 runtime preflight**.
-6. At I03, capture BEFORE, run **Create/normalize A-D persistence fixture**, and record PASS only after the AFTER snapshot verifies the required four item identities and states.
-7. Preserve the generated `evidence/` files and Kingmaker logs.
+- Package SHA-256: ``$candidateSha256``
+- Mod DLL SHA-256: ``$secondDllHash``
+- Test output SHA-256: ``$($testOutputHashes[0])``
+- Runtime-contract report SHA-256: ``$(Get-KmgSha256 -Path $contractsPath)``
 
-The adjacent `environment.json` contains the local installation path and should be reviewed before sharing publicly.
+The adjacent ``environment.json`` contains the local installation path and should not be published without review. No private game, Unity, UMM, Harmony, or Newtonsoft assembly is included in the standalone ZIP.
 "@
 Set-Content -LiteralPath $qualificationMarkdownPath -Value $markdown -Encoding UTF8
 
@@ -205,7 +221,7 @@ Set-Content -LiteralPath $bundleChecksumPath `
     -Encoding ASCII
 
 Write-Host ''
-Write-Host 'READY FOR KINGMAKER SMOKE TEST'
+Write-Host 'READY FOR KINGMAKER'
 Write-Host "Install through Unity Mod Manager: $candidatePath"
 Write-Host "Qualification bundle: $bundlePath"
-Write-Host 'Persistence remains NO-GO / incomplete until the in-game lifecycle matrix passes.'
+Write-Host 'Runtime acceptance remains pending; Sprint 23 is still blocked.'
diff --git a/scripts/validate-package.ps1 b/scripts/validate-package.ps1
index 6c45bbe25c1521ebf9a3c0e36fa400e68c3cf284..67a9e6e24dd6293c1e69b8b20e1b5e01aaff9871 100644
--- a/scripts/validate-package.ps1
+++ b/scripts/validate-package.ps1
@@ -26,35 +26,55 @@ try {
     }
 
     $modDirectory = $roots[0].FullName
-    $required = @(
-        'KingmakerGunslinger.dll',
+    $expected = @(
+        'CHANGELOG.md',
         'Info.json',
+        'KingmakerGunslinger.dll',
+        'LICENSE',
+        'README.md',
+        'SMOKE-TEST-GUIDE.md',
         'blueprints\blueprints.json',
         'blueprints\blueprints.schema.json'
     )
-    foreach ($relativePath in $required) {
-        if (-not (Test-Path -LiteralPath (Join-Path $modDirectory $relativePath) -PathType Leaf)) {
-            throw "Package is missing required file: $relativePath"
-        }
+    $actual = @(
+        Get-ChildItem -LiteralPath $modDirectory -Recurse -File |
+            ForEach-Object { $_.FullName.Substring($modDirectory.Length).TrimStart('\', '/') } |
+            Sort-Object
+    )
+    $expectedSorted = @($expected | Sort-Object)
+    if (($actual -join "`n") -ne ($expectedSorted -join "`n")) {
+        throw "Package entries do not match the strict eight-file allowlist.`nExpected:`n$($expectedSorted -join [Environment]::NewLine)`nActual:`n$($actual -join [Environment]::NewLine)"
     }
 
-    $allowed = @{
-        'KingmakerGunslinger.dll' = $true
-        'KingmakerGunslinger.pdb' = $true
-        'Info.json' = $true
-        'blueprints\blueprints.json' = $true
-        'blueprints\blueprints.schema.json' = $true
+    $packagedInfo = Get-Content -LiteralPath (Join-Path $modDirectory 'Info.json') -Raw | ConvertFrom-Json
+    if ($packagedInfo.Id -ne $info.Id -or $packagedInfo.Version -ne $info.Version) {
+        throw 'Packaged Info.json does not match the repository mod ID and version.'
     }
-    foreach ($file in Get-ChildItem -LiteralPath $modDirectory -Recurse -File) {
-        $relativePath = $file.FullName.Substring($modDirectory.Length).TrimStart('\', '/')
-        if (-not $allowed.ContainsKey($relativePath)) {
-            throw "Unexpected file in install package: $relativePath"
-        }
+    if ($packagedInfo.AssemblyName -ne 'KingmakerGunslinger.dll') {
+        throw 'Packaged Info.json names an unexpected mod assembly.'
     }
 
-    $foreignDlls = @(Get-ChildItem -LiteralPath $modDirectory -Recurse -File -Filter '*.dll' | Where-Object { $_.Name -ne 'KingmakerGunslinger.dll' })
-    if ($foreignDlls.Count -gt 0) {
-        throw "Install package contains a non-project DLL: $($foreignDlls[0].Name)"
+    $binaryFiles = @(
+        Get-ChildItem -LiteralPath $modDirectory -Recurse -File |
+            Where-Object { $_.Extension -in @('.dll', '.exe', '.pdb', '.mdb') }
+    )
+    if ($binaryFiles.Count -ne 1 -or $binaryFiles[0].Name -ne 'KingmakerGunslinger.dll') {
+        throw 'The standalone UMM package must contain exactly one binary: KingmakerGunslinger.dll.'
+    }
+
+    $forbiddenNames = @(
+        '0Harmony.dll',
+        '0Harmony12.dll',
+        'Assembly-CSharp.dll',
+        'Assembly-CSharp-firstpass.dll',
+        'Newtonsoft.Json.dll',
+        'UnityEngine.dll',
+        'UnityModManager.dll'
+    )
+    foreach ($name in $forbiddenNames) {
+        if (Get-ChildItem -LiteralPath $modDirectory -Recurse -File -Filter $name -ErrorAction SilentlyContinue) {
+            throw "Install package contains a private or foreign runtime assembly: $name"
+        }
     }
 }
 finally {
@@ -63,4 +83,4 @@ finally {
     }
 }
 
-Write-Host "Install package validation passed: $PackagePath"
+Write-Host "Strict standalone UMM package validation passed: $PackagePath"
diff --git a/scripts/validate-repository.ps1 b/scripts/validate-repository.ps1
index d4650e6c2e784be703bf06298f4101b897979da3..d037d3c7e517d53b36c6501bedeae680984a6d9a 100644
--- a/scripts/validate-repository.ps1
+++ b/scripts/validate-repository.ps1
@@ -6,9 +6,9 @@ $ErrorActionPreference = 'Stop'
 
 . (Join-Path $PSScriptRoot 'common.ps1')
 $repositoryRoot = Get-KmgRepositoryRoot -ScriptDirectory $PSScriptRoot
-$validator = Join-Path $repositoryRoot 'tools\validate_sprint22.py'
+$validator = Join-Path $repositoryRoot 'tools\validate_sprint22_repair.py'
 if (-not (Test-Path -LiteralPath $validator -PathType Leaf)) {
-    throw "Sprint 22 validator is missing: $validator"
+    throw "Sprint 22 repair validator is missing: $validator"
 }
 
 $python = Get-Command python -ErrorAction SilentlyContinue
@@ -16,12 +16,12 @@ if ($null -eq $python) {
     $python = Get-Command python3 -ErrorAction SilentlyContinue
 }
 if ($null -eq $python) {
-    throw 'Python 3 is required to run tools\validate_sprint22.py.'
+    throw 'Python 3 is required to run tools\validate_sprint22_repair.py.'
 }
 
 & $python.Source $validator
 if ($LASTEXITCODE -ne 0) {
-    throw "Sprint 22 portable validation failed with exit code $LASTEXITCODE."
+    throw "Sprint 22 repair portable validation failed with exit code $LASTEXITCODE."
 }
 
-Write-Host 'Sprint 22 repository validation passed.'
+Write-Host 'Sprint 22 repair repository validation passed.'
diff --git a/src/KingmakerGunslinger/Development/DevelopmentUi.cs b/src/KingmakerGunslinger/Development/DevelopmentUi.cs
index e877d540633c77923193f69c93b00dafa303009d..d06061279180fc489b4836d3b24cab3118a4ce02 100644
--- a/src/KingmakerGunslinger/Development/DevelopmentUi.cs
+++ b/src/KingmakerGunslinger/Development/DevelopmentUi.cs
@@ -10,8 +10,8 @@ using UnityModManagerNet;
 namespace KingmakerGunslinger.Development
 {
     /// <summary>
-    /// Unity Mod Manager panel for the Sprint 22 loaded-round attack enforcement
-    /// and native enchantment-reconciliation durability smoke test.
+    /// Unity Mod Manager panel for the Sprint 22 exact attack-hook repair and
+    /// weapon-only native enchantment-reconciliation smoke test.
     /// </summary>
     internal static class DevelopmentUi
     {
@@ -32,7 +32,7 @@ namespace KingmakerGunslinger.Development
         private static void OnGui(UnityModManager.ModEntry modEntry)
         {
             ImmediateModeGui.Label(
-                "Kingmaker Gunslinger - Sprint 22 loaded-round attack smoke test");
+                "Kingmaker Gunslinger - 0.0.22.1 Sprint 22 attack-hook repair");
             ImmediateModeGui.Label(
                 "Use only a disposable campaign. The Test Musket still displays as a Heavy Crossbow and ammunition still uses placeholder Diamond Dust artwork.");
             ImmediateModeGui.Label(
@@ -46,7 +46,7 @@ namespace KingmakerGunslinger.Development
             }
 
             ImmediateModeGui.Label(
-                "Sprint 21 evidence: the full-round reload ability consumed one powder charge and one Lead Ball and loaded the exact equipped Test Musket. Sprint 22 preserves that token through quicksave refresh and consumes one loaded round when a firearm attack roll begins.");
+                "Sprint 21 evidence proved the full-round reload transaction. Version 0.0.22 proved quicksave token preservation but missed the installed rule-event signature. Version 0.0.22.1 binds OnTrigger(RulebookEventContext), consumes one loaded round when the exact firearm attack roll begins, and excludes non-weapons from token inspection.");
 
             bool tracingWasEnabled = CombatTraceSettings.Enabled;
             bool tracingIsEnabled = ImmediateModeGui.Toggle(
diff --git a/src/KingmakerGunslinger/Diagnostics/RuleEventPatchContract.cs b/src/KingmakerGunslinger/Diagnostics/RuleEventPatchContract.cs
new file mode 100644
index 0000000000000000000000000000000000000000..f5a1743513c688fcb4ce4c1fb925675da3dabd5b
--- /dev/null
+++ b/src/KingmakerGunslinger/Diagnostics/RuleEventPatchContract.cs
@@ -0,0 +1,36 @@
+using System;
+using System.Reflection;
+
+namespace KingmakerGunslinger.Diagnostics
+{
+    /// <summary>
+    /// Defines the exact Kingmaker rule-event callback shape accepted by Harmony
+    /// patch target resolution. Keeping the reflection predicate independent of
+    /// Kingmaker types makes the contract executable in the dependency-free suite.
+    /// </summary>
+    internal static class RuleEventPatchContract
+    {
+        internal static bool IsCompatibleOnTrigger(
+            MethodInfo method,
+            Type eventContextType)
+        {
+            if (eventContextType == null)
+            {
+                throw new ArgumentNullException("eventContextType");
+            }
+
+            if (method == null ||
+                method.IsStatic ||
+                method.IsGenericMethodDefinition ||
+                !string.Equals(method.Name, "OnTrigger", StringComparison.Ordinal) ||
+                method.ReturnType != typeof(void))
+            {
+                return false;
+            }
+
+            ParameterInfo[] parameters = method.GetParameters();
+            return parameters.Length == 1 &&
+                parameters[0].ParameterType == eventContextType;
+        }
+    }
+}
diff --git a/src/KingmakerGunslinger/Diagnostics/RuleEventPatchTarget.cs b/src/KingmakerGunslinger/Diagnostics/RuleEventPatchTarget.cs
index 782a03eac18ed3b5afca9dc3143728354a8e6f02..6b2b7dfee67365314f0055f474d5de6040647c51 100644
--- a/src/KingmakerGunslinger/Diagnostics/RuleEventPatchTarget.cs
+++ b/src/KingmakerGunslinger/Diagnostics/RuleEventPatchTarget.cs
@@ -3,14 +3,15 @@ using System.Globalization;
 using System.Linq;
 using System.Reflection;
 using Kingmaker.Blueprints;
+using Kingmaker.RuleSystem;
 using KingmakerGunslinger.Bootstrap;
 
 namespace KingmakerGunslinger.Diagnostics
 {
     /// <summary>
-    /// Resolves a zero-argument instance OnTrigger method from the installed
-    /// Assembly-CSharp. Missing or ambiguous contracts disable the affected patch
-    /// instead of binding a plausible but unverified overload.
+    /// Resolves the exact one-argument rule-event OnTrigger callback from the
+    /// installed Assembly-CSharp. Missing or ambiguous contracts disable the affected
+    /// patch instead of binding a plausible but unverified overload.
     /// </summary>
     internal static class RuleEventPatchTarget
     {
@@ -38,7 +39,9 @@ namespace KingmakerGunslinger.Diagnostics
                     BindingFlags.Public |
                     BindingFlags.NonPublic |
                     BindingFlags.DeclaredOnly)
-                    .Where(IsCompatibleOnTrigger)
+                    .Where(method => RuleEventPatchContract.IsCompatibleOnTrigger(
+                        method,
+                        typeof(RulebookEventContext)))
                     .ToArray();
 
                 if (candidates.Length != 1)
@@ -48,7 +51,7 @@ namespace KingmakerGunslinger.Diagnostics
                         purpose,
                         string.Format(
                             CultureInfo.InvariantCulture,
-                            "expected one zero-argument instance OnTrigger method; found {0}",
+                            "expected one void instance OnTrigger(RulebookEventContext) method; found {0}",
                             candidates.Length));
                     return false;
                 }
@@ -62,7 +65,7 @@ namespace KingmakerGunslinger.Diagnostics
                         "patch.target",
                         string.Format(
                             CultureInfo.InvariantCulture,
-                            "Resolved {0} target {1}.{2}().",
+                            "Resolved {0} target {1}.{2}(RulebookEventContext).",
                             purpose,
                             type.FullName,
                             candidates[0].Name));
@@ -90,15 +93,6 @@ namespace KingmakerGunslinger.Diagnostics
             }
         }
 
-        private static bool IsCompatibleOnTrigger(MethodInfo method)
-        {
-            return method != null &&
-                !method.IsStatic &&
-                !method.IsGenericMethodDefinition &&
-                string.Equals(method.Name, "OnTrigger", StringComparison.Ordinal) &&
-                method.GetParameters().Length == 0;
-        }
-
         private static void LogUnavailable(
             string typeName,
             string purpose,
diff --git a/src/KingmakerGunslinger/Firearms/FirearmStateTokenReconciliationPatch.cs b/src/KingmakerGunslinger/Firearms/FirearmStateTokenReconciliationPatch.cs
index 04d748954d0239db358eac38302b3c8390c7d55d..2d80085b92c92561e5f8e076a1fd710ad6662f14 100644
--- a/src/KingmakerGunslinger/Firearms/FirearmStateTokenReconciliationPatch.cs
+++ b/src/KingmakerGunslinger/Firearms/FirearmStateTokenReconciliationPatch.cs
@@ -9,9 +9,10 @@ using KingmakerGunslinger.Bootstrap;
 namespace KingmakerGunslinger.Firearms
 {
     /// <summary>
-    /// Surrounds the exact zero-argument ItemEntity.ApplyEnchantments method. Native
-    /// Kingmaker removes dynamic enchantments with null ParentContext during this pass;
-    /// the postfix restores the one known state token captured by the prefix.
+    /// Surrounds the exact zero-argument ItemEntity.ApplyEnchantments method. Only
+    /// weapon items enter state-token inspection. Native Kingmaker removes dynamic
+    /// enchantments with null ParentContext during this pass; the postfix restores the
+    /// one known firearm-state token captured by the prefix.
     /// </summary>
     [HarmonyPatch]
     internal static class FirearmStateTokenReconciliationPatch
@@ -63,7 +64,10 @@ namespace KingmakerGunslinger.Firearms
             object __instance,
             out FirearmStateTokenReconciliationInvocation __state)
         {
-            __state = FirearmStateTokenReconciliationRuntime.Before(__instance);
+            ItemEntityWeapon weapon = __instance as ItemEntityWeapon;
+            __state = weapon == null
+                ? FirearmStateTokenReconciliationInvocation.Empty
+                : FirearmStateTokenReconciliationRuntime.Before(weapon);
         }
 
         private static void Postfix(
diff --git a/src/KingmakerGunslinger/KingmakerGunslinger.csproj b/src/KingmakerGunslinger/KingmakerGunslinger.csproj
index cb3572d3651cd38b1666e3c14e5c5aa9014ce9a4..419e2568593c8757f7d5864dd95f123716b659ce 100644
--- a/src/KingmakerGunslinger/KingmakerGunslinger.csproj
+++ b/src/KingmakerGunslinger/KingmakerGunslinger.csproj
@@ -135,6 +135,7 @@
     <Compile Include="Diagnostics\FirearmMarkerLookup.cs" />
     <Compile Include="Diagnostics\FirearmMarkerSnapshot.cs" />
     <Compile Include="Diagnostics\FirearmRangeMath.cs" />
+    <Compile Include="Diagnostics\RuleEventPatchContract.cs" />
     <Compile Include="Diagnostics\RuleEventPatchTarget.cs" />
     <Compile Include="Diagnostics\RuleEventSnapshotReader.cs" />
     <Compile Include="Firearms\AmmunitionId.cs" />
diff --git a/src/KingmakerGunslinger/Main.cs b/src/KingmakerGunslinger/Main.cs
index b1f0be190e238791378c224be61df7eadad7459d..667f69f1f5cac906f501bcb53dbb5dc985bb755b 100644
--- a/src/KingmakerGunslinger/Main.cs
+++ b/src/KingmakerGunslinger/Main.cs
@@ -7,10 +7,10 @@ using UnityModManagerNet;
 namespace KingmakerGunslinger
 {
     /// <summary>
-    /// Unity Mod Manager composition root. Sprint 22 retains the guarded lifecycle,
+    /// Unity Mod Manager composition root. The Sprint 22 repair retains the guarded lifecycle,
     /// Test Musket slice, range-limited touch-AC rule, proven item-token carrier,
     /// real ammunition components, and the full-round reload ability. It adds native
-    /// token-reconciliation durability and loaded-round enforcement for ordinary attacks.
+    /// token-reconciliation durability and repairs the exact rule-event binding used by loaded-round enforcement.
     /// </summary>
     public static class Main
     {
@@ -84,14 +84,14 @@ namespace KingmakerGunslinger
                 logger.Info(
                     "development",
                     "ui.attached",
-                    "Attached Sprint 22 proficiency, item-token persistence, ammunition, full-round reload, loaded-round attack enforcement, token-reconciliation diagnostics, and disabled-by-default firearm combat-trace controls to the Unity Mod Manager options panel.");
+                    "Attached Sprint 22 repair controls for proficiency, item-token persistence, ammunition, full-round reload, loaded-round attack enforcement, weapon-only token reconciliation, and disabled-by-default firearm combat tracing.");
 
                 lock (LoadGate)
                 {
                     _state = LoaderState.Loaded;
                 }
 
-                logger.Info("bootstrap", "load.complete", "Lifecycle bootstrap completed; Sprint 22 registered eleven custom blueprints, retained range-limited touch AC, Firearm Proficiency, ammunition, and full-round reload, guarded item-owned state tokens across native enchantment reconciliation, and enforced one loaded round per marked-firearm attack roll. Empty or Wrecked marked firearms are forced to miss. Native Heavy Crossbows remain unaffected.");
+                logger.Info("bootstrap", "load.complete", "Lifecycle bootstrap completed; the Sprint 22 repair registered eleven custom blueprints, retained range-limited touch AC, Firearm Proficiency, ammunition, and full-round reload, guarded item-owned state tokens across weapon-only native enchantment reconciliation, and bound the exact OnTrigger(RulebookEventContext) rule-event hooks used to enforce one loaded round per marked-firearm attack roll. Empty or Wrecked marked firearms are forced to miss. Native Heavy Crossbows remain unaffected.");
                 return true;
             }
             catch (Exception exception)
diff --git a/src/KingmakerGunslinger/Properties/AssemblyInfo.cs b/src/KingmakerGunslinger/Properties/AssemblyInfo.cs
index bfd210856a795699b066f96c8022884ad37d526c..ef41235b419f12c20e110b59d5b217fd06c92cf2 100644
--- a/src/KingmakerGunslinger/Properties/AssemblyInfo.cs
+++ b/src/KingmakerGunslinger/Properties/AssemblyInfo.cs
@@ -11,6 +11,6 @@ using System.Runtime.InteropServices;
 [assembly: AssemblyCulture("")]
 [assembly: ComVisible(false)]
 [assembly: Guid("8668dd56-63f3-4868-8978-5e8f00120fc3")]
-[assembly: AssemblyVersion("0.0.22.0")]
-[assembly: AssemblyFileVersion("0.0.22.0")]
-[assembly: AssemblyInformationalVersion("0.0.22-s22-loaded-round-enforcement")]
+[assembly: AssemblyVersion("0.0.22.1")]
+[assembly: AssemblyFileVersion("0.0.22.1")]
+[assembly: AssemblyInformationalVersion("0.0.22.1-s22-attack-hook-repair")]
diff --git a/tests/KingmakerGunslinger.DomainTests/KingmakerGunslinger.DomainTests.csproj b/tests/KingmakerGunslinger.DomainTests/KingmakerGunslinger.DomainTests.csproj
index 815cc8f447972e1b2550e5210142069d439df94d..f0d3ef9db871389d505643225bfb07fb0999bc04 100644
--- a/tests/KingmakerGunslinger.DomainTests/KingmakerGunslinger.DomainTests.csproj
+++ b/tests/KingmakerGunslinger.DomainTests/KingmakerGunslinger.DomainTests.csproj
@@ -125,6 +125,9 @@
     <Compile Include="..\..\src\KingmakerGunslinger\Diagnostics\FirearmRangeMath.cs">
       <Link>Diagnostics\FirearmRangeMath.cs</Link>
     </Compile>
+    <Compile Include="..\..\src\KingmakerGunslinger\Diagnostics\RuleEventPatchContract.cs">
+      <Link>Diagnostics\RuleEventPatchContract.cs</Link>
+    </Compile>
     <Compile Include="..\..\src\KingmakerGunslinger\Rules\FirearmArmorClassDecision.cs">
       <Link>Rules\FirearmArmorClassDecision.cs</Link>
     </Compile>
diff --git a/tests/KingmakerGunslinger.DomainTests/Program.cs b/tests/KingmakerGunslinger.DomainTests/Program.cs
index 3e09990cd798f787001c72b1a56149756938135d..c0279379a8f36a6f07b3c39a9eefbe4d05f56887 100644
--- a/tests/KingmakerGunslinger.DomainTests/Program.cs
+++ b/tests/KingmakerGunslinger.DomainTests/Program.cs
@@ -120,6 +120,15 @@ namespace KingmakerGunslinger.DomainTests
             Case("event-gate.reference-identity", EventGateReferenceIdentity),
             Case("event-gate.null", EventGateNull),
             Case("event-gate.value-type", EventGateValueType),
+            Case("patch-target.exact-contract", PatchTargetExactContract),
+            Case("patch-target.null-method", PatchTargetNullMethodRejected),
+            Case("patch-target.zero-argument", PatchTargetZeroArgumentRejected),
+            Case("patch-target.wrong-context", PatchTargetWrongContextRejected),
+            Case("patch-target.multiple-arguments", PatchTargetMultipleArgumentsRejected),
+            Case("patch-target.static", PatchTargetStaticRejected),
+            Case("patch-target.generic", PatchTargetGenericRejected),
+            Case("patch-target.non-void", PatchTargetNonVoidRejected),
+            Case("patch-target.null-context", PatchTargetNullContextRejected),
             Case("token-reconcile.none", TokenReconcileNoToken),
             Case("token-reconcile.preserved", TokenReconcilePreserved),
             Case("token-reconcile.restore", TokenReconcileRestore),
@@ -1781,6 +1790,87 @@ namespace KingmakerGunslinger.DomainTests
                 "A boxed value-type event identity was accepted.");
         }
 
+        private static void PatchTargetExactContract()
+        {
+            Assertions.True(
+                RuleEventPatchContract.IsCompatibleOnTrigger(
+                    typeof(ExactRuleEvent).GetMethod("OnTrigger"),
+                    typeof(FakeRulebookEventContext)),
+                "The exact Kingmaker OnTrigger contract was rejected.");
+        }
+
+        private static void PatchTargetNullMethodRejected()
+        {
+            Assertions.False(
+                RuleEventPatchContract.IsCompatibleOnTrigger(
+                    null,
+                    typeof(FakeRulebookEventContext)),
+                "A null method was accepted as a patch target.");
+        }
+
+        private static void PatchTargetZeroArgumentRejected()
+        {
+            Assertions.False(
+                RuleEventPatchContract.IsCompatibleOnTrigger(
+                    typeof(ZeroArgumentRuleEvent).GetMethod("OnTrigger"),
+                    typeof(FakeRulebookEventContext)),
+                "The obsolete zero-argument OnTrigger shape was accepted.");
+        }
+
+        private static void PatchTargetWrongContextRejected()
+        {
+            Assertions.False(
+                RuleEventPatchContract.IsCompatibleOnTrigger(
+                    typeof(WrongContextRuleEvent).GetMethod("OnTrigger"),
+                    typeof(FakeRulebookEventContext)),
+                "An OnTrigger callback with the wrong context type was accepted.");
+        }
+
+        private static void PatchTargetMultipleArgumentsRejected()
+        {
+            Assertions.False(
+                RuleEventPatchContract.IsCompatibleOnTrigger(
+                    typeof(MultipleArgumentRuleEvent).GetMethod("OnTrigger"),
+                    typeof(FakeRulebookEventContext)),
+                "A multi-argument OnTrigger callback was accepted.");
+        }
+
+        private static void PatchTargetStaticRejected()
+        {
+            Assertions.False(
+                RuleEventPatchContract.IsCompatibleOnTrigger(
+                    typeof(StaticRuleEvent).GetMethod("OnTrigger"),
+                    typeof(FakeRulebookEventContext)),
+                "A static OnTrigger callback was accepted.");
+        }
+
+        private static void PatchTargetGenericRejected()
+        {
+            Assertions.False(
+                RuleEventPatchContract.IsCompatibleOnTrigger(
+                    typeof(GenericRuleEvent).GetMethod("OnTrigger"),
+                    typeof(FakeRulebookEventContext)),
+                "A generic OnTrigger callback was accepted.");
+        }
+
+        private static void PatchTargetNonVoidRejected()
+        {
+            Assertions.False(
+                RuleEventPatchContract.IsCompatibleOnTrigger(
+                    typeof(NonVoidRuleEvent).GetMethod("OnTrigger"),
+                    typeof(FakeRulebookEventContext)),
+                "A non-void OnTrigger callback was accepted.");
+        }
+
+        private static void PatchTargetNullContextRejected()
+        {
+            Assertions.Throws<ArgumentNullException>(
+                () => RuleEventPatchContract.IsCompatibleOnTrigger(
+                    typeof(ExactRuleEvent).GetMethod("OnTrigger"),
+                    null),
+                "A null rule-event context type was accepted.");
+        }
+
         private static FirearmStateTokenReconciliationService Reconciler()
         {
             return new FirearmStateTokenReconciliationService();
@@ -6266,6 +6356,66 @@ namespace KingmakerGunslinger.DomainTests
             }
         }
 
+        private sealed class FakeRulebookEventContext
+        {
+        }
+
+        private sealed class DifferentRulebookEventContext
+        {
+        }
+
+        private sealed class ExactRuleEvent
+        {
+            public void OnTrigger(FakeRulebookEventContext context)
+            {
+            }
+        }
+
+        private sealed class ZeroArgumentRuleEvent
+        {
+            public void OnTrigger()
+            {
+            }
+        }
+
+        private sealed class WrongContextRuleEvent
+        {
+            public void OnTrigger(DifferentRulebookEventContext context)
+            {
+            }
+        }
+
+        private sealed class MultipleArgumentRuleEvent
+        {
+            public void OnTrigger(
+                FakeRulebookEventContext context,
+                object extra)
+            {
+            }
+        }
+
+        private sealed class StaticRuleEvent
+        {
+            public static void OnTrigger(FakeRulebookEventContext context)
+            {
+            }
+        }
+
+        private sealed class GenericRuleEvent
+        {
+            public void OnTrigger<T>(FakeRulebookEventContext context)
+            {
+            }
+        }
+
+        private sealed class NonVoidRuleEvent
+        {
+            public int OnTrigger(FakeRulebookEventContext context)
+            {
+                return 0;
+            }
+        }
+
         private sealed class ValueEqualItem
         {
             private readonly int _value;
diff --git a/tools/README.md b/tools/README.md
index 23909e76ff539041628da0136af594c4f7d2ac47..bc545942f65bea9d2a516a72bd16ed64eb96a78a 100644
--- a/tools/README.md
+++ b/tools/README.md
@@ -1,9 +1,9 @@
 # Portable tools
 
-- `validate_sprint17.py` validates the current Sprint 17 source, stable IDs, 373 declared tests, retained executed evidence, regression fixes, reference-export boundary, documentation, syntax, and independent models.
+- `validate_sprint22_repair.py` validates the current 0.0.22.1 repair source, stable IDs, exact `RulebookEventContext` patch contract, weapon-only reconciliation guard, 455 declared tests, runtime evidence, and private-reference boundary.
+- `validate_sprint22_repair_package.py` validates the standalone 0.0.22.1 Unity Mod Manager ZIP, including version, layout, blueprint ledger, DLL hash, path safety, CRC, and the one-project-binary rule.
 - `run_exact_net47_domain_tests.py` compiles the explicit classic test-project sources against .NET Framework 4.7 reference assemblies, executes the resulting IL repeatedly on a supplied `dotnet` runtime, and writes hash-backed evidence.
-- `run_portable_domain_tests.py` remains a simpler .NET 8 SDK convenience runner.
-- `build_mod_from_private_references.py` compiles a UMM-shaped candidate after an extracted private Kingmaker/UMM reference bundle is supplied. Its output is a compile candidate, not runtime qualification.
-- `validate_sprint16.py` is retained only to reproduce the preceding source milestone.
+- `build_mod_from_private_references.py` compiles a UMM-shaped candidate after an extracted private Kingmaker/UMM reference bundle is supplied. Its output is a compile candidate until Kingmaker runtime acceptance passes.
+- Earlier sprint validators remain available to reproduce their historical source milestones.
 
-None of these tools redistributes game, Unity, Harmony, or Unity Mod Manager assemblies.
+None of these tools redistributes game, Unity, Harmony, Newtonsoft.Json, or Unity Mod Manager assemblies.
diff --git a/tools/build_mod_from_private_references.py b/tools/build_mod_from_private_references.py
index 7b6dbece9eb2441223a2212a8a29b1a9f7be473d..dc447f9ecfee2db4df85d104f910291895f35662 100644
--- a/tools/build_mod_from_private_references.py
+++ b/tools/build_mod_from_private_references.py
@@ -135,7 +135,7 @@ def main() -> int:
 
     report = {
         "schemaVersion": 1,
-        "classification": "ready-for-loaded-round-and-token-durability-smoke-test-after-exact-reference-compile",
+        "classification": "ready-for-sprint22-attack-hook-repair-smoke-test-after-exact-reference-compile",
         "configuration": args.configuration,
         "modVersion": info["Version"],
         "compileExitCode": completed.returncode,
@@ -144,7 +144,7 @@ def main() -> int:
         "blueprintManifestSha256": sha256(root / "blueprints" / "blueprints.json"),
         "privateReferenceHashes": {path.relative_to(bundle).as_posix(): sha256(path) for path in private_refs},
         "readyForKingmakerSmokeTest": True,
-        "persistenceGateDecision": "CoreItemTokenSaveRestartPassed_ExtendedLifecycleIncomplete",
+        "persistenceGateDecision": "Sprint23Blocked_PendingAttackHookRepairRuntimeGate",
     }
     (output / "compile-candidate.json").write_text(json.dumps(report, indent=2, sort_keys=True) + "\n", encoding="utf-8")
     (output / (zip_path.name + ".sha256")).write_text(f"{report['packageSha256']}  {zip_path.name}\n", encoding="ascii")
diff --git a/tools/validate_sprint22_repair.py b/tools/validate_sprint22_repair.py
new file mode 100755
index 0000000000000000000000000000000000000000..b81050d6efca5b7d201949be9833c0a5f53d8439
--- /dev/null
+++ b/tools/validate_sprint22_repair.py
@@ -0,0 +1,350 @@
+#!/usr/bin/env python3
+"""Portable source invariant validator for the 0.0.22.1 Sprint 22 repair."""
+from __future__ import annotations
+
+import hashlib
+import json
+import re
+import sys
+import xml.etree.ElementTree as ET
+from pathlib import Path
+
+ROOT = Path(__file__).resolve().parents[1]
+MSBUILD_NS = {"m": "http://schemas.microsoft.com/developer/msbuild/2003"}
+
+EXPECTED_SCREENSHOTS = {
+    "01-empty-ready-to-reload.png": "ebcb4ba75ec3c44ca53a86ecbb6fbec10464444f3af610b7415932b5376d7844",
+    "02-loaded-after-quicksave.png": "ef23645bf52e3b272226c197a49a0f2f21acbe591ba5690e876b513b42c562d6",
+    "03-native-heavy-crossbow-pipeline-attack.png": "8e63e998216606b8005ac7ba13032d4cc1a06adea01e69b4b899326613aea529",
+    "04-post-attack-loaded-state-and-shield-faults.png": "b3e9fbd6816dfdebd6001fdf11ca6d7a814e2145fc266f472dc3f85773d8c828",
+}
+
+
+def fail(message: str) -> None:
+    raise RuntimeError(message)
+
+
+def read(relative: str) -> str:
+    path = ROOT / relative
+    if not path.is_file():
+        fail(f"Required file is missing: {relative}")
+    return path.read_text(encoding="utf-8")
+
+
+def sha256(path: Path) -> str:
+    digest = hashlib.sha256()
+    with path.open("rb") as handle:
+        for chunk in iter(lambda: handle.read(1024 * 1024), b""):
+            digest.update(chunk)
+    return digest.hexdigest()
+
+
+def compile_items(relative: str) -> list[Path]:
+    project = ROOT / relative
+    tree = ET.parse(project)
+    result: list[Path] = []
+    for node in tree.findall(".//m:Compile", MSBUILD_NS):
+        include = node.attrib.get("Include")
+        if not include:
+            continue
+        item = (project.parent / include.replace("\\", "/")).resolve()
+        if not item.is_file():
+            fail(f"Compile item does not exist: {include} -> {item}")
+        result.append(item)
+    if not result or len(result) != len(set(result)):
+        fail(f"Compile items are empty or duplicated in {relative}.")
+    return result
+
+
+def main() -> int:
+    info = json.loads(read("Info.json"))
+    if info.get("Version") != "0.0.22.1":
+        fail("Info.json must declare version 0.0.22.1.")
+    if info.get("ManagerVersion") != "0.32.4":
+        fail("Info.json must retain Unity Mod Manager 0.32.4.")
+
+    props = read("Directory.Build.props")
+    for required in (
+        "<KmgVersion>0.0.22.1</KmgVersion>",
+        "<KmgInformationalVersion>0.0.22.1-s22-attack-hook-repair</KmgInformationalVersion>",
+        "<LangVersion>7.3</LangVersion>",
+        "<PlatformTarget>AnyCPU</PlatformTarget>",
+        "<Deterministic>true</Deterministic>",
+        "<TreatWarningsAsErrors>true</TreatWarningsAsErrors>",
+    ):
+        if required not in props:
+            fail(f"Directory.Build.props is missing {required}.")
+
+    assembly_info = read("src/KingmakerGunslinger/Properties/AssemblyInfo.cs")
+    for required in (
+        'AssemblyVersion("0.0.22.1")',
+        'AssemblyFileVersion("0.0.22.1")',
+        'AssemblyInformationalVersion("0.0.22.1-s22-attack-hook-repair")',
+    ):
+        if required not in assembly_info:
+            fail(f"Assembly metadata is missing {required}.")
+
+    manifest = json.loads(read("blueprints/blueprints.json"))
+    entries = manifest.get("entries", [])
+    if len(entries) != 12:
+        fail(f"Expected 12 stable blueprint ledger entries; observed {len(entries)}.")
+    active = [entry for entry in entries if entry.get("status") == "active"]
+    reserved = [entry for entry in entries if entry.get("status") == "reserved"]
+    if len(active) != 11 or len(reserved) != 1:
+        fail(
+            f"Expected 11 active and 1 reserved blueprint; observed "
+            f"{len(active)} active and {len(reserved)} reserved."
+        )
+    symbols = [entry.get("symbol") for entry in entries]
+    guids = [entry.get("guid") for entry in entries]
+    if len(set(symbols)) != len(symbols) or len(set(guids)) != len(guids):
+        fail("Blueprint symbols and GUIDs must be unique.")
+    for guid in guids:
+        if not isinstance(guid, str) or re.fullmatch(r"[0-9a-f]{32}", guid) is None or guid == "0" * 32:
+            fail(f"Invalid stable blueprint GUID: {guid!r}")
+
+    main_items = compile_items("src/KingmakerGunslinger/KingmakerGunslinger.csproj")
+    test_items = compile_items("tests/KingmakerGunslinger.DomainTests/KingmakerGunslinger.DomainTests.csproj")
+    main_project = read("src/KingmakerGunslinger/KingmakerGunslinger.csproj")
+    test_project = read("tests/KingmakerGunslinger.DomainTests/KingmakerGunslinger.DomainTests.csproj")
+    for required in (
+        "Diagnostics\\RuleEventPatchContract.cs",
+        "Diagnostics\\RuleEventPatchTarget.cs",
+        "Firearms\\FirearmStateTokenReconciliationPatch.cs",
+        "Firearms\\FirearmStateTokenReconciliationRuntime.cs",
+        "Firing\\FirearmDischargeRuntime.cs",
+    ):
+        if required not in main_project:
+            fail(f"Main project omits {required}.")
+    if "Diagnostics\\RuleEventPatchContract.cs" not in test_project:
+        fail("Domain-test project omits the exact rule-event patch contract helper.")
+
+    contract = read("src/KingmakerGunslinger/Diagnostics/RuleEventPatchContract.cs")
+    for required in (
+        "method.ReturnType != typeof(void)",
+        "parameters.Length == 1",
+        "parameters[0].ParameterType == eventContextType",
+    ):
+        if required not in contract:
+            fail(f"Rule-event contract helper is missing {required}.")
+
+    target = read("src/KingmakerGunslinger/Diagnostics/RuleEventPatchTarget.cs")
+    for required in (
+        "using Kingmaker.RuleSystem;",
+        "BindingFlags.DeclaredOnly",
+        "RuleEventPatchContract.IsCompatibleOnTrigger",
+        "typeof(RulebookEventContext)",
+        "expected one void instance OnTrigger(RulebookEventContext) method",
+    ):
+        if required not in target:
+            fail(f"Rule-event target resolver is missing {required!r}.")
+    if "method.GetParameters().Length == 0" in target or "zero-argument instance OnTrigger" in target:
+        fail("The rejected zero-argument rule-event target assumption was reintroduced.")
+
+    reconcile_patch = read("src/KingmakerGunslinger/Firearms/FirearmStateTokenReconciliationPatch.cs")
+    for required in (
+        'method.Name,\n                        "ApplyEnchantments"',
+        "method.GetParameters().Length == 0",
+        "method.ReturnType == typeof(void)",
+        "FirearmStateTokenReconciliationRuntime.Before",
+        "FirearmStateTokenReconciliationRuntime.After",
+    ):
+        if required not in reconcile_patch:
+            fail(f"Native token-reconciliation patch is missing {required!r}.")
+
+    for required in (
+        "using Kingmaker.Items;",
+        "ItemEntityWeapon weapon = __instance as ItemEntityWeapon;",
+        "weapon == null",
+        "FirearmStateTokenReconciliationInvocation.Empty",
+        "FirearmStateTokenReconciliationRuntime.Before(weapon)",
+    ):
+        if required not in reconcile_patch:
+            fail(f"Weapon-only reconciliation prefix is missing {required!r}.")
+    guard_index = reconcile_patch.find("ItemEntityWeapon weapon = __instance as ItemEntityWeapon;")
+    before_index = reconcile_patch.find("FirearmStateTokenReconciliationRuntime.Before(weapon)")
+    if guard_index < 0 or before_index < 0 or guard_index > before_index:
+        fail("Non-weapon rejection must occur before token inspection.")
+
+    reconcile_runtime = read("src/KingmakerGunslinger/Firearms/FirearmStateTokenReconciliationRuntime.cs")
+    for required in (
+        "RestoreMissingStateToken",
+        "verified.Count != 1",
+        "FirearmStateTokenReconciliationAction.Conflict",
+    ):
+        if required not in reconcile_runtime:
+            fail(f"Token-reconciliation runtime is missing {required!r}.")
+
+    trace_patches = read("src/KingmakerGunslinger/Diagnostics/CombatTracePatches.cs")
+    discharge_index = trace_patches.find("FirearmDischargeRuntime.BeforeAttackRoll")
+    ac_index = trace_patches.find("FirearmArmorClassRuntime.BeforeAttackRoll")
+    if discharge_index < 0 or ac_index < 0 or discharge_index > ac_index:
+        fail("Loaded-round enforcement must run before touch-AC attack-roll observation.")
+
+    discharge_runtime = read("src/KingmakerGunslinger/Firing/FirearmDischargeRuntime.cs")
+    for required in (
+        "RuleAttackRoll attackRoll",
+        "FirearmMarkerLookup.ReadFromRuleEvent",
+        "FirearmRuntimeState.Service.Transition",
+        "attackRoll.AutoHit = false",
+        "attackRoll.AutoMiss = true",
+        "ReferenceEventGate",
+    ):
+        if required not in discharge_runtime:
+            fail(f"Discharge runtime is missing {required}.")
+    if any(term in discharge_runtime for term in ("BasicAmmunition", "BlackPowder", "LeadBall")):
+        fail("Attack-time discharge must not consume inventory ammunition.")
+
+    program = read("tests/KingmakerGunslinger.DomainTests/Program.cs")
+    declarations = re.findall(r'Case\("([^"]+)"\s*,', program)
+    if len(declarations) != 455 or len(set(declarations)) != 455:
+        fail(
+            f"Expected 455 unique test declarations; observed "
+            f"{len(declarations)} total and {len(set(declarations))} unique."
+        )
+    sprint22_cases = [
+        name for name in declarations
+        if name.startswith("discharge.") or name.startswith("event-gate.") or name.startswith("token-reconcile.")
+    ]
+    if len(sprint22_cases) != 27:
+        fail(f"Expected 27 original Sprint 22 cases; observed {len(sprint22_cases)}.")
+    repair_cases = [name for name in declarations if name.startswith("patch-target.")]
+    if len(repair_cases) != 9:
+        fail(f"Expected 9 patch-target repair cases; observed {len(repair_cases)}.")
+
+    inspector = read("scripts/inspect-runtime-contracts.ps1")
+    for required in (
+        "Kingmaker.RuleSystem.RulebookEventContext",
+        "$parameters.Count -eq 1",
+        "$parameters[0].ParameterType.FullName -eq $rulebookEventContextType.FullName",
+        "requiredOnTriggerSignature = 'System.Void OnTrigger(Kingmaker.RuleSystem.RulebookEventContext)'",
+        "$itemApplyEnchantmentsMethods.Count -eq 1",
+        "firearmStateTokenCarrier = [ordered]@{",
+        "requiredForCurrentBuild = $false",
+    ):
+        if required not in inspector:
+            fail(f"Runtime-contract inspector is missing {required!r}.")
+    contract_gate_start = inspector.find("$contractPassed = (")
+    contract_gate_end = inspector.find("\n    )", contract_gate_start)
+    contract_gate = inspector[contract_gate_start:contract_gate_end]
+    if contract_gate_start < 0 or contract_gate_end < 0:
+        fail("Could not locate the runtime-contract gate.")
+    for rejected_gate in ("$firearmItemIdentityContractPassed", "$unitPartVaultContractPassed"):
+        if rejected_gate in contract_gate:
+            fail(f"The rejected identity-vault contract remains a current build gate: {rejected_gate}")
+
+    package_script = read("scripts/package.ps1")
+    for required in (
+        "CHANGELOG.md",
+        "LICENSE",
+        "README.md",
+        "SMOKE-TEST-GUIDE.md",
+        "strict eight-file standalone UMM archive",
+        "PDB symbols must not be included",
+    ):
+        if required not in package_script and required not in read("scripts/README.md"):
+            fail(f"Current package tooling is missing {required!r}.")
+
+    package_validator = read("scripts/validate-package.ps1")
+    for required in (
+        "The standalone UMM package must contain exactly one binary",
+        "Package entries do not match the strict eight-file allowlist",
+        "SMOKE-TEST-GUIDE.md",
+    ):
+        if required not in package_validator:
+            fail(f"PowerShell package validator is missing {required!r}.")
+
+    qualifier = read("scripts/qualify-runtime-candidate.ps1")
+    for forbidden in (
+        "0.0.18-s18-runtime-smoke-candidate",
+        "NoGoIncomplete",
+        "blockedRowsBeforeRuntimeTest",
+    ):
+        if forbidden in qualifier:
+            fail(f"Current qualification script retains obsolete persistence-era text: {forbidden}")
+    for required in (
+        "Completed 455 tests; failures=0.",
+        "Same-output-path deterministic compilation failed",
+        "sprint23Blocked = $true",
+        "attack-hook-repair-smoke-test.zip",
+    ):
+        if required not in qualifier:
+            fail(f"Current qualification script is missing {required!r}.")
+
+    if read("SMOKE-TEST-GUIDE.md") != read("SMOKE-TEST-GUIDE-0.0.22.1.md"):
+        fail("SMOKE-TEST-GUIDE.md must match the versioned 0.0.22.1 guide exactly.")
+
+    for required in (
+        "SPRINT-22-REPORT.md",
+        "SPRINT-22-REPAIR-REPORT-0.0.22.1.md",
+        "SMOKE-TEST-GUIDE-0.0.22.md",
+        "SMOKE-TEST-GUIDE-0.0.22.1.md",
+        "planning/SPRINT-23-ENTRY-CRITERIA.md",
+        "docs/decisions/ADR-0028-exact-rule-event-contract-and-weapon-only-reconciliation.md",
+        "evidence/sprint22-repair/runtime-result-assessment.md",
+        "evidence/sprint22-repair/exact-runtime-contracts.json",
+    ):
+        read(required)
+
+    contracts = json.loads(read("evidence/sprint22-repair/exact-runtime-contracts.json"))
+    expected_assembly_hash = "3b6450ffec440e296e586f71c711b195aed144b28d53e1cbb29406d18fef5afb"
+    if contracts.get("assembly", {}).get("sha256") != expected_assembly_hash:
+        fail("Exact-runtime contract evidence references the wrong Assembly-CSharp.dll hash.")
+    expected_source_hashes = {
+        "ruleEventPatchContractSha256": sha256(ROOT / "src/KingmakerGunslinger/Diagnostics/RuleEventPatchContract.cs"),
+        "ruleEventPatchTargetSha256": sha256(ROOT / "src/KingmakerGunslinger/Diagnostics/RuleEventPatchTarget.cs"),
+        "tokenReconciliationPatchSha256": sha256(ROOT / "src/KingmakerGunslinger/Firearms/FirearmStateTokenReconciliationPatch.cs"),
+        "runtimeInspectionScriptSha256": sha256(ROOT / "scripts/inspect-runtime-contracts.ps1"),
+    }
+    if contracts.get("source") != expected_source_hashes:
+        fail("Exact-runtime contract evidence source hashes do not match the repair tree.")
+    for type_name in (
+        "Kingmaker.RuleSystem.Rules.RuleAttackRoll",
+        "Kingmaker.RuleSystem.Rules.RuleAttackWithWeapon",
+        "Kingmaker.RuleSystem.Rules.RuleCalculateAC",
+    ):
+        methods = contracts.get("ruleEventContracts", {}).get(type_name, [])
+        if len(methods) != 1 or not methods[0].get("repairedExactMatcherWouldAccept"):
+            fail(f"Exact-runtime contract evidence is incomplete for {type_name}.")
+        parameter_types = methods[0].get("signature", {}).get("parameterTypes", [])
+        if parameter_types != ["Kingmaker.RuleSystem.RulebookEventContext"]:
+            fail(f"Unexpected OnTrigger parameter contract for {type_name}: {parameter_types!r}")
+
+    screenshot_dir = ROOT / "evidence" / "sprint22-repair" / "runtime-screenshots"
+    for name, expected_hash in EXPECTED_SCREENSHOTS.items():
+        path = screenshot_dir / name
+        if not path.is_file():
+            fail(f"Runtime screenshot is missing: {path.relative_to(ROOT)}")
+        actual_hash = sha256(path)
+        if actual_hash != expected_hash:
+            fail(f"Runtime screenshot hash mismatch for {name}: {actual_hash}")
+
+    forbidden_extensions = {".dll", ".exe", ".pdb", ".mdb"}
+    for path in ROOT.rglob("*"):
+        if path.is_file() and path.suffix.lower() in forbidden_extensions and "evidence" not in path.parts:
+            fail(f"Source tree contains a compiled binary: {path.relative_to(ROOT)}")
+    forbidden_private_names = {
+        "Assembly-CSharp.dll",
+        "Assembly-CSharp-firstpass.dll",
+        "UnityModManager.dll",
+        "0Harmony12.dll",
+        "Newtonsoft.Json.dll",
+    }
+    for path in ROOT.rglob("*"):
+        if path.is_file() and path.name in forbidden_private_names:
+            fail(f"Source tree contains a private runtime reference: {path.relative_to(ROOT)}")
+
+    print("Sprint 22 repair source invariant validation passed.")
+    print("Blueprints: 12 stable, 11 active, 1 reserved.")
+    print(f"Compile items: {len(main_items)} main, {len(test_items)} test.")
+    print("Tests: 455 declared, including 27 Sprint 22 and 9 patch-target repair cases.")
+    print("Repair invariants: exact RulebookEventContext hooks and weapon-only token inspection.")
+    return 0
+
+
+if __name__ == "__main__":
+    try:
+        raise SystemExit(main())
+    except Exception as exception:
+        print("Sprint 22 repair validation FAILED: " + str(exception), file=sys.stderr)
+        raise SystemExit(1)
diff --git a/tools/validate_sprint22_repair_package.py b/tools/validate_sprint22_repair_package.py
new file mode 100755
index 0000000000000000000000000000000000000000..8cda0468f1a6c6a8eb766e1ebae5fbd6e3068aa9
--- /dev/null
+++ b/tools/validate_sprint22_repair_package.py
@@ -0,0 +1,115 @@
+#!/usr/bin/env python3
+"""Validate the standalone 0.0.22.1 Sprint 22 repair UMM ZIP."""
+from __future__ import annotations
+
+import argparse
+import hashlib
+import json
+import re
+import stat
+import zipfile
+from pathlib import Path, PurePosixPath
+
+EXPECTED = {
+    "KingmakerGunslinger/CHANGELOG.md",
+    "KingmakerGunslinger/Info.json",
+    "KingmakerGunslinger/KingmakerGunslinger.dll",
+    "KingmakerGunslinger/LICENSE",
+    "KingmakerGunslinger/README.md",
+    "KingmakerGunslinger/SMOKE-TEST-GUIDE.md",
+    "KingmakerGunslinger/blueprints/blueprints.json",
+    "KingmakerGunslinger/blueprints/blueprints.schema.json",
+}
+FORBIDDEN_DLL_NAMES = {
+    "Assembly-CSharp.dll",
+    "Assembly-CSharp-firstpass.dll",
+    "UnityEngine.dll",
+    "UnityModManager.dll",
+    "0Harmony12.dll",
+    "Newtonsoft.Json.dll",
+}
+
+
+def sha256_bytes(data: bytes) -> str:
+    return hashlib.sha256(data).hexdigest()
+
+
+def sha256(path: Path) -> str:
+    digest = hashlib.sha256()
+    with path.open("rb") as handle:
+        for chunk in iter(lambda: handle.read(1024 * 1024), b""):
+            digest.update(chunk)
+    return digest.hexdigest()
+
+
+def main() -> int:
+    parser = argparse.ArgumentParser()
+    parser.add_argument("archive", type=Path)
+    parser.add_argument("--expected-dll-sha256", required=True)
+    args = parser.parse_args()
+    if not args.archive.is_file():
+        raise FileNotFoundError(args.archive)
+
+    with zipfile.ZipFile(args.archive) as archive:
+        infos = [info for info in archive.infolist() if not info.is_dir()]
+        names = [info.filename for info in infos]
+        if len(names) != len(set(names)):
+            raise RuntimeError("Archive contains duplicate file entries.")
+        if set(names) != EXPECTED:
+            raise RuntimeError(f"Unexpected archive entries: {sorted(set(names) ^ EXPECTED)}")
+
+        for info in infos:
+            path = PurePosixPath(info.filename)
+            if path.is_absolute() or ".." in path.parts or path.parts[0] != "KingmakerGunslinger":
+                raise RuntimeError(f"Unsafe archive path: {info.filename}")
+            mode = (info.external_attr >> 16) & 0xFFFF
+            if stat.S_ISLNK(mode):
+                raise RuntimeError(f"Archive contains a symlink: {info.filename}")
+
+        info_json = json.loads(archive.read("KingmakerGunslinger/Info.json"))
+        if info_json.get("Version") != "0.0.22.1":
+            raise RuntimeError("Info.json does not declare version 0.0.22.1.")
+        if info_json.get("ManagerVersion") != "0.32.4":
+            raise RuntimeError("Info.json does not retain Unity Mod Manager 0.32.4.")
+        if info_json.get("AssemblyName") != "KingmakerGunslinger.dll":
+            raise RuntimeError("Unexpected mod assembly name.")
+
+        blueprint_manifest = json.loads(archive.read("KingmakerGunslinger/blueprints/blueprints.json"))
+        entries = blueprint_manifest.get("entries", [])
+        if len(entries) != 12:
+            raise RuntimeError("Blueprint manifest does not contain twelve stable IDs.")
+        if sum(entry.get("status") == "active" for entry in entries) != 11:
+            raise RuntimeError("Blueprint manifest does not contain eleven active IDs.")
+        if sum(entry.get("status") == "reserved" for entry in entries) != 1:
+            raise RuntimeError("Blueprint manifest does not contain one reserved ID.")
+        for entry in entries:
+            if re.fullmatch(r"[0-9a-f]{32}", entry.get("guid", "")) is None:
+                raise RuntimeError("Blueprint manifest contains an invalid GUID.")
+
+        dll = archive.read("KingmakerGunslinger/KingmakerGunslinger.dll")
+        actual_dll_hash = sha256_bytes(dll)
+        if actual_dll_hash != args.expected_dll_sha256:
+            raise RuntimeError(
+                f"DLL hash mismatch: expected {args.expected_dll_sha256}, observed {actual_dll_hash}"
+            )
+
+        binaries = [
+            name for name in names
+            if Path(name).suffix.lower() in {".dll", ".exe", ".pdb", ".mdb"}
+        ]
+        if binaries != ["KingmakerGunslinger/KingmakerGunslinger.dll"]:
+            raise RuntimeError(f"Unexpected binary entries: {binaries}")
+        if any(Path(name).name in FORBIDDEN_DLL_NAMES for name in names):
+            raise RuntimeError("Archive redistributes a private runtime/compiler reference.")
+        if archive.testzip() is not None:
+            raise RuntimeError("Archive CRC validation failed.")
+
+    print("Sprint 22 repair UMM package validation passed.")
+    print(f"Archive SHA-256: {sha256(args.archive)}")
+    print(f"DLL SHA-256: {args.expected_dll_sha256}")
+    print("Entries: 8; binaries: project-owned KingmakerGunslinger.dll only.")
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/validation/static-validation.json b/validation/static-validation.json
index b48d2126949e49f208bba130b66885d9bd526899..4e38ef95c2e20b016c32025e17afa09d1839a261 100644
--- a/validation/static-validation.json
+++ b/validation/static-validation.json
@@ -9,67 +9,98 @@
       "stableIds": 12
     },
     "documents": {
+      "broken": [],
       "brokenLocalLinks": 0,
-      "localLinksChecked": 46,
-      "markdownFiles": 115,
+      "localLinksChecked": 38,
+      "markdownFiles": 119,
       "passed": true
     },
     "exactReferenceCompile": {
-      "classification": "ready-for-loaded-round-and-token-durability-smoke-test-after-exact-reference-compile",
+      "classification": "ready-for-sprint22-attack-hook-repair-smoke-test-after-exact-reference-compile",
       "compileExitCode": 0,
       "compilerStderrBytes": 0,
-      "modDllSha256": "25f8e1a25cf7871591f5dc3778b0732ebb5f03edec7b79c62775d05bf07b1660",
-      "passed": true
+      "modDllSha256": "36f46686856d2f0b04bf91ddd9665b47d9c6676de96af428bd0d697ec32fa463",
+      "passed": true,
+      "pdbSha256": "b76d3cb21da6d21154d756a95266cadb6e9c06b2790576be0b53b10a62ff2ee7",
+      "privateReferenceManifestVerified": true,
+      "sameOutputPathDllByteIdentical": true,
+      "sameOutputPathPdbByteIdentical": true,
+      "sameOutputPathRuns": 2,
+      "warningsAsErrors": true
+    },
+    "exactRuntimeContracts": {
+      "assemblyCSharpSha256": "3b6450ffec440e296e586f71c711b195aed144b28d53e1cbb29406d18fef5afb",
+      "passed": true,
+      "ruleEventSignature": "System.Void OnTrigger(Kingmaker.RuleSystem.RulebookEventContext)",
+      "weaponOnlyReconciliation": true
     },
     "installPackage": {
       "binaryCount": 1,
-      "classification": "READY FOR KINGMAKER SMOKE TEST",
+      "binaryEntries": [
+        "KingmakerGunslinger/KingmakerGunslinger.dll"
+      ],
+      "classification": "READY FOR KINGMAKER SPRINT 22 REPAIR SMOKE TEST",
       "crcError": null,
       "duplicateEntries": 0,
       "entryCount": 8,
-      "fileName": "KingmakerGunslinger-0.0.22-loaded-round-smoke-test.zip",
+      "fileName": "KingmakerGunslinger-0.0.22.1-attack-hook-repair-smoke-test.zip",
       "passed": true,
       "pathTraversalEntries": 0,
-      "sha256": "5a6fd105e78c0a8850b6988c4d1ee0edb7b4f6460d61a30a14db17d43d0447cd"
+      "privateDependenciesRedistributed": false,
+      "sha256": "83423ad78a5e62dd4e329c07c1f5ddb20b9bd49567690e5bab6f9495e7263541",
+      "symlinkEntries": 0
     },
     "sourceCleanliness": {
-      "csharpFiles": 140,
+      "csharpFiles": 141,
       "forbiddenBinariesOutsideEvidence": 0,
       "passed": true,
-      "powershellFiles": 14
+      "powershellFiles": 14,
+      "privateCompilerReferencesFound": 0
     },
     "sourceInvariants": {
+      "declaredTests": 455,
+      "mainCompileItems": 139,
       "passed": true,
+      "patchTargetRepairCases": 9,
+      "sprint22Cases": 27,
+      "testCompileItems": 83,
+      "validator": "tools/validate_sprint22_repair.py",
       "validatorOutput": [
-        "Sprint 22 source invariant validation passed.",
+        "Sprint 22 repair source invariant validation passed.",
         "Blueprints: 12 stable, 11 active, 1 reserved.",
-        "Compile items: 138 main, 82 test.",
-        "Tests: 446 declared, including 27 Sprint 22 cases."
+        "Compile items: 139 main, 83 test.",
+        "Tests: 455 declared, including 27 Sprint 22 and 9 patch-target repair cases.",
+        "Repair invariants: exact RulebookEventContext hooks and weapon-only token inspection."
       ]
     },
     "structuredFiles": {
-      "jsonFiles": 18,
+      "jsonErrors": [],
+      "jsonFiles": 24,
       "passed": true,
+      "xmlErrors": [],
       "xmlMsbuildFiles": 3
     },
     "tests": {
-      "declaredAndExecuted": 446,
+      "declaredAndExecuted": 455,
       "failures": 0,
       "passed": true,
       "repeatedOutputIdentical": true,
       "runs": 3,
-      "testExecutableSha256": "736f2b37cbeb1629442228c7c3564d7f2c964eac36ee6d806d00b316f00b31eb",
-      "testOutputSha256": "209f26caf959cc7a88015de5a98452a9d594a40962b982800b9a9e89cfdeb401"
+      "testExecutableSha256": "c613af5456939c225952014acb5b3927ead3f199628f731eb8d360baf48ec042",
+      "testOutputSha256": "5e004b6f22411a12e4d4706b4b7c21822e43ffbfb3a47b1d57310429339d9148"
     }
   },
-  "milestone": "0.0.22-s22-loaded-round-enforcement",
+  "milestone": "0.0.22.1-s22-attack-hook-repair",
   "runtimeClaimsPending": [
-    "quicksave token survival or exact restoration",
-    "loaded attack consumes exactly one round",
-    "empty and Wrecked attacks are forced to miss",
-    "loaded Broken firearm discharges and remains Broken",
+    "loaded token survives quicksave with conflicts=0 and faults=0",
+    "loaded token survives save, desktop exit, restart, and reload",
+    "one loaded firearm attack consumes exactly one round",
+    "empty and Wrecked firearm attacks are forced to miss",
+    "loaded Broken firearm discharges once and remains Broken",
     "native Heavy Crossbow remains unaffected"
   ],
-  "runtimeStatus": "READY_FOR_KINGMAKER_LOADED_ROUND_AND_QUICKSAVE_SMOKE_TEST",
-  "schemaVersion": 1
+  "runtimeStatus": "READY_FOR_KINGMAKER_SPRINT22_REPAIR_SMOKE_TEST",
+  "schemaVersion": 1,
+  "sprint23EntryApproved": false,
+  "sprint23Status": "BLOCKED_PENDING_LIVE_REPAIR_GATE"
 }
