namespace KingmakerGunslinger.Firearms
{
    /// <summary>
    /// Project-owned firearm family. It is deliberately independent of the
    /// crossbow category used by native presentation and animation code.
    /// </summary>
    internal enum FirearmHandedness
    {
        Unknown = 0,
        OneHanded = 1,
        TwoHanded = 2
    }
}
