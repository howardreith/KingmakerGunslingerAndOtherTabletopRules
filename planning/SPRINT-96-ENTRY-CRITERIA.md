# Sprint 96 marked native scatter volley

Each planned target receives one ordinary `RuleAttackWithWeapon`. A weak,
request-scoped marker suppresses only the ordinary per-attack firearm discharge,
records exactly one natural roll, forces native success false only for a
misfire, and returns exact hit/critical evidence to the existing aggregate
policy. Marker cleanup is mandatory on completion or fault.

This checkpoint does not create or grant the player action, perform the one
canonical discharge, apply aggregate condition/explosion, or unlock content.
It requires focused contract validation, repository validation, the complete
test suite, clean exact-reference Release build, strict package validation,
and staged safety audits.
