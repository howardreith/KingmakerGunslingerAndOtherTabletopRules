# Sprint 78 entry criteria: save-free corrupt token preservation

Extend the already runtime-qualified detached production-item lifecycle observer
with the remaining conservative-corruption boundary. Attach two different
registered native firearm-state token enchantments to one detached Early Pistol,
then ask the production repository to reconstruct it.

The repository must reject the ambiguous two-token state and preserve both
token enchantments as diagnostic evidence. It must not choose a state, clear a
token, mutate inventory, publish an item, or touch a save. Cleanup must remove
only the two exact temporary enchantment instances and dispose the detached
item.

Require focused lifecycle source checks, inherited repository validation, the
complete domain suite, runner/preflight checks, clean Release build, strict
package validation, exact mod load, and two fresh save-free lifecycle PASS runs.
