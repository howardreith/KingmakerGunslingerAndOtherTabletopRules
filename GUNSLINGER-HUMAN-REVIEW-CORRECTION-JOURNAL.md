# Gunslinger Human-Review Correction Journal

## 2026-08-20 intake and authority amendment

- Resumed isolated worktree on `codex/gunslinger-overnight-bugfixes` at exact
  published intake `e2e3d9ec941549a889a1e03a590e24241b745b7f`.
- Refreshed `origin/codex/gunslinger-overnight-bugfixes`; HEAD, local branch,
  and remote ref are exactly equal. Intake contains the requested checkpoint
  and release implementation ancestor.
- Worktree is clean and no unresolved Git lock was found.
- No Kingmaker process is running. The live Mods directory has no compatibility
  sentinel, no `Mods.kmg-compat-*` sibling exists, and no active compatibility
  transaction state was found. The read-only state probe returned nonzero only
  because `rg` correctly found no active-status record.
- Unchanged intake gates: repository validation PASS; dependency-free
  domain/reflection suite 1,160/1,160 PASS.
- Fresh human evidence supersedes the synthetic Acadamae completion claim, the
  icon aesthetic claim, and the acquisition pacing claim. P0 save failure is a
  release blocker pending exact evidence, not yet attributed to Focused Aim.
- Current issue: P0 unloadable Tenebrous Depths save.
- Next action: use only the exact Kingmaker save directory and existing catalog
  safety tools to enumerate metadata, timestamps, size, and hashes without
  opening or mutating any save; copy only an unambiguous affected save into an
  ignored transaction-owned evidence directory.
# 2026-08-20 - P0 save compatibility checkpoint

Proved the saved Focused Aim marker reconstructed through a blueprint with null FX links and failed in native `Buff.SpawnParticleEffect`. Initialized the stable blueprint's empty FX/resource fields, added regression coverage and an exact disposable-copy guarded scenario, and passed repository validation, `1161/1161`, clean Release, output, SoundBank, package, and strict-package gates. Affected-copy runs crossed the prior crash. A 600-second affected run and a same-area no-marker control both stalled after Tenebrous scene setup without another material serialization exception. Originals remained byte-identical and staged copies were removed. P0 prevention/compatibility is source-qualified; full recovery is still release-blocking. Next action: commit/publish this bounded P0 repair, then investigate the real Acadamae player-command path while preserving the P0 blocker.

# 2026-08-20 - Acadamae real player-path repair

The real prepared Summon Monster I variant reproduced the human failure: its outer `AbilityData.ParamSpellSlot` was null while the exact available Wizard slot remained on the `ConvertedFrom` parent. Eligibility returned `not-prepared`, retained full-round execution, armed no completion, and native spending missed the slot. The repair searches only the non-spontaneous invocation chain, requires the candidate slot's exact spell object in that chain and the same spellbook, then binds it at authoritative command construction. Bounded command diagnostics retain the exact eligibility fields and rejection status.

Repository validation, `1161/1161`, clean Release/output, SoundBank, deterministic package, and strict package gates pass. Fresh guarded runs `20260820T1346113518157Z-cac809535698401786e65c307f7be644` and `20260820T1348390845989Z-5c143a2e41254043849e50afa8aa4598` each passed 15/15 through native `UnitUseAbility.OnAction`: Standard presentation/execution, one exact slot spend, forced success, forced failure, DC 16, permanent ordinary fatigue, rest, Cord, cancellation, toggle-off, ineligible controls, and cleanup. Package/DLL SHA-256 were `0c05e106de99654ad9e63efb2a3ab1e589d0ae2d3d6f1308bd9249033a1d3eb4` / `19a87131fcb3b2e10e0c0024de693cfc5b9f7efb08d37c3802cc1d86f825d9dc`. External deployment state restored. Next action: commit/publish Acadamae, then re-audit all 30 acquisition routes.

# 2026-08-20 - thirty-item acquisition re-audit

Replaced the structurally distinct but clustered map with 30 exact fixed base-campaign targets whose normalized named-area density is at most two. The transaction removes only retired project-owned rows, preserves native/foreign row order and values, appends each exact item once, and rolls every touched array back on bootstrap failure. The read-only observer now consumes the authoritative production specs instead of a stale parallel target list. Production documentation and fixtures now identify The Last Word as a Pistol and Watch at the World's End as a Musket.

Repository validation, `1161/1161`, clean Release/output, SoundBank, deterministic package, and strict package gates pass. Guarded packaged run `20260820T1425182231173Z-observe-rare-firearm-acquisition` passed with 30 items, 30 targets, maximum normalized density 2, exactly one loot row and zero vendor rows per project unique. Package/DLL SHA-256 were `74b6c3b160520308f4cf53fa61c32c9d33d6d0b62f048f1477ad4de93c90155d` / `dae237d18e6b91bd86fc5b017fbcdad9d074c268dc5e06eb393d54a2b278b5af`. The exact pre-run live-mod backup `20260820T1425147446013Z` was restored and verified. Ordinary in-world accessibility and theme remain consolidated human checks. Next action: commit/publish acquisition, then split BTSL permanent stock to Honest Guy and support stock to Xelliren.

# 2026-08-20 - BTSL merchant responsibility repair

The exact four-table audit proved three symmetric publishers were responsible: central firearm/support stock, generic Eastern weapons, and generic Elven Branched Spears all targeted both merchant roles, while `VendorCatalogPublication` appended every addition. Production now assigns the six firearm/permanent rows plus module-enabled Eastern weapons and spears only to Honest Guy, and the exact six support stacks only to Xelliren. Xelliren retains explicit empty Eastern/spear desired specs to remove only stale project-owned rows. Stable type/name insertion preserves all retained native/foreign references and relative order; rollback retains the exact original arrays.

Repository validation, the new integration-order behavior, `1162/1162`, clean Release/output, SoundBank, deterministic package, and strict package gates pass. Guarded packaged run `20260820T1444126864934Z-observe-rare-firearm-acquisition` passed. Package/DLL SHA-256 were `24485e1762cdf75a5ee9f734b870a3916e5e21954485a3d23e99bc53d07450fb` / `de43f1941d499ea02ccf7c88c27257cdc1b60f6516b91d0f5de0e694af48fb44`. Backup `20260820T1444092863954Z` was restored and verified. Static publication applies to new or not-yet-materialized inventories; old materialized shops and actual list aesthetics remain human-gated. Next action: commit/publish BTSL, then replace firearm feat icons using the actual Nodachi template.
